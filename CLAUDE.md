# Albius — contexto del proyecto

> Este fichero es la fuente única de reglas y decisiones del proyecto. Cualquier divergencia entre lo que digo aquí y otro sitio se resuelve a favor de este documento.

## 1. Qué es

SaaS B2B multi-tenant para gestión de turnos en empresas de transporte urbano de viajeros.
Target: empresas medianas (50-300 conductores).
Roles: super_admin (autor del producto), jefe de tráfico (operativa diaria), conductor (móvil-first).

## 2. Diferenciadores

1. **Optimizador matemático** que respeta el convenio y maximiza preferencias individuales del conductor.
2. **Mercado oficial de intercambios** entre conductores con validación automática y aprobación del jefe.

## 3. Stack

- Frontend: React 19 + Vite 6 + TypeScript strict + Tailwind v4 + shadcn/ui (estilo `new-york`, base `slate`).
- Backend: Firebase (Auth + Firestore + Cloud Functions Node).
- Optimizador: **Python + Pyomo + HiGHS** (validado en spike a escala TUCARSA; **OR-Tools descartado**) en Cloud Run. Motor MILP de assignment plaza↔conductor; servicio FastAPI en `services/optimizer/` (B29 Fase A). Su DEMANDA se deriva de `TipoTurno.tiposDiaAplicables` + `resolverTipoDia` (calendario, B27); el optimizador Python reimplementa `resolverTipoDia` (el contrato compartido es la colección `festivos`, no la función TS).
- Despliegue: Vercel con CI/CD automático desde `main`.

## 4. Identificadores del proyecto

- Firebase project ID: `albius-cbdb1`
- Repositorio: https://github.com/aangosto/albius
- Despliegue Vercel: https://albius-web.vercel.app

## 5. Estructura del repo

```
albius/
├── apps/
│   ├── web/                          ← React 19 + Vite 6 + TS + Tailwind 4 + shadcn/ui
│   │   ├── src/
│   │   │   ├── App.tsx, main.tsx, router.tsx
│   │   │   ├── components/{auth,layout,tenants,centros,usuarios,lineas,tiposTurno,conductores,ausencias,cuadrante,calendario,shared,ui}/  ← ProtectedRoute + Sidebar/Topbar (B34.3: `SidebarNav` extraído del Sidebar y compartido con `MobileNavDrawer`, drawer móvil sobre primitivos de Radix Dialog) + 4 componentes tenants/ + 4 componentes centros/ (B12) + 10 componentes usuarios/ (B14: TenantCentroSelect, UsuariosTable, JefeForm, ConductorForm, UsuarioEditForm, CrearUsuarioDialog, EditarUsuarioDialog, CambiarEstadoUsuarioDialog; B21: MultiSelectCheckbox, ConductorCamposOperativos) + 3 componentes lineas/ (B17: LineasTable, LineaForm, LineaFormDialog) + 4 componentes tiposTurno/ (B19: TiposTurnoTable, TipoTurnoForm, TipoTurnoFormDialog, CambiarEstadoTipoTurnoDialog) + 3 componentes conductores/ (B22: ConductoresTable, ConductorEditForm, ConductorEditDialog; B32.2: enlace "Ausencias" por fila) + 4 componentes ausencias/ (B32.2: AusenciasTable, AusenciaForm, AusenciaFormDialog, EliminarAusenciaDialog) + cuadrante/ (B33.1: CambiarEstadoCuadranteDialog, un dialog parametrizado publicar/cerrar/reabrir) + calendario/ (B33.2: EditarCeldaDialog, edición manual de una celda conductor×día; los sub-componentes de Mi horario B34.2 viven dentro de `MiHorarioPage.tsx`, sin carpeta propia) + 3 componentes festivos/ (B35.1: FestivosTable, FestivoFormDialog con el form inline, EliminarFestivoDialog) + shared/ (NoAutorizadoView extraído en B14, LoadingShell extraído en B15) + 13 shadcn ui (button, card pre-B10; input, label, dialog, table, select, checkbox, alert, badge en B10; textarea en B17)
│   │   │   ├── contexts/AuthContext.tsx  ← Provider + hook useAuth (Firebase Auth Web SDK)
│   │   │   ├── layouts/AppLayout.tsx
│   │   │   ├── lib/                      ← firebase (B29 C.4: +functionsEu, 2ª instancia getFunctions europe-west1 para generarCuadrante — D6.15), auth-errors, navigation (B22: /conductores solo en nav jefe), utils, callable-errors (B10), services/{tenants,centros,usuarios,lineas,tiposTurno,conductores,cuadrantes,ausencias,convenio,miHorario,festivos}.ts (B10/B12/B14/B17/B19/B22/B29/B32.2/B33.2 [cuadrantes: +publicar/cerrar/reabrir B33.1, +crear/actualizar/eliminarAsignacion B33.2; convenio: solo lectura get-by-id], única superficie I/O Firebase — D4.9; miHorario.ts B34.2: solo lectura del conductor, queries que REPLICAN las reglas self-only — D6.27), calendario.ts (B30.3: helpers puros UTC de la vista Calendario; B33.2: + avisos no bloqueantes de habilitación/ausencia/descanso entre jornadas)
│   │   │   ├── pages/                    ← 16 páginas (Login + 14 rutas autenticadas + NotFoundPage); **MiHorarioPage real (B34.2: próximos turnos + mes en lista vertical + ausencias cruzadas + resumen; móvil primero)**; CuadrantePage real (B29 C.4: vista del cuadrante + botón generar + feedback reactivo onSnapshot — D6.16); CalendarioPage (B30.3: rejilla conductor×día coloreada por línea, UTC; B33.2: herramienta de trabajo — todos los conductores del centro, celdas clicables en borrador → EditarCeldaDialog); CuadrantePage +botones publicar/cerrar/reabrir (B33.1) + aviso "generado con un convenio anterior" y KPI de findes excedidos + aviso de antelación en el dialog de publicar (B35); **ConvenioPage** (B35.1: formulario plano del singleton, grupos "aplicado" / "registrado, aún no aplicado"); **FestivosPage** (B35.1: molde Ausencias, tenant-wide y oficiales en solo lectura); AusenciasPage (B32.2: CRUD de ausencias, filtros conductor/categoría/mes, ?conductorId=)
│   │   │   └── index.css                 ← Tailwind v4 + tema shadcn
│   │   ├── e2e/                          ← tests E2E Playwright (B20, D6.7): playwright.config.ts (raíz apps/web) + global-setup/auth.setup (storageState jefe + super_admin B21 + conductor B34.2) + *.smoke.spec/*.spec (tipos-turno B20, conductor B21, conductores B22, calendario B33.2, mi-horario B34.2, mobile.smoke B34.3 a 375px, convenio + festivos B35.1) + helpers/seed.ts; scripts test:e2e en package.json
│   │   ├── vercel.json                   ← rewrite SPA catch-all a /index.html (B31; sin él, F5 en una ruta profunda daba 404 en Vercel)
│   │   └── .env.example                  ← plantilla VITE_FIREBASE_* + VITE_USE_EMULATORS
│   └── functions/                    ← Cloud Functions Node
│       ├── src/                          ← 39 functions = 38 callables + 1 worker (ping; crearJefeTrafico/crearConductor/actualizarConductor/marcarPasswordCambiada/actualizarUsuario; crearTenant/actualizarTenant; crearCentro/actualizarCentro; crearLinea/actualizarLinea; crearTipoTurno/actualizarTipoTurno; crearFrecuencia/actualizarFrecuencia/crearFrecuenciaExcepcional/actualizarFrecuenciaExcepcional; guardarConvenio; crearCuadrante/publicarCuadrante/cerrarCuadrante/**reabrirCuadrante** (B33.1) + crearAsignacion/actualizarAsignacion/eliminarAsignacion/crearAsignacionesLote B26 + actualizarCuadrante/eliminarAsignacionesCuadrante/regenerarAsignacionesCuadrante B28; crearFestivo/actualizarFestivo/eliminarFestivo B27; crearAusencia/actualizarAusencia/eliminarAusencia B32.1; **generarCuadrante** callable europe-west1 B29) + tasks/generarCuadranteWorker.ts (**onTaskDispatched**, worker async europe-west1 B29 — D6.14) + optimizer/ (client.ts cliente HTTP autenticado al Cloud Run + buildRequest.ts composición del OptimizarRequest desde Firestore [B32.3: + ausencias[] expandidas a días + exclusión del pool de los ausentes del mes completo] + contract.ts tipos espejo de schemas.py, B29 — D6.13) + calendar.ts (espejo runtime de resolverTipoDia + expandirAusenciaEnMes para el worker) + logic/ (asignaciones.ts, cuadrante.ts — escritura reutilizable D6.12, B28; +marcarEstadoGeneracion B29 C.2) + helpers (auth-guards, validation, refs, cif-validator, collections). `.env` (OPTIMIZER_URL vía defineString, gitignored)
│       └── scripts/                      ← testing infra (verify-*, seed-*, reset-*, read-* p.ej. seed/reset-tipos-turno-b19 para E2E B20 [+ tiposDiaAplicables B27]; verify/seed/reset/read-*-conductor-b21 para B21; reset-conductores-b22 para B22; verify-frecuencias para B23; verify-convenio para B25; verify-cuadrante para B26; verify-festivos para B27; verify-cuadrante-b28 para B28; verify-ausencias para B32.1; verify-cuadrante ampliado a 60 casos + reset-cuadrante-b33 para E2E en B33; verify-reglas-conductor B34.1 — sonda de REGLAS con el SDK de cliente, no Admin; reset-mi-horario-b34 para E2E de Mi horario, siembra relativo a HOY) contra emulator
├── packages/
│   └── shared/                       ← @albius/shared — tipos del modelo + validators/cif + calendar/{resolverTipoDia (B27), expandirAusenciaEnMes (B32.3)} + tests vitest (src/**/*.test.ts; firebase-stubs.ts legacy pendiente de retirar, ver §12)
├── scripts/                          ← scripts CLI standalone para PRODUCCIÓN (bootstrap-super-admin.mjs; seed-caso-prueba.mjs B29 C.4 — siembra el caso de prueba del optimizador [B30.2 +5 líneas con color; B32.3 +20 ausencias]; inspect-produccion.mjs B31 — inspector READ-ONLY: conteos por colección, cuadrantes y diagnóstico de asignaciones huérfanas). Guardas: --target production + project_id===albius-cbdb1 + detección ADC multiplataforma (D6.17)
├── services/
│   └── optimizer/                    ← servicio Python del optimizador (B29 Fase A): FastAPI + Pyomo + HiGHS. TOP-LEVEL fuera de npm workspaces (otro runtime/deps/despliegue a Cloud Run). optimizer.py (motor MILP + descomposición semanal) + main.py + schemas.py + requirements.txt + README.md + sample_input.json. **DESPLEGADO a Cloud Run privado (--no-allow-unauthenticated) en europe-west1 (B29 Fase B)**: Dockerfile + .dockerignore; la URL se inyecta en functions vía OPTIMIZER_URL. scratch/ de medición ignorado (.gitignore; B35.2: dump_request_emulador.mjs vuelca el request REAL desde el emulador vía construirOptimizarRequest compilado, run_b35_findes.py compara con/sin restricción)
├── infrastructure/
│   └── firestore/                    ← reglas activas para 14 colecciones (tenants, centros, usuarios, conductores, lineas, tipos_turno, frecuencias, frecuencias_excepcionales, convenio, cuadrantes, asignaciones, festivos, ausencias [B32.1], notificaciones [lectura self B29 C.2]) + helpers de auth + índices compuestos; resto deny-all. **Desplegadas a producción en B29 C.4.4** (reglas + índices; antes solo emulador)
├── docs/                             ← ver §7
├── package.json                      ← npm workspaces
├── tsconfig.base.json                ← strict + noUncheckedIndexedAccess + noImplicitOverride
├── eslint.config.js                  ← ESLint 9 flat config
└── .prettierrc, .nvmrc (Node 22, B31)
```

## 6. Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run lint`
- `npm run format`

Detalle en `README.md`.

## 7. Documentos de referencia (`/docs`)

- `01-especificacion-funcional.pdf` — visión, módulos del MVP, arquitectura, plan de 16 semanas.
- `02-modelo-de-datos.pdf` — 22 colecciones Firestore con campos, ejemplos, reglas e índices.
- `03-modelo-de-datos.ts` — interfaces TS del modelo (idénticas a `packages/shared/src/types.ts`).
- `04-bocetos-pantallas.html` — 8 wireframes (4 web del jefe + 4 móvil del conductor).

## 8. Decisiones tomadas (no cuestionar sin avisar)

Si propones algo que contradice un punto de aquí, primero levanta la mano para abrir conversación.

- **Aislamiento multi-tenant con campo `tenantId` en cada documento Firestore** (colecciones planas), no subcolecciones anidadas bajo cada tenant.
- **Servicios materializados selectivamente:** solo se crea documento `servicios` cuando hay asignación o incidencia que registrar. El resto se calcula al vuelo desde `frecuencias`.
- **Intercambios solo inmediatos en MVP:** únicamente intercambios sobre fechas ya publicadas. Los intercambios diferidos (compromisos futuros entre conductores) quedan fuera.
- **Horizonte del cuadrante: 2 meses publicados.** Los intercambios solo son posibles dentro de ese horizonte.
- **Optimizador en dos fases:** heurística greedy con scoring en MVP (suficiente hasta ~200 conductores); formulación MILP con OR-Tools en V2. Interfaz estable para cambiar de motor sin tocar el resto del sistema.
  - **NOTA DE ACTUALIZACIÓN (B23, revisar al diseñar el bloque Optimizador):** (a) **La DEMANDA del optimizador en MVP sale de los TIPOS DE TURNO activos por día, NO de las frecuencias.** Las `frecuencias` (B23) son dato **descriptivo** del servicio (intensidad de paso); el puente frecuencias→expediciones→turnos (vehicle + crew scheduling) queda fuera del MVP. El "servicios materializados desde frecuencias" del bullet anterior es el cálculo de expediciones para visualización/operativa, NO el insumo directo del optimizador de turnos. (b) **Un spike Pyomo validó que MILP directo (assignment plaza↔conductor con preferencias) es viable a escala real (~3-4 min con 178 conductores)**, así que el optimizador MVP podría ser **MILP directo en vez de la heurística greedy** que este bullet preveía — reevaluar greedy-vs-MILP-directo cuando se diseñe el bloque, con la interfaz estable de motor intacta. (c) **El `Convenio` (B25) define POR CENTRO los parámetros que el optimizador leerá como restricciones** (descanso entre jornadas, max horas semanales/anuales, días libres, etc.); el optimizador resolverá el `maxHoras` efectivo de cada conductor como `conductor.maxHorasSemanales ?? convenio.maxHorasSemanales` (el convenio pone el límite del centro, el conductor lo sobreescribe individualmente). (d) **NOTA B26 (la "mesa" ya existe):** el optimizador escribirá las asignaciones que decida vía `crearAsignacionesLote` sobre un `Cuadrante` en estado `borrador`, y los KPIs del resultado (cobertura, satisfacción, preferencias cumplidas) en `Cuadrante.estadisticas` vía `actualizarCuadrante` (callable aún por implementar — `TODO[actualizar-cuadrante-estadisticas]`). **Grano mensual:** un `Cuadrante` cubre un centro × un mes (id `cua_{centroId}_{año}_{mes}`); el horizonte de 2 meses publicados se materializa como 2 documentos `Cuadrante`. El descanso ordinario del conductor NO se materializa (ausencia de asignación = día libre, D6.10). (e) **NOTA B27 (la DEMANDA y el CALENDARIO ya existen como dato):** la demanda del optimizador para un día concreto = los **tipos de turno activos del centro cuyo `tiposDiaAplicables` incluye el `tipoDia` de ese día** (1 plaza por tipo y día; fiel a TUCARSA donde cada código de turno aparece máx. 1 vez/día). El `tipoDia` de cada fecha se resuelve con `resolverTipoDia(fecha, festivosAplicables)` (`@albius/shared/calendar`, función pura: el festivo gana sobre el día de semana; el caller pre-filtra los festivos del centro). La colección `festivos` ya está habilitada (CRUD por callables; **frontend pendiente**, `TODO[festivos-frontend]` — al inicio se siembran por seed). (f) **NOTA B28 (la escritura del lado Node está LISTA):** el optimizador (B29) llamará, desde un orquestador Node, a `regenerarAsignaciones` (limpiar+volcar el plan) y a `actualizarCuadranteDoc` (KPIs en `estadisticas` + bloque de generación `generadoPor`/`fechaGeneracion`/`modoGeneracion`) como **funciones internas** de `apps/functions/src/logic/` (patrón D6.12: escritura reutilizable sin `request.auth`), con `actorId='optimizador'`. Patrón de invocación previsto **C1** (la Function lee Firestore → pasa el payload al Python → recibe el plan → escribe; el Python NO toca Firestore), a cerrar en B29. Invocar Cloud Run requerirá `google-auth-library` (dep nueva) + decisión async (el spike tarda 3-4 min > timeout de un `onCall`). Lo único que falta para el optimizador es el motor (Python/OR-Tools/Cloud Run) y el orquestador que pegue lectura→Python→escritura. (g) **NOTA B29 (CERRADO, FUNCIONANDO EN PRODUCCIÓN):** el optimizador está completo end-to-end. **Arquitectura: Opción C + C1** (el motor Python computa y devuelve un PLAN; una Cloud Function Node lee Firestore, llama al Python autenticado y ESCRIBE vía B28; el Python NO toca Firestore — D6.13). **Async vía Cloud Tasks** (`onTaskDispatched`, techo 30 min) porque el motor tarda ~5 min: `generarCuadrante` (callable corto que encola) + `generarCuadranteWorker` (worker largo) — D6.14. **Infra:** Cloud Run privado en **europe-west1** (región dentro de eur3), SA default compute (hardening a SA dedicada pendiente). **Objetivo MVP**: cobertura + equidad (`W_PREF` desactivado, preferencias diferidas); Pyomo+HiGHS; descomposición semanal con cosido de fronteras; **Forma 1** de demanda (cada `TipoTurno` = plaza nominal única; el modelo B27 `tiposDiaAplicables` es suficiente). **Validado en producción** con el caso líneas 1-5 TUCARSA (60 conductores, sept 2026): **916 asignaciones, 100% cobertura**, mostradas en la web real.
- **Una única aplicación web responsive** sirve a los tres roles desde móvil y escritorio. Sin app nativa separada en V1; en V2 se podría empaquetar con React Native reutilizando código.
- **Acceso multidispositivo para todos los roles:** super_admin, jefe de tráfico y conductor acceden a la misma aplicación web responsive desde cualquier dispositivo (móvil o escritorio), con las mismas capacidades funcionales.
- **Sin mapa interactivo en MVP:** las paradas se gestionan con texto y coordenadas opcionales. Visualización cartográfica queda para V2.

## 9. Convenciones de código

- Idioma de la UI: español.
- Variables del dominio en español (`conductor`, `cuadrante`, `asignacion`); identificadores técnicos genéricos en inglés.
- Comentarios TODO con etiqueta de contexto: `TODO[auth]`, `TODO[optimizer]`, `TODO[i18n]`, etc. Permite localizar pendientes de un módulo con un grep.
- Componentes shadcn se incorporan vía CLI (copian fuente al repo), no como dependencias npm.

## 10. NO hacer sin pedir confirmación explícita

- Instalar nuevas dependencias.
- Modificar el modelo de datos en `packages/shared/src/types.ts`.
- Tocar las reglas de seguridad de Firestore (`infrastructure/firestore/firestore.rules`).
- Hacer `git push`.
- Borrar archivos.

> Decisiones §10 aprobadas (registro): **Playwright** (B20, E2E de UI) y **vitest** (B27, primer testing unitario en `packages/shared`, D6.11) se añadieron como dependencias dev con OK explícito del usuario. **exceljs** (B36.2, Excel del cuadrante en `apps/web`, cargado con `import()` en chunk diferido) se añadió como dependencia de runtime con OK explícito; elegido sobre `xlsx` porque colorea celdas y fija paneles, y su paquete npm está mantenido.

## 11. Estado actual del proyecto

**Hecho:**
- Scaffold monorepo (npm workspaces): `apps/web`, `apps/functions`, `packages/shared`, `infrastructure/firestore` (vacío).
- `apps/web` operativa: React Router v7, AppLayout con Sidebar + Topbar, 10 páginas placeholder, login deshabilitado.
- Tipos del modelo accesibles vía `@albius/shared`; CI/CD a Vercel activo desde `main`.
- Cloud Functions Node con 3 callables (`ping`, `crearJefeTrafico`, `crearConductor`), helpers reutilizables (auth-guards, validación sin Zod por D3.4, refs Firestore), custom claims operativos en backend y scripts de verificación contra emulators (42 casos pasados).
- Script CLI `scripts/bootstrap-super-admin.mjs` para alta de super_admins con 6 capas de fail-safe contra accidentes en producción (capas 1-3 verificadas empíricamente contra emulator con 11/11 casos; capas 4-6 deferred a verificación con Firebase real, ver `TODO[bootstrap-verify-production-layers]` en §12).
- Login funcional en `apps/web` con `useAuth` hook + `AuthContext` (Firebase Auth Web SDK + `signInWithEmailAndPassword`). Soporta emulator local vía `VITE_USE_EMULATORS=true`. Verificación manual contra emulator OK: login con super_admin, signOut, persistencia de sesión tras reload, mensajes de error genéricos para credenciales inválidas (decisión de seguridad: no revelar si el email está registrado).
- Reglas Firestore activas para 4 colecciones (tenants, centros, usuarios, conductores) con helpers `isSuperAdmin`/`isJefeTrafico`/`ownerOfDoc`/`sameTenant` y aislamiento por tenant. El resto del modelo tiene deny-all explícito por colección. Origen: previo a Bloque 6 (no rastreado en historial granular del proyecto; descubierto durante PASO 3 del Bloque 7).
- Routing protegido por auth y redirección por rol operativa (Bloque 6). `ProtectedRoute` como layout route gatea `status` + `user.rol`; `LoginPage` centraliza redirección post-login y redirect inverso con `homeForRol()`. Sidebar y Topbar leen `useAuth()`: sidebar muestra items por rol vía `NAV_BY_ROL` (super_admin con secciones Gobierno+Operativa, jefe_trafico con 7 items, conductor con 2), topbar muestra nombre/email + `ROL_LABEL` + signOut funcional. `ClaimsIncompletosView` para el edge case autenticado sin rol. 3 placeholders nuevos para super_admin (Tenants/Centros/Usuarios) + `NotFoundPage` catch-all con CTA inteligente. Eliminados `USUARIO_PLACEHOLDER` y `RolNavegacion` en favor de `Rol` del dominio (`@albius/shared` como SSOT). Seed extendido (`seed-test-user.mjs`) crea 4 users (super_admin/jefe_trafico/conductor/sinclaims) + docs de referencia (tenant/centro/conductor). 16/17 casos verificados contra emulator (caso 11 N/A justificado).
- Forzado de cambio de password en primer login operativo (Bloque 7). AuthContext hidrata el flag `passwordChangeRequired` leyendo `/usuarios/{uid}` con Firebase Web SDK directamente (apoyado en regla `ownerOfDoc` existente). ProtectedRoute añade dos gates: usuarios con flag=true son enviados a `/cambiar-password` antes de cualquier otra ruta privada; usuarios sin flag intentando entrar son redirigidos al home (gate inverso filtra cambio voluntario). CambiarPasswordPage full-screen fuera de AppLayout, form con mínimo 10 caracteres + confirmación + manejo de `auth/weak-password` y `auth/requires-recent-login`. Callable `marcarPasswordCambiada` idempotente cierra el ciclo (pone flag=false + registra `passwordCambiadaEn`). `refreshAuthUser()` en AuthContext re-lee /usuarios tras mutación server-side. Refactor del seed parametriza el flag por user (`jefe`=true verifica Bloque 7; `admin`/`conductor`=false para tests rápidos de Bloque 6). Modelo `Usuario` extendido con `passwordCambiadaEn?: Timestamp`. **Sesión 3 completa.**
- Setup canónico de Sesión 4 (Bloque 8): tipos del modelo `Tenant` y `Centro` extendidos con auditoría completa (`creadoPor`/`creadoEn` retroactivos de D3.7 + `actualizadoPor`/`actualizadoEn` nuevos de D4.1, todos opcionales para retrocompat). Campo `cifValidacionForzada?: boolean` añadido a `Tenant` para trazabilidad del escape hatch de validación de CIF. Validador de CIF español (categoría + dígito control, BOE RD 1065/2007 anexo VI) en triple copia coordinada por `TODO[refactor-shared-build]`: `packages/shared/src/validators/cif.ts` (canónico aspiracional, no consumido todavía) + `apps/functions/src/cif-validator.ts` (consumirá B9) + `apps/web/src/lib/validators/cif.ts` (consumirá B10). Cabecera de cada archivo recuerda mantener sincronizadas las 3 copias; integridad verificable con SHA256 (las 3 copias tienen hash idéntico tras B8). 8/8 + 4/4 PASS del algoritmo verificados empíricamente contra CIFs reales (Telefónica `A28017895`, UCM `Q2818014I`) y sintéticos cubriendo las 3 ramas de categoría (REQ_NUM, REQ_LETRA, flexible) más 3 categorías de error (control-mismatch, category-letter, length/digits). Reglas Firestore refinadas (defensa en profundidad sobre los callables que vendrán, D6.1): `update` en `/tenants` y `/centros` veta cambios sobre campos inmutables (`id`, `cif`, `fechaAlta`/`fechaCreacion`, `creadoPor`, `creadoEn`, y `tenantId` del Centro) reutilizando el helper `fieldsChanged` existente. Dry-run de `firebase deploy --only firestore:rules` verde. 4 canónicas en §13: D4.1 (auditoría UPDATE), D4.2 (defaults backend), D4.3 (soft-delete vía UPDATE), D4.4 (validators de dominio + patrón escape hatch). 3 TODOs nuevos en §12.
- Backend Tenants operativo (Bloque 9): callables `crearTenant` + `actualizarTenant` en `apps/functions/src/callables/`. `crearTenant` aplica defaults backend D4.2 (`estado='activo'`, `plan='basico'`, `configuracion={Europe/Madrid, es}`), valida CIF con escape hatch D4.4 (`forzarCIF: true` + persistencia de `cifValidacionForzada: true`; Opción C3 silenciosa cuando válido + forzar — omit del flag), verifica unicidad de CIF entre tenants (`assertCIFUnico` en `refs.ts`), id auto-generado por Firestore, auditoría D3.7. `actualizarTenant` aplica auditoría D4.1 (`actualizadoPor`/`actualizadoEn` en cada write, incluso no-ops), soft-delete D4.3 vía cambio de `estado` con verificación D4.6 de centros activos (`failed-precondition` si los hay), reactivación con `FieldValue.delete()` sobre `fechaCancelacion`, `configuracion` replace completo D4.5 en UPDATE. Validators dedicados con type guards a mano (D3.4) y 4 helpers nuevos en `validation.ts` (`assertOptionalBoolean`, `assertOptionalEnum`, `assertOptionalConfiguracion[Parcial|Completa]`, `assertAtLeastOneField`). Índice compuesto `(centros: tenantId+estado)` en `firestore.indexes.json` para la query de D4.6. Verify script `verify-tenants.mjs` con **19/19 PASS contra emulator** cubriendo las 4 ramas del escape hatch CIF + 3 transiciones de estado + autorización a 3 niveles + validaciones de payload. Convención del proyecto confirmada: `expectedCode` en verify scripts va en UPPER_SNAKE_CASE (formato del wire protocol HTTPS Callable v2), coherente con verify-jefe/conductor/marcarPassword. 2 canónicas nuevas D4.5 + D4.6 en §13. 2 TODOs nuevos en §12 (`deploy-firestore-indexes`, `validar-zona-iana`).
- Frontend Tenants completo (Bloque 10). `TenantsPage` con listado client-side (D4.8), filtros por estado + búsqueda case-insensitive, alta/edición en Dialog modal (D4.7), escape hatch CIF onBlur con checkbox dinámico (D4.12), soft-delete con Dialog destructivo separado (D4.11), reactivación inline. Servicio `apps/web/src/lib/services/tenants.ts` como única superficie I/O Firebase desde la UI (D4.9). Mapper de errores HttpsCallable centralizado en `apps/web/src/lib/callable-errors.ts` (D4.10). Gate suave de rol con patrón split component en `TenantsPage`+`TenantsPageAuthorized` (D4.13). 8 componentes shadcn nuevos en B10 (input/label/dialog/table/select/checkbox/alert/badge). Verify manual **25/25 PASS contra emulator** (caso 9 sobre bloqueo de cierre durante submit verificado por código, no por interacción — el callable en emulator local termina demasiado rápido para clickar la X). Bundle web 930.84 kB (gzip 256.43 kB; +126 kB por grafo Dialog/Select de Radix umbrella vs B7). 7 canónicas nuevas D4.7-D4.13 en §13 + 5 TODOs nuevos en §12 + correcciones a D4.4 / `TODO[validar-cif-consolidar-shared]` (doble copia) / `TODO[web-bundle-splitting]` (cifras actualizadas). **Sesión 4 completa.**
- Backend Centros operativo (Bloque 11, primer bloque de Sesión 5). Callables `crearCentro` + `actualizarCentro` paralelos a Tenants. `crearCentro` aplica validación jerárquica D5.1 (`assertTenantActivo` en `refs.ts`: el centro solo se crea en un tenant existente y activo) con semántica de códigos D5.2 (`invalid-argument` para "tenant no existe", `failed-precondition` para "existe pero estado bloqueante: suspendido/cancelado"). Sin validador de dominio (no hay CIF ni equivalente en Centro); defaults D4.2 trivializados (solo `estado='activo'` hard-coded). `actualizarCentro` aplica auditoría D4.1, soft-delete D4.3 vía `estado='inactivo'` con verificación D4.6 cascada (`assertNoConductoresActivosEnCentro`: bloquea si hay conductores en estados `activo`/`baja_temporal`/`vacaciones`; `baja_definitiva` NO bloquea, lista positiva self-documenting). Reactivación trivial sin `FieldValue.delete()` (Centro NO tiene `fechaCancelacion`). Coordenadas como `GeoPoint`: wire usa `{latitude, longitude}` (validado en `assertOptionalCoordenadas` con rangos [-90,90] y [-180,180]), conversión a `new GeoPoint(lat, lon)` diferida al momento de escritura (mantiene `validation.ts` libre de dependencias firebase-admin). Veto de inmutables explícito en validator (defensa en profundidad sobre reglas Firestore): `tenantId`, `id`, `fechaCreacion`, `creadoPor`, `creadoEn` con mensaje específico cada uno. Índice compuesto `(conductores: centroId+estado)` añadido a `firestore.indexes.json` para la query D4.6 con operador `in`. Verify script `verify-centros.mjs` con **22/22 PASS contra emulator** (11 casos `crearCentro` + 11 casos `actualizarCentro`); seed específico de B11 con 3 tenants (3 estados), 3 centros (con-bloqueantes/sin-conductores/solo-baja-definitiva) y 5 conductores con distribución crítica para validar la lista positiva. 8 callables totales operativos. 2 canónicas nuevas D5.1 + D5.2 en §13 (nueva sección "Bloque 5 — Centros backend + frontend"). 1 TODO nuevo en §12 (`centro-nombre-unicidad`) + 1 TODO ampliado (`delete-on-empty-fields` con mención de coordenadas).
- Frontend Centros completo (Bloque 12). `CentrosPage` paralela a `TenantsPage` con gate D4.13 split (super_admin only, `CentrosPageAuthorized` interno), listado client-side D4.8 sin paginación ni `onSnapshot`, Dialog modal D4.7 para alta/edición. Servicio `apps/web/src/lib/services/centros.ts` (D4.9 única superficie I/O Firebase). Carga paralela `Promise.all([listarCentros(), listarTenants()])` para hidratar selector de tenant padre + lookup de nombre del tenant en la tabla. Selector de alta muestra SOLO tenants activos (coherencia con D5.1: evita fricción frontend porque el backend rechazaría no-activos con failed-precondition). Form SIN Select de estado (D5.3 canónica nueva): centro tiene solo 2 estados, transiciones gestionadas únicamente con botones "Inactivar"/"Reactivar" + Dialog destructivo. Coordenadas con inputs `type="number" step="any"` y patrón ambos-o-ninguno (parseCoords devuelve `{coord, error}` para validación live + submit-blocking); rangos lat[-90,90] lon[-180,180] validados en cliente, backend gatea como segunda capa. `CentrosTable` con columna Tenant padre (sufijo " (suspendido)/(cancelado)" en `text-muted-foreground` si no-activo, opción sutil inline), icono `MapPin` de `lucide-react` (NO emoji) para indicar coordenadas con tooltip nativo `title=` en `<span>` envoltorio (más portable que `<title>` SVG). Filtros estado/tenant/búsqueda multicampo (incluye lookup de nombre tenant en búsqueda); dropdown de tenant solo lista los que tienen al menos 1 centro, derivado del listado completo (no se reduce al cambiar filtro de estado). `mapCallableError` reusado de B10 preserva el mensaje D4.6 ("los N conductores") en el Alert del Dialog cuando el backend rechaza inactivar. 4 componentes nuevos en `components/centros/` (CentrosTable, CentroForm, CentroFormDialog, CambiarEstadoCentroDialog) + servicio + page rewrite. Sin nuevas deps shadcn (Radix umbrella ya estaba en B10). Verify manual **22/22 PASS** contra emulator con seeds combinados de `verify-centros.mjs` (B11) + `seed-test-user.mjs` (B10), cubriendo gate D4.13 con 3 roles, listado + lookup, filtros, alta mínima + completa con GeoPoint, validación coords (solo-una + fuera-rango), edición (tenant inmutable, omit-only, sin cambios), soft-delete D4.6 cascada con conteo "3 conductores" visible en Dialog + reactivar, reset DI10.15 entre ediciones. Bundle web **943.18 kB** (gzip 256.72 kB; +12.34 kB lineal vs B10 — servicios y componentes sin nuevas deps Radix). 1 canónica nueva D5.3 en §13. **Sesión 5 completa.**
- Backend Usuarios operativo (Bloque 13, primer bloque de Sesión 6). Callable `actualizarUsuario` añadido a los 3 existentes (`crearJefeTrafico`, `crearConductor`, `marcarPasswordCambiada`). Edita campos no-críticos: `nombreCompleto`, `telefono`, `email`, `estado` (D4.3 soft-delete binario activo↔suspendido, patrón D5.3 sin Select). Identidad y claims VETADOS (D5.5): `rol`/`tenantId`/`centroId`/`conductorId` con mensajes que nombran callables dedicados futuros (`cambiarRolUsuario`, `moverUsuario`); `conductorId` inmutable de por vida (D3.1). Dual-write Auth+Firestore con rollback inverso (D5.4): orden Auth-primero (captura `email-already-exists` antes de tocar Firestore, sin rollback), si Firestore falla tras Auth ya escrito revierte Auth al valor previo; Auth se toca SOLO si `email`/`nombreCompleto` cambian de valor real (evita reset gratuito de `emailVerified`). Auditoría D4.1 (`actualizadoPor` + `actualizadoEn` nuevos en el modelo `Usuario`, opcionales para retrocompat). Reglas Firestore `/usuarios` endurecidas: rama super_admin veta inmutables (`id`/`creadoPor`/`creadoEn`/`fechaCreacion`); rama owner veta también `email` + `nombreCompleto` (canaliza la edición dual-homed obligatoriamente por callable, evita drift Auth↔doc); regla `read` self-only intacta (no-regresión Bloque 7). `assertUsuarioExists` en `refs.ts` devuelve `Usuario` (divergencia consciente vs `assert*Exists` hermanos para evitar doble lectura). Validators con vetos categorizados (identidad/claims + inmutables + passwords), sin helpers nuevos (`assertEmail` reusado de B3). Verify script `verify-usuarios.mjs` con **22/22 PASS contra emulator** (seed dual Auth+Firestore con 4 targets reales + 1 doc huérfano para A22; A17 valida en runtime el orden Auth-primero — doc.email intacto tras `ALREADY_EXISTS`; A3 confirma que el emulator resetea `emailVerified=false` igual que el SDK real). Suspensión SOLO cambia el doc, NO toca `Auth.disabled` ni `revokeRefreshTokens` (decisión MVP sin turnos asignados). 9 callables totales operativos. 2 canónicas nuevas D5.4 + D5.5 en §13 (nueva sección "Bloque 6 — Usuarios backend + frontend"). 4 TODOs nuevos en §12 (`email-verification-on-change`, `suspension-efectos-auth`, `perfil-self-edit-via-callable`, `dual-write-rollback-failed`). Housekeeping de §5 (estaba desactualizada desde B7: "3 callables" → 9, falta de `components/centros/`, `services/`, `callable-errors`).
- Frontend Usuarios completo (Bloque 14, segundo bloque de Sesión 6). UsuariosPage con gate D4.13 split como Tenants/Centros. Carga paralela 3 fuentes (`Promise.all([listarUsuarios, listarTenants, listarCentros])`) en `services/usuarios.ts` (D4.9). Listado con 7 columnas: nombre completo + teléfono subtexto, email, rol (Badge `ROL_LABEL` reusado de `navigation.ts`), tenant (lookup, "—" si super_admin), centro (lookup, "—"), estado (Badge default/secondary), acciones. Filtros: rol (4 opciones con super_admin)/tenant (derivado de usuarios DI14.9)/estado/búsqueda multicampo (4 campos incluido lookup `tenant.nombre`); sin filtro centro (Q15 evitar saturación). Alta partida en 2 botones separados "Nuevo jefe de tráfico" + "Nuevo conductor" (DI14.2) con `CrearUsuarioDialog` parametrizado por rol que muestra `JefeForm` o `ConductorForm` según corresponda. Edición role-agnostic (DI14.1) con `UsuarioEditForm`: rol/tenant/centro/conductorId read-only en cajas `bg-muted` con helper text único nombrando los callables futuros (D5.5). Soft-delete D5.3 binario vía `CambiarEstadoUsuarioDialog` (espejo CentroDialog). `TenantCentroSelect` (D5.7) sub-componente compartido entre `JefeForm` y `ConductorForm` con reset hijo encapsulado al cambiar padre + placeholder dinámico "(Sin centros — crea uno primero)" + submit bloqueado si sin centros. Pantalla éxito D5.6 en `CrearUsuarioDialog`: el `linkPasswordReset` se renderiza antes del refetch (background no-bloqueante con `.catch` que solo loggea, para que un fallo de refetch nunca oculte el secreto); cierre por overlay/Esc/X bloqueado durante pantalla éxito, solo botón Cerrar explícito. `NoAutorizadoView` extraído a `components/shared/NoAutorizadoView.tsx` (cierre del comentario en `CentrosPage.tsx:44`; refactor de 3 páginas como SUB-PASO 0). Guarda DI14.4: super_admin no es suspendible desde UI (solo Editar), divergencia consciente vs CentrosTable; Editar disponible en ambos estados activo+suspendido (un usuario suspendido puede necesitar corrección administrativa). Campos operativos del conductor (`lineasPreferentes`/`tiposTurnoPermitidos`/…) omitidos en alta y edición (Q4/Q11; backend defaultea `[]` en alta; volverán cuando exista CRUD de líneas/tipos-turno → `TODO[conductor-campos-operativos-en-alta]`). Verify manual **30/30 PASS** contra emulator con 3 tenants + 2 centros creados desde UI (M16 cambio tenant resetea centro, M17 tenant sin centros bloquea submit, M18 pantalla D5.6 con link real copiado, M22/M23 mensajes específicos `already-exists`, M26 cambio email end-to-end D5.4, M30 guarda super_admin; M1 SKIPPED — jefe seed con `passwordChangeRequired=true` correctamente desviado a `/cambiar-password` por B7, gate cubierto por M2 conductor). 9 callables (B14 no añadió callables). 2 canónicas nuevas D5.6 + D5.7 en §13. 1 TODO nuevo en §12 (`conductor-campos-operativos-en-alta`). Bundle web **961.23 kB** (gzip 261.81 kB; +18.24 kB lineal vs B12-fin por servicio + 10 componentes + 1 page rewrite sin nuevas deps Radix). **Sesión 6 completa.**
- Bundle splitting (Bloque 15, sub-bloque de infraestructura, primer bloque de Sesión 7). Cierra el TODO[web-bundle-splitting] PRIORITARIO abierto desde B7. router.tsx: 12 páginas autenticadas bajo AppLayout convertidas a React.lazy (las 3 fuera de AppLayout — Login, CambiarPassword, NotFound — siguen eager: viven fuera del Suspense boundary y CambiarPassword es flujo crítico B7). AppLayout: un único `<Suspense>` envolviendo `<Outlet/>` con fallback LoadingShell `fullscreen={false}` (sidebar+topbar visibles durante carga de chunk). LoadingShell extraído de ProtectedRoute a `components/shared/` con prop `fullscreen` (default true preserva comportamiento original de ProtectedRoute). vite.config.ts: manualChunks callback con regex anclado a segmento `[\\/]node_modules[\\/]<paquete>[\\/]` (separadores Windows; evita falsos positivos de lucide-react/@radix-ui/react-* que un `includes('react')` ingenuo capturaría) separa firebase-vendor (@firebase/firebase, 474.68 kB) + react-vendor (react/react-dom/react-router/scheduler, 285.67 kB), ambos cacheables entre deploys. Resultado: entry monolítico 961.23 kB → 18 chunks; entry/shell ahora 55.86 kB (-94.2%); páginas admin (Tenants/Centros/Usuarios 12-19 kB) + table Radix compartido (97 kB, hoisteado automáticamente por Rollup) + checkbox (4 kB) diferidos por ruta; 9 placeholders ~0.31 kB cada uno (crecerán independiente al rellenarse). Warning Vite de 500 kB eliminado. CSS único 35.93 kB (Tailwind v4 no se beneficia de split por ruta). Total JS +3.2 kB overhead de boilerplate de chunks (negligible). El peso crítico real al boot (entry+firebase+react ~816 kB) no baja — firebase se inicializa top-level y queda en camino crítico (ver `TODO[firebase-lazy-init]`) — pero ahora va en 3 chunks paralelos/cacheables en vez de 1 monolito, y el código de páginas (~130 kB) sí se difiere. Sin canónicas nuevas (infraestructura de build, no patrón de dominio). Verify empírico **8/8 PASS** contra emulator: primer paint solo carga entry+vendors+CSS (no chunks de página), navegación lazy carga el chunk al entrar, table compartido se cachea entre páginas admin, gate B7 intacto (jefe flag=true → /cambiar-password sin parpadeo), roles redirigen bien, F5 en ruta lazy profunda recarga limpia, ruta inválida → NotFoundPage eager. Sin nuevas deps. Bundle entry 55.86 kB / firebase-vendor 474.68 kB / react-vendor 285.67 kB.
- Backend Líneas operativo (Bloque 16, segundo bloque de Sesión 7). **Primer modelo OPERATIVO del proyecto** (cierra el arco gobierno→operativa: Tenants/Centros/Usuarios eran administrativos; Línea es operativa del jefe de tráfico). Modelo `Linea` extendido (D3.4 type guards, sin Zod): enum nuevo `TipoLinea` (`'urbana'|'cercanias'|'interurbana'`, eje de servicio); `+tipo`, `+vigenciaDesde?`/`vigenciaHasta?` (estacionalidad; ausente=vigente siempre), `+observaciones?`; `color` pasa a opcional con validación HEX si presente; `esNocturna` se mantiene `boolean` **ortogonal** a `tipo` (no mezclar ejes, error del PDF original); `fechaCreacion` ELIMINADO y migrado a `creadoEn` (D6.4 — primera entidad sin lastre); auditoría completa D3.7+D4.1 (`creadoPor`/`creadoEn`/`actualizadoPor`/`actualizadoEn`); `estado` enum-3 `'activa'|'inactiva'|'suspendida'` **requerido en CREATE, NO defaulteado** (una línea estacional puede nacer `'suspendida'`; D6.2, excepción consciente a D5.3). 2 callables nuevos (`crearLinea`/`actualizarLinea`, clones de `crearCentro`/`actualizarCentro` + auth de `crearConductor`) → **11 callables totales**. Auth: super_admin libre, jefe_trafico scoped a su tenant+centro (anti-cross, simétrico a `crearConductor` D3.6; en update el anti-cross se evalúa contra el doc, no el payload). `assertCentroActivo` nuevo en `refs.ts` (no existía; solo `assertTenantActivo`): D5.1/D5.2 (`invalid-argument` si centro no existe, `failed-precondition` si existe pero no activo). `assertCodigoLineaUnico` nuevo (D6.3, unicidad de `codigo` POR CENTRO; revalidada en update solo si `codigo` cambia, con `excludeLineaId` para no chocar consigo misma). Defaults D4.2 (`paradasIda`/`Vuelta` → `[]` en el callable). Validators nuevos: `assertOptionalColorHex` (regex `/^#[0-9A-Fa-f]{6}$/`), `assertOptionalISODate`, `assertVigenciaCoherente` (cruza desde<hasta); vetos de inmutables en update (`id`/`tenantId`/`centroId`/`creadoPor`/`creadoEn`). Reglas Firestore `/lineas` abiertas (deny-all sustituido): `read` tenant-scoped, `write` (create+update) jefe-scoped por tenant **patrón `/conductores`** (Línea es operativa del jefe, NO `/centros` super_admin-only) + veto de inmutables en update (defensa en profundidad D6.x — `/conductores` no lo tiene, aquí sí); regla tenant-scoped vs callable centro-scoped (la verdad operativa la impone el callable, la regla es defensa de último recurso). Índice compuesto `(lineas: centroId+codigo)` en `firestore.indexes.json` (unicidad + query de listado B17). Verify script `verify-lineas.mjs` con **33/33 PASS contra emulator** (17 `crearLinea` + 16 `actualizarLinea`: altas mínima/completa, validaciones de payload, D5.1/D5.2, D6.3 unicidad por-centro, auth 3 niveles + anónimo, vetos de inmutables, 3 transiciones de estado, auditoría D4.1, anti-cross del jefe). 3 canónicas nuevas D6.2/D6.3/D6.4 en §13 (nueva sección "Bloque 7 — Líneas"). 3 TODOs nuevos en §12 (`modelo-linea-paradas`, `linea-softdelete-cascada`, `unificar-fechacreacion-creadoen`). **Líneas backend-completo; frontend pendiente (B17).**
- Frontend Líneas completo (Bloque 17, tercer bloque de Sesión 7). **Líneas COMPLETO end-to-end** — primera entidad OPERATIVA con UI completa y primera página de `jefe_trafico` con CRUD (la trilogía Gobierno era super_admin-only). `services/lineas.ts` (D4.9): `crearLinea`/`actualizarLinea` (wire con `vigenciaDesde/Hasta` como ISO string) + `listarLineas(tenantId, centroId)`. `LineasPage` con gate D4.13 split **`rolRequerido='jefe_trafico'`** (NO super_admin — divergencia consciente vs Gobierno; el centro viene de claims, no se elige); listado client-side D4.8 con re-fetch tras mutación, filtrado por `tenantId`+`centroId` del jefe. `LineasTable`: `TIPO_LABEL`/`ESTADO_LABEL` exportados como SSOT de etiquetas, Badges tipo+estado, icono `Moon` si nocturna, `formatVigencia` (rango DD/MM/AAAA o "Siempre"); SIN columna ni filtro de centro (implícito por claims). Filtros estado/tipo/búsqueda (codigo+nombre). `LineaForm` + `LineaFormDialog` (clones de `CentroForm`/`CentroFormDialog`) **SIN selector de centro** (tenantId/centroId de claims) y **CON Select de estado enum-3** (D6.2, patrón Tenant D4.11) en alta Y edición — `'activa'` por defecto, alta directa `'suspendida'` soportada (caso estacional). Validación cliente espejo del backend: color HEX onBlur con preview + vigencia `desde<hasta` submit-blocking. `buildDelta` omit-only (DI10.13), reset por `key` (DI10.15). `textarea` shadcn nuevo (observaciones). Paradas NO gestionadas en el form (`TODO[modelo-linea-paradas]`, deny-all sin CRUD; como B14 omitió campos operativos del conductor). **Bug de listado descubierto y resuelto:** la regla `read` de `/lineas` (`sameTenant(resource.data.tenantId)`) rechaza queries `list` que no constriñen `tenantId` (`resource.data` undefined en `list`); fix = `where('tenantId')` en `listarLineas` + índice `(lineas: tenantId+centroId+codigo)`; **`firestore.rules` NO tocado** (la regla era correcta y bloquea cross-tenant — canónica D6.5). Verify manual **9/9 PASS en navegador** (gate jefe/conductor, listado, filtros, alta mínima/completa, validación HEX+vigencia, unicidad D6.3, edición omit-only + Select estado, alta directa suspendida). 1 canónica nueva D6.5 en §13. `TODO[modelo-linea-paradas]` con dirección resuelta (doble-array denormalizado). Bundle: `LineasPage` chunk lazy propio. **Sesión 7 completa.**
- Backend Tipos de turno operativo (Bloque 18, primer bloque de Sesión 8). **Segunda entidad operativa** del jefe de tráfico (tras Líneas); backend-completo, frontend pendiente. Modelo `TipoTurno` reconciliado (divergencia TS↔PDF resuelta, base canónica TS): `centroId` opcional→**REQUERIDO** (mata el tenant-wide; un tipo pertenece a un centro porque los horarios dependen del convenio/operativa local); **`tipoFranja` ELIMINADO** (eje mezclado: solapaba franja-horaria con `esPartido`) → `esNocturno` boolean ortogonal + `esPartido` boolean; **+`color?` HEX** (cuadrante); auditoría canónica D3.7/D4.1 con **`fechaCreacion` eliminado** (D6.4); `estado` binario `activo`/`obsoleto` (**D5.3**, no enum-3 como Línea); **duración DECLARADA no calculada** (D6.6: `duracionMinutos`/`duracionEfectivaMinutos` en payload, backend valida coherencia pero no recalcula — partidos y cruce de medianoche rompen la aritmética); cruce de medianoche por convención `horaFin < horaInicio` sin flag redundante. Tipo `TipoFranjaTurno` eliminado (0 huérfanos). 2 callables nuevos (`crearTipoTurno`/`actualizarTipoTurno`, clones de crear/actualizarLinea) → **13 callables totales**. Auth super_admin libre + jefe scoped anti-cross. `assertCentroActivo` **generalizado** (mensaje genérico, ahora compartido por crearLinea+crearTipoTurno, D5.1/D5.2). `assertCodigoTipoTurnoUnico` (D6.3). Validators nuevos `assertHoraHHmm` (HH:mm 00:00–23:59) + `assertPositiveNumber` + `assertTramosPartido` (validación cruzada `esPartido↔tramosPartido`, `efectiva<=total`, tramos dentro de rango si no cruza medianoche). Soft-delete D4.3 `activo↔obsoleto` sin cascada (`TODO[tipoturno-obsoleto-conductores]`). Reglas `/tipos_turno` patrón `/lineas` (jefe-scoped por tenant + veto inmutables, deny-all sustituido) + índice `(tipos_turno: tenantId+centroId+codigo)` **aplicando D6.5 (B17) desde el diseño** (el listado de cliente del jefe exigirá `where('tenantId')`). Verify script `verify-tipos-turno.mjs` con **37/37 PASS contra emulator** (22 `crearTipoTurno` + 15 `actualizarTipoTurno`: altas mínima/completa/partido, validaciones HH:mm/duración/tramos/estado/color, D5.1/D5.2, D6.3 unicidad por-centro, auth 3 niveles + anónimo, vetos de inmutables, soft-delete activo↔obsoleto, anti-cross del jefe). 1 canónica nueva D6.6 en §13. 2 TODOs nuevos en §12 (`tramos-partido-dentro-de-rango`, `tipoturno-obsoleto-conductores`). **Desbloquea parcialmente `TODO[conductor-campos-operativos-en-alta]`**: ya existe la colección `tipos_turno` que poblará los pickers del `ConductorForm` (pendiente el frontend del picker). Marca Tipos de turno **backend-completo, frontend pendiente**. **Sesión 8 en curso.**
- Frontend Tipos de turno end-to-end (Bloque 19, Sesión 8). **Tipos de turno COMPLETO end-to-end** — tercera entidad operativa con UI (tras Líneas). Clon del molde Líneas (B17) con la diferencia clave: **estado BINARIO `activo`/`obsoleto` (D5.3, patrón Centro)** — botones "Marcar obsoleto"/"Reactivar" + Dialog destructivo, **NO** Select de estado como Línea (enum-3 D6.2). `services/tiposTurno.ts` (D4.9, única superficie I/O): `crearTipoTurno`/`actualizarTipoTurno` + `listarTiposTurno(tenantId, centroId)` con `where('tenantId')+where('centroId')+orderBy('codigo')` (D6.5, índice B18); `tramosPartido` viaja como `{inicio, fin}` "HH:mm". `TiposTurnoPage` gate D4.13 split **`rolRequerido='jefe_trafico'`** (centro de claims), listado D4.8 con re-fetch, filtros estado+búsqueda. `TiposTurnoTable`: `ESTADO_TIPO_TURNO_LABEL` SSOT, Badges estado, iconos `Moon` (nocturno) + `SplitSquareHorizontal` (partido), indicador "+1 día" cuando `horaFin <= horaInicio` (cruce de medianoche), columna Duración (total + efectiva). `TipoTurnoForm` con **UI nueva que Líneas no tenía**: checkbox `esPartido` que despliega una **lista DINÁMICA de tramos** (botón "+ Añadir tramo" + papelera por fila); horas como `input type="time"`, duraciones como `input type="number"` **declaradas no calculadas (D6.6)**; validación cliente espejo del backend (efectiva≤total, partido exige ≥1 tramo válido y dentro de rango si no cruza medianoche, color HEX onBlur); estado hard-codeado `'activo'` en alta (como `crearCentro`). 3 componentes Dialog (`TipoTurnoFormDialog` alta/edición + `CambiarEstadoTipoTurnoDialog` activo↔obsoleto, clon de `CambiarEstadoCentroDialog`). `buildDelta` omit-only (DI10.13) con manejo de la regla cruzada esPartido↔tramos del backend; reset por `key` (DI10.15). Routing y `NAV_BY_ROL` ya tenían `/tipos-turno` bajo `jefe_trafico` (icono `Clock`) desde antes. Sin nuevas deps shadcn. **Verificado vía E2E Playwright (B20), no manual.** Bundle: `TiposTurnoPage` chunk lazy propio (15.69 kB). **Desbloquea el picker de tipos de turno del `ConductorForm` (`TODO[conductor-campos-operativos-en-alta]`).** Marca Tipos de turno **COMPLETO end-to-end**.
- Infraestructura de testing E2E con Playwright (Bloque 20, Sesión 8). **Primer testing automatizado de navegador del proyecto** — cierra el verify de B19 (frontend implementado, verificado ahora por E2E en vez de checklist manual). `@playwright/test` 1.61 en `apps/web` (junto a la app que testea) + Chromium headless. **Login resuelto vía `storageState` con `indexedDB:true`** (Firebase Auth Web SDK persiste la sesión en IndexedDB, no en cookies/localStorage; soportado desde Playwright 1.51): proyecto `setup` (`auth.setup.ts`) loguea como jefe UNA vez con esperas explícitas (no sleeps) y vuelca el estado; los specs declaran `dependencies:['setup']` + `storageState` y arrancan ya autenticados. **`webServer` propio en puerto fijo 5180 (`--strictPort`)** → independiente del 5175 variable del usuario; `reuseExistingServer` local. **Emuladores = prerequisito asumido corriendo** (decisión documentada: boot lento + el seed necesita paso aparte): `globalSetup` verifica el hub 4400 (falla con mensaje claro si no), **siembra idempotente** (`seed-tipos-turno-b19.mjs`) y hace **warm-up de los callables** (POST a los endpoints `crearTipoTurno`/`actualizarTipoTurno` — evita el cold-start del Functions emulator que colgaba la 1ª mutación). `workers:1` + `fullyParallel:false` (BD del emulador compartida). **13 tests verdes y re-ejecutables** (1 setup + 1 smoke + 11 flujos de Tipos de turno = el checklist manual de B19 automatizado: gate, listado, filtros, alta simple/partido/nocturna, validación cliente, unicidad D6.3, edición omit-only, obsoleto/reactivar). **Idempotencia** vía `reset-tipos-turno-b19.mjs` en `beforeEach` (recrea SOLO los tipos del centro, **sin tocar usuarios** — sus UIDs de Auth deben permanecer estables o se invalidaría el `storageState`). `npm run test:e2e`. Override ESLint para `e2e/**` + `playwright.config.ts` (node globals). El usuario autorizó las deps de Playwright (excepción consciente a §10). 1 canónica nueva D6.7 en §13. **A partir de B20 la verificación de UI es automatizada (Claude Code ejecuta `npm run test:e2e`), ya no manual.** **Sesión 8 completa.**
- Conductores-config (Bloque 21, Sesión 8). **Cierra `TODO[conductor-campos-operativos-en-alta]`** (abierto desde B14). Dos mitades. **(1) Callable `actualizarConductor` nuevo (14º callable, molde update)**: edita SOLO campos que viven EXCLUSIVAMENTE en `/conductores` — `lineasPreferentes`/`lineasSecundarias`, `tiposTurnoPermitidos`/`tiposTurnoExcluidos`, `maxHorasSemanales`, `observaciones`, `puedeSerReserva`, `estado` (`EstadoConductor` enum-4, distinto del estado de `/usuarios`). **Veta** (invalid-argument por campo) la identidad/pertenencia (`id`/`tenantId`/`centroId`/`dni`/`usuarioId`/`numeroEmpleado`/`categoria`/`fechaAntiguedad`/`fechaIncorporacion`/`fechaBaja`/`creadoPor`/`creadoEn`) Y los campos **dual-homed con `/usuarios`** (`email`/`telefono`/`nombre`/`apellidos` → mensaje que redirige a `actualizarUsuario`, evita drift `/conductores`↔`/usuarios`↔Auth; refina D5.4/D5.5). Auth super_admin libre + jefe anti-cross contra el doc (no el payload), simétrico a actualizarLinea/TipoTurno. Lee el doc inline (no hay `assertConductorExists`). Auditoría D4.1: modelo `Conductor` +`actualizadoPor?`/`actualizadoEn?`. `validateActualizarConductorPayload` reusa validators de B13 (`assertOptionalStringArray`/`assertOptionalPositiveNumber`/`assertOptionalNonEmptyString`/`assertOptionalBoolean`/`assertOptionalEnum`). Verify `verify-actualizar-conductor.mjs` **20/20 PASS** (editar cada grupo, auditoría, 7 vetos, `al menos un campo`, inexistente, auth 3 niveles + anti-cross centro/tenant, array con elemento vacío). **(2) Pickers operativos en el ALTA** (flujo super_admin, `UsuariosPage`→`CrearUsuarioDialog`→`ConductorForm`): `MultiSelectCheckbox` genérico (sin deps nuevas, usa el `Checkbox` existente, con `disabledIds` para exclusión mutua + mensaje de vacío); `ConductorCamposOperativos` con 2 secciones colapsables (Líneas pref/sec, Tipos perm/excl, colapsadas por defecto) + `maxHorasSemanales`/`observaciones`, **pobla con `listarLineas`+`listarTiposTurno` del centro** (reutilizables, ya filtran D6.5; super_admin pasa la regla read porque `isSuperAdmin()` cortocircuita `sameTenant`), **exclusión mutua en cliente** (una línea no puede ser preferente Y secundaria; un tipo no permitido Y excluido) vía `disabledIds`, **resetea las selecciones al cambiar de centro** (en `handleCentroChange` del form). `CrearConductorInput` +6 campos operativos (el backend de B13 ya los aceptaba, defaultea `[]`). `Field` del ConductorForm ahora asocia label↔input (`useId`+`cloneElement`, a11y + testabilidad E2E). `CrearUsuarioDialog` con `max-h-[90vh] overflow-y-auto` (fix de scroll para form alto). **E2E 4 tests nuevos (D6.7)**: alta con selecciones persiste los arrays, exclusión mutua deshabilita la opción cruzada, centro sin líneas permite alta con arrays `[]`, cambio de centro resetea; storageState super_admin nuevo (segundo proyecto setup); seeds/reset/read `{seed,reset,read}-conductor-b21.mjs`. **Suite E2E 18/18 verde sin regresión, re-ejecutable.** La **edición vía UI** (la `ConductoresPage` del jefe, hoy placeholder) queda para **B22** — el callable ya está listo y verificado. Marca el **alta de conductor COMPLETA con config operativa**.
- ConductoresPage del jefe (Bloque 22, Sesión 8). **Conductores COMPLETO end-to-end** (alta super_admin en Usuarios + edición de config operativa por el jefe). Convierte el placeholder en página real: el jefe LISTA y EDITA la config operativa de los conductores de SU centro (centro+tenant por claims, **sin `TenantCentroSelect`**) reutilizando el callable `actualizarConductor` (B21). **Opción A (decisión del usuario)**: el jefe SOLO edita; el **alta sigue super_admin-only** (flujo de Usuarios) — división onboarding/gobierno (crea Auth user + `linkPasswordReset`) vs config/operativa (líneas/tipos/estado). **`/conductores` retirado de la nav del super_admin** (`navigation.ts`): la página es jefe-céntrica y el token del super_admin no lleva `centroId`; si entra por URL → `NoAutorizadoView` (gate D4.13). `services/conductores.ts` (D4.9): `listarConductores(tenantId, centroId)` con `where('tenantId')+where('centroId')` (D6.5; el `list` del jefe pasa la regla `read sameTenant` porque la query constriñe `tenantId`), **SIN `orderBy` server-side** (orden en memoria por apellidos → **no requiere índice compuesto nuevo**; el `(conductores: centroId+estado)` de B11 no servía para tenantId+centroId+orden); wrapper `actualizarConductor` con `ActualizarConductorInput` (solo campos editables). `ConductoresPage` molde LineasPage/TiposTurnoPage: gate D4.13 split, listado D4.8 client-side + re-fetch tras editar, filtros estado+búsqueda (nombre/apellidos/nº empleado), gate de claims incompletos. `ConductoresTable`: columnas Conductor (nombre+apellidos+nº empleado), Categoría, Estado (Badge), Config (conteo "N líneas · M turnos"), Reserva, Editar; `ESTADO_CONDUCTOR_LABEL` SSOT (enum-4 `activo`/`baja_temporal`/`vacaciones`/`baja_definitiva`). `ConductorEditDialog`+`ConductorEditForm` (molde LineaFormDialog): **reutiliza `ConductorCamposOperativos` (B21) SIN modificarlo** (ya era controlado vía `value`/`onChange`) precargando el `OperativosValue` desde el conductor (las selecciones actuales aparecen checked); Select de estado enum-4 (patrón Línea, no binario); checkbox `puedeSerReserva`; **`buildDelta` omit-only con comparación de arrays como CONJUNTO (`sameSet`)** → el orden de selección no genera falsos cambios y solo se reenvía un array si el usuario lo toca (de paso **no re-persiste ids muertos** de líneas/tipos borrados que el conductor arrastrara); maxHoras/observaciones sin delete-on-empty (DI10.13). **E2E 6 tests nuevos (D6.7)**: gate (super_admin sin link en sidebar + URL→NoAutorizado), listado con conteos, edición con precarga que persiste (verificada leyendo el doc), omit-only, exclusión mutua en edición, filtros. Idempotencia **self-sufficient** (`reset-conductores-b22.mjs` en `beforeEach` recrea 3 conductores conocidos; convive con el reset de B21 que borra los de tenant-test). **Suite E2E 24/24 verde sin regresión, re-ejecutable.** **Sin tocar backend, reglas Firestore ni índices.** Marca **Conductores COMPLETO end-to-end**.
- Backend Frecuencias (Bloque 23, Sesión 8). **Primer insumo de DEMANDA del servicio** — Frecuencia regular + FrecuenciaExcepcional, backend-completo (frontend en B24). **DECISIÓN CLAVE**: Frecuencia es **dato DESCRIPTIVO** del servicio (intensidad de paso por `lineaId`+`tipoDia`+tramo+`intervaloMinutos`+`sentido`), **NO alimenta el optimizador en el MVP** — la demanda del optimizador saldrá de los **tipos de turno activos por día** (como validó el spike Pyomo: MILP de assignment plaza↔conductor). El puente **frecuencias→expediciones→turnos** (vehicle scheduling + crew scheduling) queda **FUERA del MVP**. **Modelo** (`types.ts`): `Frecuencia` **+`activa`** (soft-delete D4.3) **−`fechaInicio`/`fechaFin`** (la estacionalidad vive en `Línea.vigencia*`) + auditoría canónica D6.4 (`creadoPor?`/`creadoEn?`/`actualizadoPor?`/`actualizadoEn?`); `sentido` se queda `'ida'|'vuelta'|'ambos'` (NO 'circular' del PDF); `FrecuenciaExcepcional` migrada a canónica (**−`fechaCreacion`**, `creadoPor`→opcional, +`creadoEn?`/`actualizado*`) +`activa`, mantiene `fecha`/`motivo?`/`sentido`. **Backend**: 4 callables (`crear`/`actualizar` × regular/excepcional, molde TipoTurno) → **18 callables totales**; `assertLineaActiva` nuevo en `refs.ts` (D5.1/D5.2, valida la **línea padre** — Frecuencia cuelga de Línea, no de Centro; devuelve tenantId/centroId de la línea para coherencia); **`assertNoSolapeFrecuencia`** + `assertNoSolapeFrecuenciaExcepcional` (D6.8, **primera validación de no-solapamiento temporal** del proyecto, intervalo medio-abierto, `'ambos'` colisiona con todo); auth super_admin libre + jefe scoped anti-cross; coherencia tenant/centro vía línea; defaults D4.2 (`activa=true`); en UPDATE se valida con valores efectivos (payload ?? doc) y solo se re-chequea solape si queda activa. `assertHoraInicioAntesFin` (las frecuencias NO cruzan medianoche). **Reglas** `/frecuencias` + `/frecuencias_excepcionales` abiertas (deny-all sustituido) con patrón `/lineas` (jefe-scoped por tenant + veto inmutables `id/tenantId/centroId/lineaId/creadoPor/creadoEn`); 2 **índices compuestos** `(frecuencias: tenantId+centroId+lineaId)` y `(frecuencias_excepcionales: tenantId+centroId+fecha)`. Verify `verify-frecuencias.mjs` **29/29 PASS** + sonda de cliente que confirma la regla `/frecuencias` cargada y D6.5 (list sin tenantId → permission-denied). 1 canónica nueva D6.8 en §13. 1 TODO nuevo `frecuencia-cruce-medianoche` en §12. **Frecuencias backend-completo; frontend pendiente (B24).**
- Backend Convenio (Bloque 25, Sesión 8). **El Convenio parametriza las restricciones del optimizador** (R1-R6 del spike Pyomo). El modelo YA EXISTÍA (por tenant); **MIGRADO a singleton POR CENTRO** (`id=centroId`, **+`centroId`**, exactamente uno por centro, **sin `estado`** — singleton, no aplica D5.3). Los horarios/operativa dependen del convenio local (decisión B18 sobre `centroId` requerido en TipoTurno). **+`computoHoras?` (`'jornada'|'conduccion'`)** resuelve el TODO del spike sobre si R3 cuenta jornada total o conducción efectiva. **Auditoría migrada a canónica D6.4** (`creadoPor?`/`creadoEn?`/`actualizadoPor?`/`actualizadoEn?`; **fuera** el esquema viejo `fechaUltimaActualizacion: Timestamp` + `actualizadoPor: string` requerido). **9 campos de restricciones mantenidos** (descanso entre jornadas, max horas sem/anuales, domingos libres año, fines de semana/días consecutivos trabajados, descanso semanal, antelación publicación, festivos extras) — el optimizador MVP usará los validados por el spike (descanso entre jornadas→R2, max horas→R3); el resto quedan **preparados pero no aplicados** (`TODO[convenio-restricciones-no-aplicadas-mvp]`). Mirror `docs/03-modelo-de-datos.ts` actualizado. **Backend**: 1 callable `guardarConvenio` **UPSERT** (no hay crear/actualizar separados porque `id=centroId` es determinista; set-si-no-existe con D3.7 / update-preservando-`creado*` con D4.1) → **19 callables**. `assertCentroActivo` **reutilizado** (D5.1, sin helper de unicidad: el id determinista la garantiza). Auth super_admin libre + jefe scoped a su centro (anti cross-tenant/centro). Reglas `/convenio` patrón `/lineas` (jefe-scoped por tenant + veto inmutables `id/tenantId/centroId/creadoPor/creadoEn`), lectura **get-by-id** (escapa el bug D6.5 de `list`: en `get` sí hay `resource.data`); **sin índice** (get por id, no query). Veto en el validador de campos server-managed (`id`/`creado*`/`actualizado*`); tenantId inmutable en UPDATE. Verify `verify-convenio.mjs` **24/24 PASS** (upsert create/update con `creadoEn` preservado e `id===centroId`, opcionales, validación de rangos, D5.1 inexistente/inactivo, auth 3 niveles + anti-cross, veto de inmutables). 1 canónica nueva **D6.9 (singleton por padre)** en §13. **Relación override**: `convenio.maxHorasSemanales` es el límite del centro; `conductor.maxHorasSemanales` lo sobreescribe individualmente (el optimizador resolverá `conductor ?? convenio`). Marca **Convenio backend-completo; frontend pendiente.**
- Modelo + backend Cuadrante + Asignaciones (Bloque 26, Sesión 8). **El corazón del sistema: la "mesa" donde el optimizador escribirá** (B27 es el optimizador; B26 solo construye dónde escribe). **ALCANCE:** solo `cuadrantes` + `asignaciones`; `versiones_cuadrante` y `cambios_asignaciones` **DIFERIDAS** al bloque de Intercambios (siguen deny-all). **MODELO** (`types.ts` + mirror `docs/03`): **Cuadrante** = cabecera (centro+mes, id determinista `cua_{centroId}_{año}_{mes}` singleton por centro+mes patrón D6.9-like, estados `borrador`/`publicado`/`cerrado`, KPIs en `estadisticas`); **mantiene los campos de dominio** `generadoPor`/`fechaGeneracion`/`publicadoPor`/`fechaPublicacion` (`generadoPor`≠`creadoPor`: el optimizador re-genera sin tocar `creado*`) **+ auditoría canónica D6.4 añadida** (`creadoPor?`/`creadoEn?`/`actualizadoPor?`/`actualizadoEn?`; `ultimaModificacion`→`actualizadoEn`). **Asignacion** = estado vivo (1 fila = conductor×día×turno; `tenantId`/`centroId` derivados del cuadrante padre, no del payload → sin drift); **migrada a D6.4 sin `fechaCreacion`** (`ultimaModificacion`→`actualizadoEn`); **el descanso ordinario NO se materializa** (ausencia de fila = día libre, **D6.10**); materializables = `turno`/`reserva_presencial`/`reserva_localizable`/`vacaciones`/`baja` (`'libre'` rechazado por el validator). **BACKEND**: 7 callables nuevos → **26 callables**. Ciclo de vida: `crearCuadrante` (borrador, `modoGeneracion='manual'` en alta manual; duplicado → `already-exists`; `assertCentroActivo` D5.1; auditoría D3.7) / `publicarCuadrante` (borrador→publicado, sella `fechaPublicacion`+`publicadoPor`) / `cerrarCuadrante` (publicado→cerrado); transiciones inválidas → `failed-precondition`. Asignaciones: `crearAsignacion`/`actualizarAsignacion`/`eliminarAsignacion` (hard-delete coherente con D6.10) + **`crearAsignacionesLote`** (`writeBatch` en chunks de 500, cota `LOTE_MAX=2000`, **para que el optimizador B27 vuelque el resultado de golpe**). **`assertCuadranteEditable`** (nuevo en `refs.ts`): valida existe (invalid-argument) + estado `borrador` (failed-precondition) y **devuelve `{tenantId, centroId, año, mes}` en una sola lectura** — las asignaciones SOLO se editan si el cuadrante está en `borrador` (publicado/cerrado → los cambios pasan por intercambios, bloque futuro); la `fecha` de cada asignación se valida DENTRO del mes del cuadrante (`assertFechaEnMes`). Auth super_admin libre + jefe scoped anti-cross (contra el cuadrante para asignaciones). Reglas `/cuadrantes`+`/asignaciones` patrón `/lineas` (jefe-scoped por tenant + veto inmutables; `/cuadrantes` veta también `año`/`mes`; `delete` super_admin a nivel regla, el `eliminarAsignacion` del jefe va por Admin SDK). **3 índices** nuevos: `(cuadrantes: tenantId+centroId)`, `(asignaciones: tenantId+cuadranteId+fecha)` = parrilla conductor×día del cuadrante, `(asignaciones: tenantId+conductorId+fecha)` = "mi horario" del conductor. Verify `verify-cuadrante.mjs` **34/34 PASS** (ciclo de vida + transiciones inválidas, unicidad duplicado, asignaciones CRUD solo en borrador + rechazo si publicado/cerrado + veto inmutables + lote, auth 3 niveles + anti-cross, D5.1, fecha-en-mes, `'libre'` rechazado). 1 canónica nueva **D6.10** en §13. `TODO[asignacion-validar-referencias]`. **`actualizarCuadrante`** (escribir `estadisticas`/`generadoPor`/`modoGeneracion` al regenerar) **DIFERIDO a B27** (lo necesita el optimizador). Marca **el modelo del cuadrante LISTO; el optimizador —quien escribe— es B27.**
- Demanda + Calendario (Bloque 27, Sesión 8). **Resuelve los 2 gaps que bloqueaban el optimizador** (la demanda no existía como dato; no había calendario fecha→tipoDia). **NO incluye el optimizador ni Cloud Run** (bloque posterior). **DEMANDA**: `tiposDiaAplicables: TipoDia[]` en `TipoTurno` (types.ts + mirror), **REQUERIDO en crear** (array no vacío sin duplicados, helper `assertTiposDiaArray`), opcional en actualizar; expuesto en validators, ambos callables, servicio web y `TipoTurnoForm` (grupo de 4 checkboxes con `TIPO_DIA_LABEL` SSOT, comparación como conjunto `sameSet` en `buildDelta`, submit bloqueado si vacío). **Semántica**: la demanda del optimizador para un día = los tipos de turno activos del centro cuyo `tiposDiaAplicables` incluye el `tipoDia` de ese día, **1 plaza c/u** (fiel a TUCARSA: cada código de turno aparece máx. 1 vez/día). **CALENDARIO**: `resolverTipoDia(fecha, festivosAplicables): TipoDia` función PURA en `@albius/shared/calendar` (el festivo que coincide gana sobre el día de semana; comparación en UTC; el caller pre-filtra los festivos del centro) + **10 tests vitest** de casos límite. **Festivos HABILITADO** (era deny-all): `Festivo` migrado a auditoría canónica D6.4 (fuera `fechaCreacion`); validators crear/actualizar/eliminar; **3 callables** (`crearFestivo`/`actualizarFestivo`/`eliminarFestivo`) → **29 callables**; reglas `/festivos` patrón `/lineas` + índice `(festivos: tenantId+fecha)`. **Auth por scope**: festivo tenant-wide (`centroId` ausente) = **super_admin only**; de centro = **jefe scoped** + `assertCentroActivo`; `esEditable=false` protege oficiales (bloquea update/delete con `failed-precondition`, helper `assertFestivoEditable`); **hard-delete**; `centroId` inmutable en update (cambiar scope = recrear). Gate doc-based compartido `assertPuedeTocarFestivoDoc`. **vitest ADOPTADO** en `packages/shared` (primer testing unitario del proyecto, **D6.11**; dep aprobada §10). **Fix colateral**: `TipoTurnoFormDialog` no scrolleaba y el grupo de checkboxes nuevo dejaba el submit fuera del viewport en turnos partidos → `max-h-[90vh] overflow-y-auto` (patrón B21). Seed/reset de tipos de turno B19 actualizados con `tiposDiaAplicables` (campo requerido nuevo). Verify `verify-festivos.mjs` **22/22 PASS** (CRUD, auth por scope, `esEditable` bloquea oficiales, D5.1 centro inexistente/inactivo, hard-delete) + **E2E 24/24** (incluye "alta simple: el campo persiste en el form"). 1 canónica nueva **D6.11** en §13. `TODO[festivos-frontend]`. **El frontend de festivos queda pendiente** (al inicio se siembran por seed). Marca **demanda + calendario RESUELTOS; falta el motor del optimizador (Python/OR-Tools/Cloud Run) + `actualizarCuadrante`.**
- actualizarCuadrante + maquinaria de escritura reutilizable (Bloque 28, Sesión 8). **Prepara la Opción C** (el optimizador B29 computa y devuelve un PLAN; el lado Node ESCRIBE — lógica de escritura en un solo sitio, TS, sin duplicar en Python). **NO construye el orquestador ni el cliente HTTP a Cloud Run** (eso es B29). **Patrón nuevo (D6.12)**: la lógica de escritura se extrae a `apps/functions/src/logic/` (función PURA sin `request.auth` + callable envoltorio que hace solo auth + anti-cross y delega con `actorId=invocadorUid`), reutilizable callable↔orquestador. **Piezas**: (1) `escribirAsignacionesLote` (extraída de `crearAsignacionesLote`, que se **refactorizó sin cambiar comportamiento** — `verify-cuadrante.mjs` B26 **34/34 sin regresión**); (2) `limpiarAsignacionesCuadrante` (**gap B6 resuelto**: borrado masivo por `cuadranteId` en batches de 500, exige borrador) + callable `eliminarAsignacionesCuadrante`; (3) `regenerarAsignaciones` (limpiar→volcar, orden definido, **NO atómica** por el límite de Firestore >500 docs/múltiples batches, documentado; idempotente para reintento) + callable `regenerarAsignacionesCuadrante`; (4) `actualizarCuadranteDoc` (superficie editable = `estadisticas` + bloque de regeneración `generadoPor`/`fechaGeneracion`/`modoGeneracion`; `fechaGeneracion` server-stamped; **veto** de identidad/`estado`/`publicado*`/`fechaPublicacion`/`versionActual`/`creado*`; exige borrador; auditoría D4.1) + `validateActualizarCuadrantePayload` + callable `actualizarCuadrante`. **3 callables nuevos → 32 callables**. Convención **`actorId='optimizador'`** para escritura por servicio (paralelo a `bootstrap-cli`/`system-seed`). **Sin reglas/índices nuevos** (query `where(cuadranteId==X)` single-field auto-indexada; los callables escriben vía Admin SDK que bypassa reglas). Verify `verify-cuadrante-b28.mjs` **18/18 PASS** (refactor no-regresión, limpiar borrador, regenerar 3→2 = quedan 2 no 5, actualizarCuadrante estadisticas + bloque regeneración + veto inmutables + exige borrador + auth 3 niveles + anti-cross, actorId en creadoPor). **Dirección C1** (la Function lee Firestore y pasa el payload al Python; el Python no toca Firestore) — a confirmar en B29. 1 canónica nueva **D6.12** en §13. **Cierra `TODO[actualizar-cuadrante-estadisticas]`**; `TODO[actualizar-cuadrante-estadisticas-publicado]` anotado. Marca **la escritura del lado Node LISTA para el optimizador**.
- El MOTOR del optimizador en local (Bloque 29 Fase A, Sesión 8). **Servicio Python** (`services/optimizer/`, FastAPI, top-level fuera de npm workspaces) **portado del spike validado (Pyomo + HiGHS)** a datos reales de Albius. Recibe JSON (demanda = `TipoTurno` activos por `tipoDia`; `convenio` = restricciones; `conductores` = habilitaciones) y devuelve un PLAN (`asignaciones` + KPIs en `estadisticas`) que **encaja 1:1 con `escribirAsignacionesLote`/`actualizarCuadranteDoc` de B28** (`AsignacionItemPayload`). **Formulación MILP** (paridad con el spike): assignment plaza↔conductor; R1 (1 turno/día), R2 (descanso vía tiempos ABSOLUTOS → cruce de medianoche lineal, `conflict_pairs` clique), R3 (max horas/semana natural), R4 (habilitación estructural), **racha** (≤N días consecutivos = `convenio.maxDiasConsecutivosTrabajados`, sustituye al R_REST hardcodeado del spike), R6 (cobertura blanda con déficit penalizado). **Objetivo MVP**: `W_COV·déficit + W_EQ·span + W_NOCT·z` — **cobertura + equidad + bienestar nocturno; `W_PREF` DESACTIVADO** (preferencias diferidas: `tiposTurnoPermitidos/Excluidos` son duras, no hay preferencia blanda usable hoy). **Horizonte mensual vía DESCOMPOSICIÓN SEMANAL** (4-5 sub-problemas por semana natural con **cosido de fronteras**: R2 domingo→lunes con el último día de la semana previa FIJO, + racha arrastrada entre semanas) — el monolito mensual NO escalaba. **CAUSA RAÍZ de rendimiento corregida: `wload` (carga por conductor) como VARIABLE** (`m.wload`=Var + constraint `wload==Σdur·x`), no inline — inlinearla metía todas las `x` en cada constraint de equidad (`mean_load`) → matriz **14× más densa** (1.27M nonzeros a 178×7) → el probing del presolve de HiGHS se atascaba. **VALIDADO**: 178×30 (escala TUCARSA real) al **100% cobertura en ~5min con HiGHS por DEFECTO**, `validate_solution` PASS sobre el plan MENSUAL completo (incluido el cosido R2+racha de fronteras). **El modelo de demanda de B27 (`tiposDiaAplicables`) CONFIRMADO suficiente para la Forma 1** (cada `TipoTurno`/`codigo` = plaza nominal única; demanda estable por `tipoDia`). El motor NO tiene verify `.mjs` (es Python, fuera del flujo Node): su validación es `validate_solution` interna + las mediciones de escala documentadas (tests `pytest` futuros). Scratch de medición en `services/optimizer/scratch/` (ignorado). **Fases B y C cerradas en los bullets siguientes.**
- El optimizador EN CLOUD RUN + ORQUESTADOR + FRONTEND (Bloque 29 Fases B y C, Sesión 8). **B29 CERRADO Y FUNCIONANDO EN PRODUCCIÓN.** **Fase B**: motor dockerizado y desplegado a **Cloud Run privado** (`--no-allow-unauthenticated`) en **europe-west1**; URL inyectada en functions vía `OPTIMIZER_URL` (`.env`, `defineString`). **Fase C** (orquestador async + frontend + seed + deploy): **(C.2)** modelo `EstadoGeneracion` + campos `estadoGeneracion`/`errorGeneracion` en `Cuadrante` (eje ortogonal al ciclo de vida) + `marcarEstadoGeneracion` (`logic/cuadrante.ts`) + regla lectura-self de `notificaciones` + espejo runtime `resolverTipoDia` (`apps/functions/src/calendar.ts`); **(C.3) patrón async Cloud Tasks** — `generarCuadrante` (onCall europe-west1, corto: valida borrador + marca `'generando'` + encola Cloud Task + devuelve `{ok,cuadranteId}` <1s) + `generarCuadranteWorker` (`onTaskDispatched`, 1800s: lee Firestore → compone `OptimizarRequest` → llama al motor autenticado (`google-auth-library`, ID token audience=URL) → escribe el plan vía `regenerarAsignaciones`+`actualizarCuadranteDoc` (B28, `actorId='optimizador'`) → marca `'completado'` + `Notificacion 'cuadrante_generado'`; catch → `'error'` + `Notificacion 'otro'`); dep `google-auth-library`; `maxAttempts:1` (errores de app deterministas; `TODO[generacion-stuck-reset]` para infra colgada); **(C.4)** frontend `CuadrantePage` real + `services/cuadrantes.ts` + `functionsEu` (2ª instancia europe-west1) + feedback reactivo `onSnapshot`; seed `scripts/seed-caso-prueba.mjs` (Admin SDK directo, patrón bootstrap) + deploy a producción. **Validado end-to-end en PRODUCCIÓN**: caso líneas 1-5 TUCARSA (60 conductores, sept 2026) → botón → Cloud Tasks → worker → MILP en Cloud Run → **916 asignaciones, 100% cobertura** escritas y mostradas en la web real. **Primera funcionalidad con datos reales en producción del proyecto.** HEAD 51a993b. 5 canónicas nuevas D6.13-D6.17 en §13.
- **Hitos de "primera vez en producción" (B29 C.4, aprendizajes de despliegue reutilizables):** (1) habilitar **Email/Password** en Firebase Auth (deshabilitado en proyecto virgen → el login fallaba); (2) desplegar **`firestore:rules` + `firestore:indexes`** (nunca subidos — proyecto virgen en *locked mode* → el cliente daba `permission-denied`; el seed por Admin SDK bypassa reglas e índices, de ahí que los datos estuvieran pero la web no los leyera — el índice `(asignaciones: tenantId+cuadranteId+fecha)` tardó minutos en construirse); (3) **fix ADC Windows** del seed (`%APPDATA%\gcloud`, no `~/.config/gcloud`). Checklist de deploy a producción: **functions + firestore (rules+indexes)**; Storage/Hosting **N/A** (la web está en Vercel, no en Firebase Hosting).

- Vista Calendario + lineaId (Bloque 30, Sesión 9). **(B30.1)** `lineaId?` OPCIONAL en `TipoTurno` (modelo + validators + callables + Select en `TipoTurnoForm`), validado con **`assertLineaDelCentro` LAXA** (existe y es del centro; NO exige `estado='activa'` — en TipoTurno la línea es etiqueta de agrupación, no dependencia operativa; D6.18). Cierra `TODO[tipoturno-lineaid]`. **(B30.2)** el seed crea las 5 líneas como entidades (con colores categóricos, doc-id `lin_{codigo}`) y enlaza los 38 turnos por `lineaId` (+ assert de coherencia turnos↔líneas). **(B30.3.1)** `CalendarioPage` (nav del jefe, `/calendario`): rejilla conductor×día del mes coloreada por línea (asignación→tipoTurno→lineaId→linea.color), columna y cabecera congeladas, findes atenuados, leyenda de líneas, estados explícitos (cargando / error con reintentar / sin cuadrante / sin generar); **fechas en UTC** (`lib/calendario.ts`, D6.22). Consume un cuadrante ya generado (one-shot, sin onSnapshot). **PENDIENTE de B30:** filas conductor×línea con nombre agrupado (3.2), filtros (3.3), toggle semanal/mensual (3.4).
- Saneamiento (Bloque 31, Sesión 9). **Node 22** en Cloud Functions (engines + `"runtime": "nodejs22"` explícito en firebase.json + .nvmrc; Node 20 se decomisiona el **30-oct-2026**), sin deps que subir ni breaking changes en el código; **desplegado y verificado**: 38 functions en nodejs22. **`apps/web/vercel.json`** con rewrite SPA catch-all (F5 en ruta profunda daba 404). **`scripts/inspect-produccion.mjs`**: inspector READ-ONLY de producción (Admin SDK, guardas bootstrap sin confirmación). **Incidencia detectada:** las functions llevaban sin desplegar desde el 25-jun, así que **B30.1 (lineaId en callables) no estuvo en producción hasta B31** — el `lib/` compilado y el deploy no eran parte del cierre de B30. Aprendizaje: un cambio en `apps/functions/src` no está en producción hasta que hay deploy explícito; comprobar con `gcloud functions list --v2 --format="table(name,updateTime,serviceConfig.runtime)"`. Falsa alarma resuelta por diagnóstico: un "User code failed to load… Timeout after 10000" en el análisis del deploy NO era el mismatch Node 20 local vs 22 declarado (el CLI solo avisa y usa el host) ni carga lenta (el módulo carga en 300 ms, discovery en 750 ms; `--dry-run` verde); fue transitorio. `FUNCTIONS_DISCOVERY_TIMEOUT=60` como red de seguridad.
- Ausencias por rango (Bloque 32, Sesión 9). **Desbloquea el cuadrante real**: el optimizador ya no asigna turnos a conductores de vacaciones/baja/permiso (en TUCARSA real el 17% está ausente la semana entera). **(B32.1 backend)** entidad `Ausencia` (colección `ausencias`, **fuente única, NO materializada como asignaciones** — D6.19): `conductorId`, `categoria` cerrada `'vacaciones'|'baja'|'permiso'` + `codigo?` libre (sigla de la empresa: V/B/AP/PS) — D6.20, `fechaInicio`/`fechaFin` en **rango CERRADO** (iguales = día suelto), auditoría D6.4. 3 callables clonados de Festivo (**37 callables**); `assertConductorDelCentro` (laxa, no exige activo) + `assertNoSolapeAusencia` (solape cerrado, `excludeId`); el update parcial de una sola fecha se cruza contra el valor **persistido**. Reglas patrón líneas + `sameCentro`, índice `(ausencias: tenantId+centroId+fechaInicio)`. **RET (retén) NO es ausencia** (turno de guardia). El `estado` del Conductor NO se toca (pool = `estado=='activo'`; `'vacaciones'`/`'baja_temporal'` deprecados de facto → `TODO[estado-conductor-deprecar-ausencias]`). Verify `verify-ausencias.mjs` **33/33**; festivos 22/22 sin regresión. **(B32.3 cruce)** `buildRequest` puebla `ausencias[]` (rangos expandidos a días ISO recortados al mes con `expandirAusenciaEnMes`, shared + espejo en functions, 10 tests vitest, todo UTC) y **excluye del pool a los ausentes del MES COMPLETO** (D6.21; `TODO[equidad-normalizar-dias-disponibles]`); dos errores legibles nuevos (pool vacío; tipos de turno sin habilitados). Seed +20 ausencias del perfil real (10 totales, 4 cruzando límites de mes, 8 permisos de 1 día). Dry-run contra emulador: 60 activos → 50 en pool, 10 excluidos exactos, 0 días fuera de mes. **(B32.2 UI)** `AusenciasPage` (nav del jefe) con listado por nombre de conductor, filtros conductor/categoría/mes (por solape), `?conductorId=` desde el enlace "Ausencias" de cada fila de Conductores, form con atajo **"Un solo día"**, dialogs de alta/edición y borrado destructivo; E2E existente 24/24 sin regresión + smoke Playwright del flujo completo (sin spec propio → `TODO[ausencias-e2e-spec]`). **VERIFICADO EN PRODUCCIÓN:** cuadrante sept-2026 regenerado con **50 conductores en pool (10 ausentes totales excluidos), 100% cobertura, 916 asignaciones**.
- Publicar/cerrar/reabrir + edición manual (Bloque 33, Sesión 9). **Convierte el cuadrante de "resultado del optimizador" en HERRAMIENTA DE TRABAJO del jefe**; ningún jefe de tráfico confía ciegamente en el optimizador y sin retocar ni publicar el piloto se rompe a la primera excepción. **(B33.1 ciclo de vida)** `publicarCuadrante`/`cerrarCuadrante` (B26) cableados a la UI (`CuadrantePage` → `CambiarEstadoCuadranteDialog`, un dialog parametrizado por acción; Publicar solo con `borrador && completado`, Reabrir/Cerrar solo en `publicado`; el `onSnapshot` refleja el cambio sin recarga). **Callable nuevo `reabrirCuadrante`** (publicado→borrador, **38 callables**): publicar era irreversible y sin despublicar un error en una demo dejaba al jefe atascado; solo desde `publicado` (`cerrado` es definitivo), `assertCentroActivo` D5.1, **borra `fechaPublicacion`/`publicadoPor`** (describen la publicación vigente, no un histórico — el histórico es `versiones_cuadrante`, diferido a Intercambios). **Guard nuevo en publicar: rechaza si `estadoGeneracion==='generando'`** — antes se podía publicar durante la generación y el worker chocaba 5 min después con `assertCuadranteEditable`, marcaba `'error'` y dejaba el cuadrante publicado y vacío. Verify `verify-cuadrante.mjs` **45/45**. **(B33.2 edición manual)** el jefe asigna/cambia/quita turnos por celda conductor×día desde la rejilla del Calendario (`EditarCeldaDialog`). **Validación HÍBRIDA (D6.23)**: el backend RECHAZA lo estructural — `assertConductorLibreEnFecha` (R1, índice `(tenantId+conductorId+fecha)` de B26 + `excludeId`), `assertConductorDelCentro` + `assertTipoTurnoDelCentro` laxas (D6.26; cierra `TODO[asignacion-validar-referencias]`; `crearAsignacionesLote` sigue laxo, D6.24) — y la UI AVISA sin bloquear lo de convenio (habilitación con la semántica del motor, ausencia del día, descanso <12 h contra el día anterior/siguiente con cruce de medianoche; `lib/calendario.ts`, `services/convenio.ts` nuevo). La rejilla **muestra a TODOS los conductores del centro** (antes solo los que tenían asignaciones: fila vacía para poder dar turno a alguien libre), **renderiza en cuanto existe el cuadrante** (antes exigía `estadoGeneracion==='completado'`; un cuadrante nunca generado se rellena a mano) y **las celdas son clicables solo en borrador** (publicado/cerrado → solo lectura con motivo visible). Solo materializa `'turno'` (vacaciones/baja viven en Ausencias, D6.19); sin "mover de A a B" atómico (`TODO[mover-asignacion-atomico]`); KPIs desactualizados hasta regenerar (`TODO[cuadrante-kpis-tras-edicion-manual]`). Verify **60/60** (+15 casos E1-E15 con seed de conductores y tipos, los previos usaban ids ficticios), **E2E 31/31** con spec propio `calendario.spec.ts` (7 tests) + `reset-cuadrante-b33.mjs` + warm-up de los callables de asignación en `globalSetup` (el primer run falló por cold-start de `crearAsignacion`). **VERIFICADO EN PRODUCCIÓN** (publicar/reabrir/cerrar + edición de celdas con validación estructural). 4 canónicas nuevas D6.23-D6.26.
- Mi horario del conductor (Bloque 34, Sesión 9). **El segundo argumento de venta existe: el conductor entra con su cuenta y ve su horario en el móvil.** Tres sub-bloques. **(B34.1 vínculo + reglas + seed)** `conductorId` pasa a los **custom claims** del conductor (`crearConductor` + seeds; `Claims` lo tipa) porque las reglas Firestore no pueden hacer `get()` a `/usuarios` por cada doc de un `list`; `AuthContext` lo expone en `AuthUser` leyéndolo del **doc** `/usuarios` (funciona aunque el token sea anterior al claim; `TODO[claims-conductorid-migracion]`). **REGLAS ENDURECIDAS ANTES de crear la primera cuenta**: hasta aquí TODAS las colecciones eran `sameTenant` — un conductor con cuenta habría podido leer las **fichas de sus compañeros (DNI, teléfono, observaciones)**, las asignaciones de todos y los cuadrantes en borrador; inocuo mientras ningún conductor tenía cuenta, problema de datos personales en cuanto la tuviera. Helper `esSuConductor(conductorId)` por claim; `/asignaciones` y `/ausencias` solo las suyas, `/conductores` solo su ficha (`usuarioId == uid`), `/cuadrantes` solo publicado/cerrado; jefe y super_admin sin cambio; catálogo (`/tipos_turno`, `/lineas`) sigue `sameTenant`. **Sonda `verify-reglas-conductor.mjs` con el SDK DE CLIENTE** (los verify de callables usan Admin SDK, que bypassa reglas) **28/28**: lee lo suyo y nada de otro, un `list` sin constreñir → denegado (D6.5), conductor sin claim → denegado, jefe/super_admin sin regresión. Seed de producción con **3 cuentas de conductor** de perfiles distintos (05 turnos variados, 11 con una semana de vacaciones parcial nueva, 53 con dos permisos sueltos), **contraseñas generadas por ejecución** (o `SEED_CONDUCTOR_PASSWORD`), impresas en el resumen, **no en el repo**. Verifies 60/60 + 33/33 + 25/25, E2E 31/31. **Desplegado** (rules + functions + seed). **(B34.3 drawer móvil)** el Sidebar es `hidden md:flex` y el botón hamburguesa no hacía nada: en móvil no había navegación. `MobileNavDrawer` sobre los **primitivos de Radix Dialog que ya trae `radix-ui`** (es lo que envuelve el Sheet de shadcn, sin instalarlo — §10): foco atrapado, Escape, clic fuera, `role=dialog` de serie; `SidebarNav` extraído y compartido (una sola lista `NAV_BY_ROL`). **Hallazgo: `tw-animate-css` NO está instalado**, así que las clases `animate-in` de todos los dialogs nunca animaron nada (`TODO[tw-animate-css-ausente]`); keyframe propio para el drawer. Repaso barato a 375 px: 6 páginas de listado desbordaban el body por filas de filtros `flex` sin `flex-wrap` → arreglado (`TODO[movil-revision-paginas-jefe]` para el resto). `mobile.smoke.spec.ts`; suite 33/33. **(B34.2 pantalla)** `MiHorarioPage` real, **móvil primero**: **próximos turnos** (hoy / mañana / resto de la semana natural, cards con código, horario y color de línea; "Libre" y la ausencia se dicen explícitamente) + **resumen** (turnos, horas, días libres) + **el mes en LISTA VERTICAL**, una fila por día (no rejilla: con un solo conductor no aporta y en móvil es inusable). Ve **solo cuadrantes publicados y cerrados** (D6.28), **ausencias cruzadas** con `expandirAusenciaEnMes` (D6.29), mes sin publicar → mensaje honesto. `services/miHorario.ts` con queries que **replican las reglas** (D6.27). E2E: proyecto setup del conductor + `reset-mi-horario-b34` relativo a HOY (UTC) + `mi-horario.spec.ts` (5 tests: sus turnos y no los de otro, ausencia, mes sin publicar en dos variantes, 375 px + drawer, gate) → **suite 39 tests**. Hallazgo E2E: los resets de B21/B22 borran los conductores del tenant **y su Auth user enlazado**, y una ficha de prueba con `usuarioId` tumbó el login del conductor en la suite completa (`TODO[e2e-resets-borran-auth-enlazado]`). 3 canónicas nuevas D6.27-D6.29.
- Convenio y festivos: UI + motor (Bloque 35, Sesión 9). **El bloque más peligroso para una demo: un jefe con veinte años detecta en un minuto un cuadrante que incumple convenio.** **(B35.1 UI)** `ConvenioPage` (`/convenio`, nav del jefe): formulario plano en Cards sobre el singleton por centro (no CRUD); estado "sin convenio" con aviso de que el optimizador no puede generar (`buildRequest` falla sin defaults) y el mismo form vacío; validación cliente espejo de los rangos del validator. **Separa VISUALMENTE los campos "aplicado por el optimizador" de los "registrado, aún no aplicado"** (D6.32): prometer que se aplican los 10 campos destruye la confianza en cuanto el jefe lo descubra. `FestivosPage` (`/festivos`, molde Ausencias): festivos APLICABLES al centro (los suyos + tenant-wide, misma regla que el motor), filtro por mes, alta/edición/borrado de los del centro; **tenant-wide en solo lectura** ("Todos los centros", los gestiona super_admin) y **oficiales sin acciones** (`esEditable=false`); el jefe puede marcar ámbito nacional en uno de su centro (raro pero inocuo). Cierra `TODO[festivos-frontend]`. Aviso "generado con un convenio anterior" en borradores generados comparando `fechaGeneracion` con `actualizadoEn`/`creadoEn` del convenio (sin campos nuevos). Nav del jefe en dos secciones (Operativa / Configuración, 11 items). **Hallazgo:** leer un convenio INEXISTENTE devuelve `permission-denied`, no "no existe" (la regla evalúa `resource.data` y sin doc no hay `resource`); `obtenerConvenio` lo traduce a `null` → `TODO[rules-convenio-get-inexistente]`. E2E 49/49 (+10) + smoke móvil ampliado. **(B35.2 motor)** `maxFinesSemanaConsecutivosTrabajados` en el MILP como regla **BLANDA** (D6.30/D6.31): `w[c,semana]` binaria "trabaja el finde", exceso continuo penalizado con `W_FINDE=500` — muy por debajo de `W_COV=10000` (cubrir siempre gana) y por encima de `W_NOCT=8` y `W_EQ=2`; **cosido entre semanas con el patrón de la racha** (`findes_prev`: si entra con k ≥ N, trabajar el finde es exceso penalizado, no prohibido; el contador arranca en 0 cada mes, `TODO[findes-consecutivos-cruce-meses]`). **Las blandas REPORTAN**: `validar_solucion` cuenta los excesos sobre el mes y `EstadisticasOutput` expone `findesConsecutivosExcedidos` + `conductoresConFindesExcedidos` → KPI en Cuadrante. `antelacionMinimaPublicacionDias` como **aviso** en el dialog de publicar con "Publicar de todos modos" (D6.33; calculado en la UI, el callable no cambia). NO implementados a propósito: descanso semanal 36 h (`TODO[descanso-semanal-36h-explicito]`: R2 + racha ya lo dan de facto), domingos libres y horas anuales (cruzan meses, B35.3 si el piloto lo pide), campo de noches consecutivas (`TODO[noches-consecutivas-campo-convenio]`). **MEDICIÓN** (request REAL volcado del emulador: 50 en pool, 916 plazas, 38 turnos; y sintético 178 del spike; 60 s/semana): **en el caso real la restricción es GRATIS y mejora el resultado** — elimina las rachas de 3-4 findes (3 conductores) y el solver prueba óptimo en **5/5 semanas en vez de 3/5, en 92 s frente a 189 s**, cobertura 100 % en ambos. En 178 baja de 97,9 % a 96,4 % con 60 s/semana pero **vuelve al 100 % con 120 s**: la caída es presupuesto de tiempo, no peso (`TODO[week-time-limit-por-tamano-pool]`). Verifies 60/60 + 18/18 + 24/24, E2E 50/50. **Desplegado y verificado en producción** (Cloud Run + functions + web). 4 canónicas nuevas D6.30-D6.33.
- **Procedimiento (Sesión 9):** los cierres de bloque hacen **commit Y push juntos** (ver §14.12). Deploy de functions/rules/indexes sigue siendo explícito del usuario.

**No hecho (huecos para el PILOTO, en orden):**
1. **Exportación PDF/Excel** del cuadrante (el jefe la necesita para colgarla en el tablón y para la empresa).
2. **Carga masiva de datos + email** (import CSV de conductores/turnos o plantillas; `TODO[catalogo-turnos-escala-ux]`; el alta de usuarios con `linkPasswordReset` **ya es posible**: dominio `albius.es` comprado, falta proveedor transaccional → `TODO[email-transport]`).
3. **Terminar el Calendario** (lo pendiente de B30: filas conductor×línea con nombre agrupado, filtros, toggle semanal/mensual; `TODO[cuadrante-vista-calendario]`).
4. **Dashboard real** (hoy placeholder para jefe y super_admin).
5. **Mercado de intercambios** (2º diferenciador; `solicitudes_intercambio`/`versiones_cuadrante`/`cambios_asignaciones` siguen deny-all; revisar `TODO[reabrir-cuadrante-vs-intercambios]` y `TODO[actualizar-cuadrante-estadisticas-publicado]`).
6. **B35.3 — restricciones que cruzan meses** (domingos libres al año, horas anuales, arrastre de findes entre meses: `TODO[findes-consecutivos-cruce-meses]`) **solo si el piloto lo pide**.
- Cerrado en B33: publicar/cerrar/reabrir + edición manual de celdas. Cerrado en B34: Mi horario del conductor con cuentas reales, reglas self-only y navegación móvil. Cerrado en B35: convenio y festivos con UI + findes consecutivos en el motor + aviso de antelación.
- Después: notificaciones visibles, reset de generación colgada (`TODO[generacion-stuck-reset]`), incidencias, Mis preferencias (placeholder mientras `W_PREF` esté desactivado en el motor).

## 12. Deuda técnica conocida (TODOs activos)

Pendientes asumidos conscientemente durante la construcción del proyecto, con etiqueta de contexto para grep. Revisar en el bloque o sesión indicada.

- `TODO[firebase-json-runtime-explicito]` — ✅ **RESUELTO en B31**: `"runtime": "nodejs22"` explícito en el bloque `functions` de `firebase.json` (junto con engines.node=22 y .nvmrc). Origen: Bloque 3.
- `TODO[deuda-tecnica-deps-firebase]` — cluster de 9 vulnerabilidades `low` transitivas en `firebase-admin` → `@google-cloud/firestore` → `google-gax` → `retry-request` → `teeny-request` → `http-proxy-agent` → `@tootallnate/once`. `npm audit fix --force` bajaría `firebase-admin` a v10 (breaking change). Postpuesto hasta que Firebase publique nuevas versiones del SDK con las deps actualizadas.
- `TODO[verificar-reglas-en-uso-real]` — Reglas Firestore activas verificadas implícitamente en runtime:
  - ✅ /usuarios read self-only (Bloque 7, AuthContext)
  - ⏳ /tenants, /centros, /conductores (pendientes de CRUDs en Sesiones 4-7)
- `TODO[refactor-zod]` — si los callables crecen a 10 o más, refactorizar validación de payloads a Zod.
- Pendiente actualizar Node de 20.17 a 20.19+ cuando convenga (warn `EBADENGINE` de `eslint-visitor-keys`, no bloqueante).
- `TODO[refactor-shared-build]` — compilar `@albius/shared` a JS con su propio paso de build, eliminar el módulo local `apps/functions/src/collections.ts` y volver a importar `COLLECTIONS` desde `@albius/shared`. Origen: commit `e879854` (sub-bloque 3.2.c). Hoy `shared` se distribuye como TypeScript crudo (`main: "./src/index.ts"`, `noEmit: true`), lo cual funciona en `apps/web` por el resolver de Vite pero no en `apps/functions` tras compilar a CJS. Requiere bloque dedicado: toca `packages/shared`, `apps/functions`, scripts de build raíz y posible verificación de Vite.
- `TODO[email-transport]` — implementar transporte real de email para enviar el `linkPasswordReset` que devuelven los callables `crearJefeTrafico` (3.2.b) y `crearConductor` (3.2.d). Hoy el link queda en la respuesta del callable y el super_admin lo distribuye manualmente. Bloquea el flujo automático de alta de usuarios en producción. Decidir entre SendGrid, Resend, Firebase Extensions u otra alternativa antes del primer despliegue real. **Actualización (post-B35, 2026-09-17):** ya hay dominio — **`albius.es` comprado, con reenvío `info@` configurado**. Falta el proveedor transaccional (candidato: **Resend**) y la verificación DNS del dominio (SPF/DKIM) para enviar desde él; después, cablear el envío del `linkPasswordReset` en `crearJefeTrafico`/`crearConductor` (y `TODO[double-password-change-en-callables]` aflorará entonces).
- `TODO[refactor-verify-helpers]` — extraer los helpers duplicados de `apps/functions/scripts/verify-crearJefeTrafico.mjs` y `verify-crearConductor.mjs` (`signInWithCustomToken`, `invokeCallable`, `checkEmulatorsUp`, `expectError`, `record`) a un módulo compartido. Origen: commit `af29340` (sub-bloque 3.2.e). El Bloque 4 añadió `scripts/bootstrap-super-admin.mjs` y el Bloque 5 añadió `apps/functions/scripts/seed-test-user.mjs`, ambos con scope distinto que no comparte estos helpers concretos. Encaja cuando llegue un tercer script de verificación contra emulator que sí los comparta. Decisión pendiente: módulo `.mjs` auxiliar o migración a `.mts` con TypeScript (pipeline nueva: build / tsx / ts-node).
- `TODO[refactor-ping-helpers]` — el callable `ping` del sub-bloque 3.1 (commit `d6f2540`) lee `request.auth.token` directamente para extraer el campo `rol`. Refactorizar para usar `extractClaims` y `assertAuth` de `apps/functions/src/auth-guards.ts` (introducidos después en 3.2.a, commit `348f77f`), simétrico al resto de callables. Deuda menor, sin urgencia.
- `TODO[tipos-conductor-requeridos]` — revisar si los arrays `lineasPreferentes`, `lineasSecundarias` y `tiposTurnoPermitidos` del modelo `Conductor` (`packages/shared/src/types.ts`) deben permanecer required. El callable `crearConductor` los defaultea a `[]` cuando el payload los omite (DUDA-8 de 3.2.d, commit `e810832`). Si el dominio acepta "todavía no asignado" como estado natural, considerar marcarlos opcionales (`?`) para reflejar mejor el modelo conceptual.
- `TODO[jefe-claims-incompletos]` — definir comportamiento cuando un token tiene `rol=jefe_trafico` pero falta `tenantId` o `centroId` en custom claims. Hoy `crearConductor` cae en el check anti cross-tenant con mensaje confuso (3.2.d): `claims.tenantId === undefined` siempre será distinto del `payload.tenantId`, lanzando "Un jefe de tráfico no puede crear conductores en otro tenant." cuando el problema real es alta incompleta. Decidir entre `permission-denied` con mensaje específico o `failed-precondition` simétrico a `assertJefeTrafico`.
- `TODO[verify-cleanup-usuarios-huerfanos]` — los scripts `verify-crearJefeTrafico.mjs` y `verify-crearConductor.mjs` dejan documentos `/usuarios/{uid}` huérfanos en Firestore tras ejecuciones repetidas: el `uid` se regenera con cada `createUser` y los docs anteriores no se borran. No afectan funcionalmente (los tests no consultan esos docs), pero ensucian el estado del emulator. Añadir limpieza opcional al inicio (p.ej. borrar `/usuarios` con `creadoEn` > timestamp del seed) cuando se consolide la infra de testing. Encaja con `TODO[refactor-verify-helpers]`.
- `TODO[bootstrap-verify-production-layers]` — verificar empíricamente las capas 4 (banner production), 5 (confirmación 'CONFIRMAR' interactiva) y 6 (project ID = albius-cbdb1) del script `scripts/bootstrap-super-admin.mjs`. Origen: Bloque 4 (alta de super_admin). Hoy están probadas solo por inspección de código + revisión de mensajes; las capas 1-3 sí se verificaron empíricamente (11/11 casos contra emulator). Verificación empírica de 4-6 requiere ejecución contra Firebase real, pospuesta al Bloque 18 (deploy) o equivalente. **Actualización B29 C.4.4:** la **capa de credenciales (3b, ADC)** quedó funcional en Windows (fix de ruta `%APPDATA%\gcloud`, D6.17) y el **patrón completo de guardas (capas 1-6) se EJERCITÓ con éxito contra producción** por `seed-caso-prueba.mjs`, que clona el mismo `setupTarget`/`detectProductionCredentials` (target explícito + `project_id===albius-cbdb1` + confirmación `CONFIRMAR` + veto de env-vars de emulador). En la práctica el patrón bootstrap está validado contra Firebase real; el `bootstrap-super-admin.mjs` concreto sigue sin correrse en producción, pero su lógica está probada por el gemelo.
- `TODO[verify-full-password-reset-flow]` — verificar empíricamente el flujo COMPLETO del password reset link emitido por el bootstrap CLI con `generatePasswordResetLink`. Hoy solo se ha verificado con `apps/functions/scripts/seed-test-user.mjs` que usa password directo (atajo aceptable solo para testing en emulator, decidido durante el Bloque 5). Origen: Bloque 5. Encaja cuando exista transporte de email implementado (ver `TODO[email-transport]`) o cuando se valide manualmente siguiendo el link emitido por bootstrap CLI en emulator desde el navegador.
- `TODO[web-bundle-splitting]` — ✅ **RESUELTO (parcial) en Bloque 15** (era PRIORITARIO desde B7).

  RESUELTO (parcial) en Bloque 15. Splitting implementado: React.lazy por ruta + manualChunks (firebase-vendor + react-vendor). Entry 961→55.86 kB. Objetivo redefinido (Cuestión 5 B15): ningún chunk monolítico + firebase aislado cacheable + páginas diferidas por ruta — CUMPLIDO. El <500 kB estricto del entry crítico NO se persiguió (requiere diferir init de firebase, ver `TODO[firebase-lazy-init]`). Histórico de cifras preservado abajo.

  Estado actual (histórico previo a B15):
  - Bundle principal: 943.18 kB (gzip 258.72 kB) tras Bloque 12 (+12.34 kB lineal por servicios + 4 componentes Centros, sin nuevas deps Radix); histórico: 615 kB tras B6, +189 kB en B7 por `firebase/functions` SDK + deps transitivas gRPC/Protobuf, +126 kB en B10 por umbrella Dialog/Select de Radix, +12.34 kB en B12 lineal.
  - Umbral Vite warning (500 kB) cruzado desde Bloque 5
  - Umbral "obligatorio" auto-impuesto (700 kB) cruzado en Bloque 7

  Plan de ataque (sub-bloque dedicado, Sesión 4 o 5):
  1. Analizar bundle con `vite-bundle-visualizer` para identificar top consumidores
  2. Code-splitting por ruta (`React.lazy` + `Suspense`) para páginas pesadas
  3. Considerar `manualChunks` en `vite.config.ts` para separar firebase en chunk vendor propio
  4. Objetivo: bundle principal <500 kB, lazy chunks <200 kB cada uno

  Justificación de no abordarlo en Bloque 7:
  - Cerrar Sesión 3 es prioridad
  - 804 kB funciona y se despliega
  - Solución completa requiere arquitectura, no parche local

  Origen: Bloque 7 PASO 4 (crecimiento por `firebase/functions` SDK) → ampliado en Bloque 10 (umbrella Radix Dialog/Select).
- `TODO[firebase-lazy-init]` — Firebase se inicializa top-level en `lib/firebase.ts` y AuthContext lo consume en el árbol raíz vía AuthProvider en `main.tsx`. Esto mantiene firebase (~450-500 kB) en el camino crítico del primer paint pase lo que pase. B15 lo separa en vendor chunk cacheable + paralelizable, pero NO lo difiere. Si en futuro Sesión 18+ se persigue agresivamente reducir el chunk crítico (<500 kB estricto), diferir la init de Firebase con lazy dynamic import dentro de AuthContext sería el camino. Toca `lib/firebase.ts` + AuthContext + posiblemente ProtectedRoute. Riesgo: romper flujo login + gate B7. Sub-bloque dedicado si se aborda. Origen: Bloque 15.
- `TODO[remove-firebase-stubs]` — eliminar `packages/shared/src/firebase-stubs.ts`. Era un stub temporal con tipos `Timestamp` y `GeoPoint` mientras `@albius/shared` no integraba el Firebase SDK. Hoy `apps/web/src/lib/firebase.ts` (Web SDK) y `apps/functions/src/` (Admin SDK) están operativos; el stub ya no cumple función. Verificar que ningún tipo de `packages/shared` lo importe antes de borrar (grep actual: `types.ts` y `index.ts` lo referencian; migrar a importar tipos de `firebase/firestore` o `firebase-admin/firestore` directamente, o decidir si `@albius/shared` se mantiene neutral SDK). Origen: Bloque 5 (verificación de §5).
- `TODO[topbar-tenant-centro-hidratado]` — Topbar muestra solo `ROL_LABEL` del usuario. Cuando exista hidratación de tenant/centro en `AuthContext` (Sesiones 4-7), refinar para mostrar 'ALSA Murcia · Centro Espinardo' en vez de solo 'Jefe de tráfico'. Origen: Bloque 6. Patrón a seguir: D7.3 + D7.9 (extender `fetchUsuarioDoc` o equivalente, exponer en `AuthUser`, usar `refreshAuthUser()` si requiere refresh).
- `TODO[double-password-change-en-callables]` — los callables `crearJefeTrafico` y `crearConductor` y el bootstrap CLI dejan `passwordChangeRequired=true` al alta Y emiten `linkPasswordReset` con `generatePasswordResetLink`. Cuando un usuario real complete el link reset (set password vía Firebase Auth UI) y haga login, ProtectedRoute lo enviará a `/cambiar-password` y cambiará la contraseña **dos veces**. UX wart en producción. Alternativas a considerar: (a) flipear el flag durante el reset link flow (Firebase no notifica al backend, requeriría tracking lateral); (b) cambiar callables/bootstrap para set `passwordChangeRequired=false` por defecto y dejar que el link sea la única ceremonia; (c) detectar "first login" por `ultimoLogin` ausente en lugar del flag. NO bloquea Bloque 7 (los users seed con flag=true funcionan porque no usan link, usan password directo). Surface en producción cuando se cierre `TODO[email-transport]`. Origen: descubrimiento durante PASO 3 del Bloque 7.
- `TODO[firebase-region]` — actualmente `us-central1` por default de Firebase en `apps/web/src/lib/firebase.ts` (`getFunctions(firebaseApp, 'us-central1')`). Considerar `europe-west1` cuando se despliegue a producción real, para reducir latencia desde España (~80 ms vs ~150 ms desde Europa hacia US-central). Cambio coordinado: también hay que desplegar los callables a esa región (`apps/functions`). Origen: Bloque 7. **Ampliación B29:** parcialmente abordado — los callables del optimizador (`generarCuadrante` + `generarCuadranteWorker`) SÍ están en `europe-west1` y el frontend los invoca con la 2ª instancia `functionsEu` (D6.15); los **33 callables restantes + la instancia `functions` por defecto siguen en `us-central1`**. Mover todo implicaría re-desplegar los 33 + cambiar `firebase.ts`; pendiente como decisión de optimización, no urgente.
- `TODO[validar-cif-consolidar-shared]` — consolidar el validador de CIF en `packages/shared/src/validators/cif.ts` cuando cierre `TODO[refactor-shared-build]`. Hoy doble copia (D4.4): `packages/shared/src/validators/cif.ts` (canónica, consumida por `apps/web` vía `@albius/shared`) + `apps/functions/src/cif-validator.ts` (consumida por callables de B9/B11; duplicada por el resolver CJS de `apps/functions`). Integridad verificable con `Get-FileHash`/`sha256sum` sobre los 2 archivos — las 2 deben tener hash SHA256 idéntico. La cabecera de cada archivo recuerda mantener sincronizadas las 2 copias. Origen: Bloque 8 (Sesión 4).
- `TODO[tenant-logo-upload]` — implementar upload de logo de tenant (`Tenant.logoUrl`). Requiere Firebase Storage habilitado + reglas storage + UI dedicada (input file + preview + delete). Out of scope de Sesión 4 (decidido en PASO 2 del Bloque 8). Encaja cuando se aborde branding/personalización de UI por tenant. Origen: Bloque 8 (Sesión 4).
- `TODO[edit-cif-procedimiento]` — si surge necesidad operativa de corregir un typo en el CIF de un tenant existente, documentar procedimiento manual (consola Firebase + auditoría) o evaluar añadir callable especial con flag explícito de "corrección de typo". Por defecto, `cif` es inmutable: reglas Firestore (B8 sub-paso 6) lo bloquean a nivel `update`, y los callables de B9 lo vetarán también. El escape hatch existente (`forzarCIF: true` + `cifValidacionForzada: true`) cubre el caso "CIF legítimo fuera del formato español estándar" pero NO el caso "typo en alta". Origen: Bloque 8 (Sesión 4).
- `TODO[deploy-firestore-indexes]` — ✅ **RESUELTO en B29 C.4.4.** `firebase deploy --only firestore:rules,firestore:indexes` ejecutado contra producción (antes solo se había desplegado functions; el proyecto virgen estaba en *locked mode* → el cliente daba `permission-denied` en `onSnapshot` del cuadrante). Aprendizaje: en el primer deploy a producción **reglas + índices se despliegan explícitamente**; los índices tardan minutos en construirse (el de `asignaciones` se necesita para `listarAsignaciones`). Origen: Bloque 9 (Sesión 4); resuelto B29.
- `TODO[validar-zona-iana]` — validar `configuracion.zonaHoraria` contra `Intl.supportedValuesOf('timeZone')` o lista hardcoded de zonas europeas comunes. Hoy: `assertNonEmptyString` en `validation.ts`. Si llega valor inválido, el render falla visiblemente con `Intl` en el frontend (B10+) — error visible, no silencioso. Origen: Bloque 9 (Sesión 4).
- `TODO[shadcn-add-cwd]` — `npx shadcn@latest add` desde `apps/web` crea filesystem literal `@/components/ui/` en lugar de resolver el alias a `src/components/ui/`. Workaround aplicado en B10: `Move-Item` post-creación + `Remove-Item` de la carpeta `@`. Cuando se aborde refactor general de herramientas, evaluar flag de `cwd` del CLI o configurar `components.json` para que respete el alias en sub-directorios. Alternativa: ejecutar `shadcn add` desde la raíz del repo. Origen: Bloque 10.
- `TODO[radix-import-style-mix]` — el proyecto mezcla dos estilos de import de Radix: `button.tsx` (pre-B10) usa `import { Slot } from '@radix-ui/react-slot';` `dialog.tsx`, `select.tsx` y demás de B10 usan `import { ... } from 'radix-ui'` (umbrella). Coexisten en `package.json` (`@radix-ui/react-slot ^1.1.0` + `radix-ui ^1.4.3`). Sin conflicto funcional pero `Slot` puede aparecer duplicado en bundle. Refactor: migrar `button.tsx` al umbrella + eliminar `@radix-ui/react-slot`. Trivial (3 líneas). Origen: Bloque 10. **Ampliación B15:** el splitting de B15 reveló empíricamente que conviven 2 implementaciones de Slot en chunks distintos: standalone (`@radix-ui/react-slot`) en el entry vía `button.tsx`, y umbrella (`radix-ui`) en table/checkbox vía Dialog/Select/Checkbox. No es duplicación dañina dentro del mismo chunk pero sí redundancia confirmada. Migrar `button.tsx` a `import { Slot } from 'radix-ui'` + eliminar dep `@radix-ui/react-slot` reutiliza el Slot del umbrella. Sub-bloque trivial (~3 líneas). Prioridad baja (~1-2 kB en chunks separados). Origen ampliación: Bloque 15.
- `TODO[cif-copia3-redundante]` — la copia 3 en `apps/web/src/lib/validators/cif.ts` creada en B8 nunca llega a tener consumer. B10 demostró que `apps/web` puede consumir directamente la copia canónica vía `@albius/shared` (Vite resuelve los .ts crudos del paquete shared). La duplicación real es solo `apps/functions/src/cif-validator.ts` (copia 2, necesaria porque `tsc → CJS` no resuelve los .ts en runtime). Acción: eliminar `apps/web/src/lib/validators/cif.ts` cuando se aborde limpieza de archivos muertos, o cuando cierre `TODO[refactor-shared-build]`. Encaja en B11 o Sesión 5. Origen: Bloque 10.
- `TODO[delete-on-empty-fields]` — vaciar campos opcionales preexistentes (`nombreComercial`, `logoUrl`) en el form de edición no envía el cambio al backend porque el callable no soporta delete-on-empty. Limitación: el operador no puede "quitar" un nombre comercial una vez asignado. Soluciones cuando surja necesidad: (a) callable acepta `FieldValue.delete()` sentinel; (b) frontend traduce `""` a sentinel de borrado. También aplica a coordenadas de Centro (B11): vaciar coordenadas preexistentes no se soporta, solo omit (omit = "no tocar"). Mismo patrón de solución cuando se aborde. Origen: Bloque 10 (ampliado en Bloque 11).
- `TODO[token-refresh-strategy]` — Firebase Auth ID tokens expiran tras 1 hora. Si el operador deja la web abierta mucho tiempo, el primer callable tras expiración falla con `unauthenticated`. UX actual: error genérico ("Sesión expirada. Recarga la página."). Mejor: interceptar el error y forzar refresh con `getIdToken(true)`; solo mostrar el mensaje si el refresh también falla, o redirect a `/login` con banner "Sesión expirada". Pospuesto a Sesión 18 (deploy hardening). Origen: Bloque 10.
- `TODO[centro-nombre-unicidad]` — actualmente no se valida unicidad de nombre de centro dentro de un tenant. Dos centros con mismo nombre en el mismo tenant son posibles. Si en uso real genera confusión, añadir `assertNombreCentroUnico(db, tenantId, nombre)` en `refs.ts` + índice `(centros: tenantId + nombre)` en `firestore.indexes.json`. Origen: Bloque 11.
- `TODO[email-verification-on-change]` — `actualizarUsuario` hace `auth.updateUser({email})` que resetea `emailVerified=false` en Auth. UX actual: el usuario debe re-verificar por su cuenta o vía password reset link. Cuando cierre `TODO[email-transport]`, implementar envío automático de verification link tras cambio de email. Origen: Bloque 13.
- `TODO[suspension-efectos-auth]` — `estado='suspendido'` en `/usuarios` actualmente SOLO cambia el doc; NO toca `Auth.disabled` ni `revokeRefreshTokens`. Usuario suspendido puede seguir haciendo login. Decisión MVP coherente (sin turnos asignados no hay cascada operativa). Revisar cuando aparezcan turnos y asignaciones: ¿bloquear login? ¿revocar tokens? ¿bloquear callables de operativa? Origen: Bloque 13.
- `TODO[perfil-self-edit-via-callable]` — El endurecimiento de la regla owner en `/usuarios` update (B13) bloquea edición directa de `email`/`nombreCompleto` por el propio usuario. Cuando exista pantalla "editar mi perfil" (Sesión futura), debe canalizar esa edición por un callable dedicado (ej. `actualizarMiPerfil` con `assertAuth` + self-only + el mismo dual-write Auth+Firestore que `actualizarUsuario`). NO añadir lógica directa de cliente. Origen: Bloque 13.
- `TODO[dual-write-rollback-failed]` — `actualizarUsuario` hace dual-write Auth+Firestore con rollback inverso. Si la escritura Firestore falla, se revierte Auth. Si ESE rollback también falla (caso edge muy improbable), el sistema queda inconsistente (Auth con valor nuevo, Firestore con valor viejo) y solo se loggea. Mitigación futura: alerta/notificación + reconciliación periódica + idempotent retry. Por ahora confiamos en que el rollback no falla en práctica. Origen: Bloque 13.
- `TODO[conductor-campos-operativos-en-alta]` — ✅ **RESUELTO en Bloque 21.** Pickers operativos en el alta (`ConductorCamposOperativos` + `MultiSelectCheckbox`, poblados con `listarLineas`/`listarTiposTurno` del centro) + callable `actualizarConductor` para editarlos después. Los conductores creados desde UI ya configuran líneas preferentes/secundarias y tipos de turno permitidos/excluidos. La edición vía UI (ConductoresPage del jefe) queda para B22; el callable ya existe. Origen: Bloque 14.
- `TODO[modelo-linea-paradas]` — `paradasIda`/`paradasVuelta` de `Linea` son `string[]` de IDs de paradas embebidos. **PROVISIONAL, no contrato canónico.** La relación línea↔parada se redecide en B17/Paradas: ¿array embebido, entidad de unión `linea_paradas` con orden+sentido, o parada con array inverso? El embebido escala mal (una parada física la comparten muchas líneas; el orden importa; ida/vuelta puede ser asimétrico). B16 mantiene los arrays por continuidad con el modelo previo, el alta los deja vacíos (D4.2), pero NO los fosiliza como decisión. Comentario inline en `types.ts` (`Linea`). Origen: Bloque 16. **Dirección resuelta en B17 (OK del usuario):** doble-array denormalizado (Línea→paradas + `Parada.lineas[]` inverso, sincronizados por una Cloud Function), **NO** entidad de unión — idiomático Firestore, evita el N+1 al pintar el recorrido, y alineado con el PDF §5.5/§5.6. **Ejecución PENDIENTE** cuando llegue el CRUD de Paradas: colapsar `paradasIda`/`paradasVuelta` en un único `paradasIds` ordenado (corrige la divergencia TS↔PDF detectada en B16); el sentido ida/vuelta vive en `Frecuencia` (`sentido`), no en la Parada. El form de B17 NO gestiona paradas (colección deny-all sin CRUD todavía).
- `TODO[linea-softdelete-cascada]` — `actualizarLinea` cambia `estado` (incluida la inactivación/suspensión) sin verificación D4.6 de dependencias hijas activas, porque paradas/frecuencias que colgarán de Línea aún no existen como CRUD. Cuando existan, evaluar si inactivar/suspender una línea debe verificar dependencias activas (como Tenant→Centros en `actualizarTenant` y Centro→Conductores en `actualizarCentro`). Comentario inline ya en `actualizarLinea.ts`. Origen: Bloque 16.
- `TODO[unificar-fechacreacion-creadoen]` — originalmente 10 entidades del modelo (`packages/shared/src/types.ts`) usaban `fechaCreacion` (esquema viejo); 4 (Tenant/Centro/Usuario/Conductor) usan el `creadoEn` canónico (D3.7), y Conductor coexiste con ambos. Cortaron limpio al canónico (D6.4: sin `fechaCreacion`): Línea (B16), TipoTurno (B18), **Asignacion (B26)** y **Festivo (B27)**. Pendiente: migrar las **6 entidades restantes** que aún usan `fechaCreacion` (Tenant, Centro, FrecuenciaExcepcional, PreferenciaPuntual, SolicitudIntercambio, Notificacion) a `creadoEn` y retirar `fechaCreacion`. (Nota: Asignacion se migró en B26 pero este TODO no se actualizó entonces — de ahí la corrección de "8 restantes" a "6" en B27.) Toca modelo + reglas Firestore (`firestore.rules` lo veta como inmutable en tenants/centros/usuarios) + validators (`validation.ts` lo rechaza como no editable). Bloque dedicado bajo §10 (modelo + reglas). Origen: Bloque 16.
- `TODO[tramos-partido-dentro-de-rango]` — la validación de que los tramos de un turno partido caen dentro de `[horaInicio, horaFin]` solo se aplica cuando el turno NO cruza medianoche (`horaFin > horaInicio`). Para turnos partidos que cruzan medianoche, solo se valida formato `HH:mm` + `inicio < fin` por tramo, no la contención en rango. Resolver la lógica de contención con cruce de medianoche cuando surja un caso real de partido nocturno. Comentario inline en `validation.ts` (`assertTramosPartido`). Origen: Bloque 18.
- `TODO[tipoturno-obsoleto-conductores]` — marcar un tipo de turno como `'obsoleto'` (soft-delete D4.3) NO verifica ni bloquea si hay conductores que lo referencian en `tiposTurnoPermitidos`/`tiposTurnoExcluidos`. Un tipo obsoleto sigue siendo referenciable (histórico). Revisar cuando exista el cuadrante: ¿un tipo obsoleto debe poder asignarse en turnos nuevos? ¿se limpia de las preferencias del conductor? Comentario inline en `actualizarTipoTurno.ts`. Origen: Bloque 18.
- `TODO[conductor-fechabaja-cascada]` — `actualizarConductor` cambia `estado` (incluido el paso a `'baja_definitiva'`) SIN auto-gestionar `fechaBaja` ni verificar/cascadear dependencias (no hay turnos ni asignaciones en MVP). Al pasar a `baja_definitiva` debería, en su momento, sellar `fechaBaja` y evaluar la cascada operativa (¿desasignar turnos futuros? ¿bloquear login como en `TODO[suspension-efectos-auth]`?). Además, `actualizarConductor` NO valida que los IDs de `lineasPreferentes`/`tiposTurnoPermitidos`/… referencien docs reales (paridad con `crearConductor`); la UI solo ofrece IDs del centro vía pickers, pero un cliente directo podría enviar IDs inexistentes. Revisar ambas cuando exista el cuadrante. Comentario inline en `actualizarConductor.ts`. Origen: Bloque 21.
- `TODO[frecuencia-cruce-medianoche]` — las frecuencias NO cruzan medianoche en el MVP: un tramo es una franja del MISMO día con `horaInicio < horaFin` (a diferencia de TipoTurno, donde `horaFin < horaInicio` señala cruce). Una línea nocturna que opera de madrugada se modela con horas de madrugada del mismo día (p.ej. 00:30–05:00). Si surge un servicio real que cruza las 00:00 (p.ej. 23:30–01:00), habrá que partirlo en dos frecuencias o introducir un flag de cruce como en TipoTurno (y entonces la lógica de no-solape D6.8 tendría que contemplar el wrap). Comentario inline en `validation.ts` (`assertHoraInicioAntesFin`). Origen: Bloque 23.
- `TODO[convenio-restricciones-no-aplicadas-mvp]` — **PARCIAL**: `maxDiasConsecutivosTrabajados` se aplica desde B29 (racha, dura), `maxFinesSemanaConsecutivosTrabajados` desde B35.2 (BLANDA, `W_FINDE`, con reporte en `estadisticas`) y `antelacionMinimaPublicacionDias` desde B35.2 como aviso en el dialog de publicar. **Siguen sin aplicarse**: `descansoSemanalMinimoHoras` (`TODO[descanso-semanal-36h-explicito]`), `minDomingosLibresAño` y `maxHorasAnuales` (cruzan meses, B35.3 si el piloto lo pide) y `horasFestivoComputanComoExtras` (nóminas, no motor). Texto original: el modelo `Convenio` (B25) tiene campos de restricción que el optimizador MVP **NO aplicará todavía**: `descansoSemanalMinimoHoras` (36h continuas), `maxDiasConsecutivosTrabajados`, `maxFinesSemanaConsecutivosTrabajados`, `minDomingosLibresAño`, `antelacionMinimaPublicacionDias`, `horasFestivoComputanComoExtras`. El spike Pyomo solo validó R2 (descanso entre jornadas), R3 (max horas semanales) y R5 (mínimo de días libres). Estos campos quedan persistidos y disponibles, pero incorporarlos como restricciones reales del optimizador cuando se aborde el bloque Optimizador (igual que `computoHoras?`, que tampoco tiene consumidor aún). Origen: Bloque 25.
- `TODO[asignacion-validar-referencias]` — ✅ **RESUELTO en B33.2** para `crearAsignacion`/`actualizarAsignacion` (`assertConductorDelCentro` + `assertTipoTurnoDelCentro` laxa + R1 `assertConductorLibreEnFecha`); `crearAsignacionesLote` sigue laxo a propósito (lo usa el optimizador con ids reales del centro). Texto original: `crearAsignacion`/`actualizarAsignacion`/`crearAsignacionesLote` (B26) NO validan que `conductorId` ni `tipoTurnoId` referencien documentos reales (paridad con la laxitud del resto del proyecto: el alta no comprueba existencia de ids embebidos). La UI del cuadrante visual solo ofrecerá ids del propio centro (conductores y tipos de turno activos), pero un cliente directo podría enviar ids inexistentes. Revisar cuando exista el cuadrante visual / al diseñar B27 (el optimizador trabajará con ids reales del centro, así que el riesgo es solo de escritura manual fuera de la UI). Comentario inline en `crearAsignacion.ts`. Origen: Bloque 26.
- `TODO[actualizar-cuadrante-estadisticas]` — ✅ **RESUELTO en Bloque 28.** Callable `actualizarCuadrante` + función `actualizarCuadranteDoc` (`logic/cuadrante.ts`) implementados: escriben `estadisticas` (KPIs) y el bloque de regeneración (`generadoPor`/`fechaGeneracion`/`modoGeneracion`), con veto de inmutables, auditoría D4.1 y gate de borrador. Origen: Bloque 26; resuelto B28.
- `TODO[actualizar-cuadrante-estadisticas-publicado]` — `actualizarCuadrante` (B28) **exige cuadrante en borrador** (`assertCuadranteEditable`). Cuando llegue el bloque de Intercambios, habrá que **relajar el gate para escribir `estadisticas` (solo) en estado `publicado`**: tras un intercambio sobre un cuadrante publicado, los KPIs (cobertura/satisfacción/preferencias) se recalculan y deben poder persistirse sin reabrir el cuadrante. La regeneración (bloque `generadoPor`/`fechaGeneracion`/`modoGeneracion` + volcado de asignaciones) debe seguir exigiendo borrador. Comentario inline en `logic/cuadrante.ts`. Origen: Bloque 28.
- `TODO[festivos-frontend]` — ✅ **RESUELTO en B35.1** (`FestivosPage` del jefe: listado + alta/edición/borrado de los festivos de su centro; tenant-wide y oficiales en solo lectura; los tenant-wide siguen entrando por seed o super_admin). Texto original: la colección `festivos` está habilitada en backend (callables `crearFestivo`/`actualizarFestivo`/`eliminarFestivo`, reglas, índice; B27) pero NO tiene UI de gestión para el jefe (página/CRUD de festivos). Mientras tanto, los festivos se siembran por seed para que el optimizador (y `resolverTipoDia`) puedan leerlos. Construir la página de festivos (molde de otras páginas operativas del jefe: listado + Dialog alta/edición + Dialog destructivo; respetar `esEditable` para no ofrecer editar/borrar oficiales; el ámbito tenant-wide solo lo gestiona super_admin). Origen: Bloque 27.
- `TODO[mirror-tipoturno-drift-b18]` — el mirror `docs/03-modelo-de-datos.ts` quedó desincronizado del canónico `packages/shared/src/types.ts` para `TipoTurno` desde B18 (el mirror aún tiene `tipoFranja: TipoFranjaTurno`, `fechaCreacion`, `centroId?` opcional y le faltan `esNocturno`/`color`/auditoría canónica). B27 solo añadió ahí la línea `tiposDiaAplicables` sin reconciliar el resto (no es deuda de B27). Reconciliar el mirror entero contra types.ts cuando se aborde limpieza de docs. Origen: detectado en Bloque 27 (deuda de B18).
- `TODO[demanda-variable-intra-tipodia]` — la demanda del optimizador (vía `TipoTurno.tiposDiaAplicables`) es **uniforme por `tipoDia`**: todos los laborables del mes tienen EXACTAMENTE el mismo cuadro de turnos activos. NO se puede expresar "el turno 7M2 se cubre unos laborables sí y otros no" (variación día a día dentro del mismo `tipoDia`). Suficiente para la Forma 1 estable (servicio urbano regular; los festivos ya son un `tipoDia` aparte vía B27/`festivos`). Si surge demanda real día-variable (refuerzo solo los viernes, recorte por evento en una fecha concreta), haría falta un **override de demanda por fecha** (paralelo a `frecuencias_excepcionales` B23, pero para el lado de demanda del optimizador, que hoy no existe). Origen: Bloque 29 Fase A.
- `TODO[tipoturno-lineaid]` — ✅ **RESUELTO en B30.1** (`lineaId?` opcional + `assertLineaDelCentro` laxa, D6.18; el seed B30.2 enlaza los 38 turnos). Texto original: `TipoTurno` **NO tiene `lineaId`** (la línea solo está embebida por convención en el `codigo`, p.ej. "7M2"→línea 7, no legible por máquina). Por eso `Conductor.lineasPreferentes` (IDs de `Linea`) **no puede cruzarse con los turnos** para un término de preferencia por línea (detectado en reconocimiento B29). Añadir `lineaId?: string` a `TipoTurno` (y validarlo contra `/lineas` del centro) cuando se aborden las preferencias del optimizador (hoy `W_PREF` está desactivado). Toca modelo + validator + callables + form. Origen: Bloque 29 Fase A.
- `TODO[catalogo-turnos-escala-ux]` — en la Forma 1 (cada `TipoTurno` = plaza única) un centro real tiene ~100-140 tipos de turno. El **dato y el backend aguantan** (índice `(tenantId+centroId+codigo)`, listado client-side <1000), pero la **UX NO está dimensionada para ~140**: (a) `TiposTurnoPage` muestra una tabla plana (filtros estado+búsqueda, pero 140 filas piden agrupación por franja/línea); (b) el **alta es 1-a-1** (dar de alta 140 turnos a mano es inviable → hará falta import masivo / plantillas / duplicar turno); (c) el `MultiSelectCheckbox` de `ConductorCamposOperativos` (B21) lista TODOS los tipos del centro **sin buscador** → con 140 es inmanejable. Reforzar (buscador/paginación/import) cuando se cargue el catálogo real. Origen: Bloque 29 Fase A.
- `TODO[ausencias-por-rango]` — ✅ **RESUELTO en B32** (B32.1 modelo+callables+reglas, B32.3 cruce en `buildRequest` + seed, B32.2 UI; verificado en producción: 50/60 en pool, 100% cobertura). Texto original: **EL GRAN SIGUIENTE BLOQUE tras B29.** El optimizador hoy genera con `ausencias=[]` (no hay modelo de ausencias en MVP), así que un cuadrante REAL asignaría turnos a conductores de **vacaciones / baja / permiso (V/E/B)**. Falta: (a) modelar ausencias por RANGO de fechas (no día a día) por conductor; (b) UI para que el jefe (o el conductor) las registre; (c) cruzarlas en `buildRequest` → `OptimizarRequest.ausencias[]` (el schema Python y el worker ya lo aceptan, solo llega vacío). Sin esto el optimizador no es usable en producción real. Origen: B29 (reconocido como el bloqueante del primer cuadrante real).
- `TODO[cuadrante-asignaciones-error-state]` — la vista del cuadrante **enmascara el error** de `listarAsignaciones`: el `catch` deja `asignaciones=[]` y `AsignacionesTabla` muestra *"sin asignaciones"* tanto si la query FALLA (p.ej. índice construyéndose) como si hay 0 reales. Costó tiempo de diagnóstico en C.4.4 (916 asignaciones escritas pero la vista decía 0 porque el índice aún no estaba READY). Distinguir **"no se pudo cargar (reintentar)"** de **"0 asignaciones reales"** con un estado de error explícito. `apps/web` CuadrantePage/AsignacionesTabla. Origen: B29 C.4.4.
- `TODO[node-runtime-upgrade]` — ✅ **RESUELTO en B31**: engines.node=22 + runtime nodejs22 + .nvmrc, desplegado y verificado (38 functions en nodejs22). Node 20 se decomisionaba el 2026-10-30. Origen: B29 C.4.
- `TODO[generacion-stuck-reset]` — el worker `generarCuadranteWorker` usa `maxAttempts:1` (los errores de APLICACIÓN se capturan y marcan `'error'`, deterministas → no reintentar). Pero un fallo de **INFRAESTRUCTURA** (OOM/crash/timeout antes del `catch`) deja el cuadrante colgado en `estadoGeneracion='generando'`, y `generarCuadrante` rechaza re-lanzar si ya hay una generación en curso → atascado. Añadir un **reset**: un botón "forzar reset" para el jefe, o un timeout que marque `'error'` si lleva > N min en `'generando'`. `regenerarAsignaciones` es idempotente, así que re-lanzar es seguro. Comentario inline en el worker. Origen: B29 C.3.
- `TODO[cuadrante-vista-superadmin]` — la vista del cuadrante es **jefe_trafico-only** (gate D4.13, centro de claims). El super_admin tiene el link en su nav pero vería `NoAutorizadoView`; necesitaría un **selector de centro** (su token no lleva `centroId`). Origen: B29 C.4.2.
- `TODO[cuadrante-vista-calendario]` — **PARCIAL en B30.3.1** (`CalendarioPage`: rejilla conductor×día coloreada por línea, sticky, UTC) **+ B33.2** (edición manual por celda, filas de todos los conductores, rejilla en cuanto existe el cuadrante). Pendiente: filas conductor×línea con nombre agrupado (3.2), filtros (3.3), toggle semanal/mensual (3.4), y que `CuadrantePage` deje de mostrar la tabla plana (cap 200 de ~916 filas) o enlace al calendario. Origen: B29 C.4.2.
- `TODO[estado-conductor-deprecar-ausencias]` — retirar `'vacaciones'` y `'baja_temporal'` del enum `EstadoConductor` (`types.ts` + mirror + validators + `ESTADO_CONDUCTOR_LABEL` + Select de `ConductorEditForm` + seeds/E2E de B22 que siembran un conductor en `'vacaciones'`): desde B32 son **redundantes con `Ausencia`** y además sacaban al conductor del mes ENTERO. Hoy conviven (deprecados de facto; el pool del optimizador sigue siendo `estado=='activo'`). **Requiere migración** de los docs que los tengan (→ `'activo'` + una ausencia equivalente). Bloque dedicado bajo §10 (modelo + reglas). Origen: B32.1.
- `TODO[equidad-normalizar-dias-disponibles]` — `buildRequest` **excluye del pool** a los conductores ausentes el mes completo (D6.21) porque el término de equidad de `optimizer.py` (`span` vs `mean_load`) los vería con `wload=0` y empujaría a bajar la carga de los demás. Es el parche barato en el orquestador. La solución correcta: que **`optimizer.py` normalice la equidad por días disponibles** de cada conductor (carga relativa), lo que también trata bien a los ausentes PARCIALES (una semana de vacaciones hoy cuenta como carga baja "injusta"). Entonces la exclusión sobra. Toca el motor Python + redeploy a Cloud Run. Origen: B32.3.
- `TODO[ausencias-e2e-spec]` — la UI de ausencias (B32.2) se verificó con la suite existente (24/24, regresión de `ConductoresTable`) y un **smoke Playwright ad-hoc** (alta de rango, día suelto con el atajo, solape rechazado, filtro por mes, edición parcial, borrado; no commiteado). Falta el **spec propio** `apps/web/e2e/ausencias.spec.ts` + reset idempotente (D6.7), reutilizando el storageState del jefe y los 3 conductores de `reset-conductores-b22`. Origen: B32.2.
- `TODO[npm-audit-vulnerabilidades]` — `npm audit` reporta **39 vulnerabilidades transitivas (2 críticas: tar, websocket-driver; 15 altas: grpc-js, vite, react-router, undici, ws, protobufjs…)**, todas con fix disponible, muchas vía `firebase-tools`/deps de build. Distinto de `TODO[deuda-tecnica-deps-firebase]` (9 low en firebase-admin). Revisar y subir versiones **antes del piloto con datos reales** de una empresa (sub-bloque de deps, con build + lint + E2E de regresión). Origen: B31.2 (detectado al hacer `npm install`).
- `TODO[credenciales-seed-en-git]` — `scripts/seed-caso-prueba.mjs` lleva en claro el **email y la contraseña del jefe de prueba de PRODUCCIÓN** (`JEFE_EMAIL`/`JEFE_PASSWORD`), y el resumen los imprime. Aceptable para el caso sintético actual; **inaceptable para un piloto con datos de una empresa**: mover la contraseña a variable de entorno / `.env` gitignored (o generarla y entregar un `linkPasswordReset` como hacen los callables), y rotar la actual antes de cargar datos reales. Origen: reconocimiento post-B30 (Sesión 9). **Actualización B34.1:** las 3 cuentas de conductor del seed ya generan su contraseña por ejecución (`randomBytes`, o `SEED_CONDUCTOR_PASSWORD`) y solo la imprimen en el resumen; **queda el jefe** (`JEFE_PASSWORD` en claro).
- `TODO[reabrir-cuadrante-vs-intercambios]` — `reabrirCuadrante` (B33.1) devuelve un cuadrante `publicado` a `borrador` como salida de emergencia de publicar (limpia `fechaPublicacion`/`publicadoPor`; `cerrado` es definitivo y no se reabre). Hoy es seguro porque no existen intercambios. Cuando llegue el bloque de Intercambios, revisar si reabrir es legítimo con intercambios ya cursados sobre ese cuadrante (probablemente rechazar con `failed-precondition` si hay intercambios aceptados, o exigir cancelarlos antes) y si debe dejar rastro en `versiones_cuadrante`. Comentario inline en `reabrirCuadrante.ts`. Origen: Bloque 33.1.
- `TODO[mover-asignacion-atomico]` — "mover un turno de un conductor A a otro B" NO existe como operación única en la edición manual (B33.2): no hay callable de swap ni transacción multi-asignación (los batches de B28 son por cuadrante entero). El jefe lo hace en dos ediciones (quitar en A + asignar en B, o cambiar `conductorId` vía `actualizarAsignacion`); si la segunda falla queda estado intermedio visible en la rejilla (no silencioso). Si el caso resulta frecuente en el piloto, añadir un callable `moverAsignacion`/`intercambiarAsignaciones` con `runTransaction` (≤500 ops, cabe) y R1 sobre ambos destinos. Origen: Bloque 33.2.
- `TODO[cuadrante-kpis-tras-edicion-manual]` — tras una edición manual (crear/cambiar/quitar asignación desde el Calendario, B33.2) `Cuadrante.estadisticas` (cobertura, satisfacción, preferencias) queda DESACTUALIZADO hasta volver a generar; el dialog lo avisa. Opciones: recalcular cobertura en el cliente (barato: plazas cubiertas / demanda por `tiposDiaAplicables`+`resolverTipoDia`) o un callable `recalcularEstadisticas` que escriba `estadisticas` (encaja con `TODO[actualizar-cuadrante-estadisticas-publicado]`). Origen: Bloque 33.2.
- `TODO[habilitacion-vacia-semantica]` — el aviso "no habilitado" de `EditarCeldaDialog` (B33.2) usa la **semántica del motor** (`buildRequest`): un conductor está habilitado para un turno solo si está en `tiposTurnoPermitidos` y no en `Excluidos`, así que un conductor con `tiposTurnoPermitidos=[]` recibe el aviso para CUALQUIER turno. En un piloto con habilitaciones sin configurar el aviso saldrá siempre y se volverá ruido ignorado (y el optimizador no le asignará nada). Decidir si "vacío = todos habilitados" es mejor — cambiaría motor (`buildRequest`/`optimizer.py` R4) y UI a la vez para no divergir. Revisar con datos reales de cliente. Origen: Bloque 33.2.
- `TODO[rules-cuadrante-estado-sin-veto]` — `firestore.rules` permite al jefe `update` directo de `/cuadrantes` vetando solo id/tenant/centro/año/mes/creado*, pero NO veta `estado` ni `estadoGeneracion`, y `/asignaciones` no comprueba el estado del cuadrante padre. El gate "solo se edita en borrador" (`assertCuadranteEditable`) vive SOLO en los callables; un cliente directo con SDK podría publicar/cerrar o editar asignaciones de un cuadrante publicado saltándose los callables. La UI de B33 usa callables, así que no afecta al flujo normal. Endurecer (vetar `estado`/`estadoGeneracion`/`publicado*` en la regla; opcionalmente `get()` del cuadrante padre en `/asignaciones`) en un bloque de reglas bajo §10. Origen: reconocimiento B33.0.
- `TODO[claims-conductorid-migracion]` — desde B34.1 `crearConductor` y el seed ponen `conductorId` en los custom claims del conductor, y las reglas self-only de `/asignaciones` y `/ausencias` lo leen del token. Los conductores creados ANTES de B34.1 no lo llevan (en producción no existe ninguno con cuenta, así que no hay migración); si en algún entorno los hubiera, re-emitir claims con Admin SDK (`setCustomUserClaims` leyendo `/usuarios.conductorId`) y forzar refresh del token. `AuthContext` lee `conductorId` del doc `/usuarios`, no del token, así que la UI funciona igual con token viejo — solo fallarían las lecturas de Firestore. Origen: Bloque 34.1.
- `TODO[movil-revision-paginas-jefe]` — B34.3 dio navegación móvil (drawer) y un repaso BARATO a 375 px: `flex-wrap` en cabeceras y filas de filtros de las páginas de listado (Líneas, Tipos de turno, Conductores, Tenants, Centros, Usuarios; Ausencias y Cuadrante ya lo tenían) para que no desborden el body. Verificado con `mobile.smoke.spec.ts` (7 páginas del jefe sin overflow horizontal). NO revisado en profundidad: las tablas scrollean en su contenedor pero son incómodas en móvil (varias columnas fuera de vista), los Dialogs de alta/edición largos (TipoTurnoForm, ConductorForm) y la rejilla del Calendario (pensada para escritorio). El móvil es para el CONDUCTOR (Mi horario, B34.2); el jefe usa escritorio. Rediseñar las páginas del jefe para móvil solo si el piloto lo pide. Origen: Bloque 34.3.
- `TODO[tw-animate-css-ausente]` — `tw-animate-css` NO está instalado (ni importado en `index.css`), así que las clases `animate-in`/`animate-out`/`fade-in-0`/`zoom-in-95` que shadcn puso en `dialog.tsx` y `select.tsx` (B10) **nunca han animado nada**: los dialogs aparecen y desaparecen en seco. Detectado en B34.3 al hacer el drawer (que usa un keyframe propio `drawer-in` en `index.css`). Decidir: instalar `tw-animate-css` (dependencia nueva, §10; es lo que shadcn espera) o retirar las clases muertas. Origen: Bloque 34.3.
- `TODO[e2e-resets-borran-auth-enlazado]` — `reset-conductor-b21.mjs` y `reset-conductores-b22.mjs` borran TODOS los `/conductores` de tenant-test **y el Auth user + doc `/usuarios` enlazados por `usuarioId`** (necesario para los tests de alta de B21, que crean Auth users con email único). Efecto colateral: cualquier ficha de conductor de prueba que enlace por `usuarioId` a una **cuenta fija del seed** (`conductor@albius.local`, con la que loguea el proyecto setup) hace que la suite completa se lleve esa cuenta por delante y el siguiente spec que loguee como conductor falle (ocurrió en B34.2: `tipos-turno.spec` "como conductor" cayó tras `mobile.smoke`). Mitigación actual: `reset-mi-horario-b34` crea `cond_e2e_conductor` **sin `usuarioId`**. Mejor: que esos resets respeten los uids de los usuarios fijos del seed (lista blanca por email) o que borren solo conductores con `creadoPor` de test. Origen: Bloque 34.2.
- `TODO[rules-convenio-get-inexistente]` — la regla `read` de `/convenio/{centroId}` es `sameTenant(resource.data.tenantId)`; en un `get` sobre un doc INEXISTENTE no hay `resource`, así que Firestore devuelve **permission-denied** en vez de "no existe". `obtenerConvenio` (B35.1) lo traduce a `null` en cliente (para el jefe leyendo su propio centro solo puede significar "aún sin convenio"), pero es un parche: la solución es en reglas, autorizando el `get` por doc-id (`isJefeTrafico() && request.auth.token.centroId == centroId`, evaluable sin `resource`) — mismo patrón conviene revisar en el resto de singletons/get-by-id (`/cuadrantes/{id}` en `obtenerCuadrante`/`suscribirCuadrante`, que hoy funciona porque el caller no distingue "no existe" de "denegado" y ambos acaban en "sin cuadrante"). Bloque de reglas bajo §10. Origen: Bloque 35.1.
- `TODO[descanso-semanal-36h-explicito]` — `descansoSemanalMinimoHoras` (36 h continuas) NO se formula en el motor a propósito (decisión B35.2): R2 (12 h entre jornadas, dura) + el día libre que fuerza la racha (`maxDiasConsecutivosTrabajados`) dan ≥ 36 h continuas en la práctica; la formulación explícita añadiría binarias por ventana candidata (por conductor y semana) para algo que ya se cumple de facto. Implementarla (ventana de 36 h sin turno por conductor y semana, con frontera como R2) solo si un cliente exige literalmente "36 h continuas" o si `validar_solucion` detecta un caso real que la viola. Origen: Bloque 35.2.
- `TODO[noches-consecutivas-campo-convenio]` — el motor penaliza las noches consecutivas como bienestar BLANDO (`W_NOCT=8` por par de noches seguidas) sin límite numérico; muchos convenios fijan un máximo (p.ej. ≤ 3). No hay campo en `Convenio` para ello. Cuando un cliente dé un número concreto: campo `maxNochesConsecutivas?` en el modelo (§10), validator, UI de convenio, `ConvenioInput`/`schemas.py`, y en `optimizer.py` ventana de N+1 noches (dura o blanda según el convenio) con cosido de frontera como la racha. Origen: Bloque 35.2.
- `TODO[findes-consecutivos-cruce-meses]` — la restricción blanda de findes consecutivos (B35.2) cuenta desde 0 al inicio de cada mes: el cosido entre semanas (`findes_prev`, patrón `RachaCarry`) funciona dentro del mes, pero un conductor que cerró el mes anterior con N findes seguidos puede trabajar el primer finde del mes sin penalización. Con 4-5 findes por mes el efecto es visible. Solución (B35.3): `buildRequest` lee el cuadrante anterior (publicado/cerrado) del centro y pasa `findesConsecutivosPrevios` por conductor en `ConductorInput`; el motor lo usa como `findes_prev` de la semana 1. Mismo mecanismo serviría para domingos libres y horas anuales acumuladas. Origen: Bloque 35.2.
- `TODO[week-time-limit-por-tamano-pool]` — `WEEK_TIME_LIMIT=60` s por semana (`optimizer.py`) se queda corto a partir de ~150 conductores en pool. Medido en B35.2 con la restricción blanda de findes: el caso real (50 en pool) da 100 % y óptimo 5/5 a 60 s; el sintético de 178 da 96,4 % (31 plazas sin cubrir) a 60 s y 100 % a 120 s. La caída es presupuesto de tiempo, no el peso. Solución: que `buildRequest` escale `timeLimitSeconds` según el tamaño del pool (p.ej. 60 s hasta 100, 120 s hasta 200) en vez de un fijo; el request ya admite el override. No urgente: el piloto son 50-60 conductores. Origen: Bloque 35.2.
- `TODO[rejilla-mi-horario-unificar]` — B36.1 extrajo `construirRejilla` (lib/calendario, puro, ausencias cruzadas) y lo comparten Calendario y las exportaciones, pero **Mi horario** sigue montando su propia lista día a día (`DiaVista`: turno con horario/minutos/línea + ausencia) en `MiHorarioPage`; solo reutiliza `descripcionAusencia`. Unificarlo exigiría que la celda de la rejilla llevase horario y minutos (hoy solo código/color/title) y que Mi horario consumiese una rejilla de UN conductor. Se dejó a propósito (coste > valor en B36.1). Encaja cuando el PDF individual por conductor (B36.4) necesite la misma vista día a día que Mi horario: entonces la celda compartida se enriquece y Mi horario pasa a leerla.

## 13. Decisiones canónicas de diseño

Decisiones de diseño aprobadas durante la planificación de cada bloque y consolidadas aquí como referencia para implementaciones futuras. Subdivididas por bloque. Las decisiones internas (no canónicas) viven en los mensajes de commit, no aquí.

### Bloque 3 — callables crearJefeTrafico + crearConductor

- **D3.1 — `conductorId` desacoplado del `uid` de Firebase Auth.** El identificador del conductor coincide con el número de empleado (información de negocio que debe sobrevivir a cambios de cuenta de Auth) y NO con el `uid` del documento `/usuarios`. El enlace usuario↔conductor se hace mediante el campo `conductorId` en el documento `/usuarios`.
- **D3.2 — `crearConductor` crea `/usuarios` y `/conductores` atómicamente.** El callable escribe ambos documentos en una única operación. Si alguna de las dos escrituras falla, rollback completo (incluyendo el usuario de Firebase Auth si ya se creó). Un conductor sin sus dos documentos no es funcional.
- **D3.3 — Contraseña inicial vía `generatePasswordResetLink`.** El email de configuración de contraseña se envía con `generatePasswordResetLink` del Admin SDK. NUNCA devolver contraseñas en la respuesta del callable ni escribirlas en logs. Si el usuario pierde el enlace, se repite el flujo desde super_admin.
- **D3.4 — Validación de payloads con type guards a mano.** En este bloque NO se introduce Zod: se valida cada campo con type guards y se lanza `invalid-argument` con mensaje claro al fallar. Si los callables crecen a 10 o más se refactoriza (ver `TODO[refactor-zod]` en §12).
- **D3.5 — Sesión 3 partida en dos sub-sesiones por tamaño.** Bloque 3.1: scaffold del paquete `apps/functions` + callable `ping` (completado). Bloque 3.2: callables `crearJefeTrafico` y `crearConductor`.
- **D3.6 — Verificación de existencia de referencias antes de crear.** `crearJefeTrafico` verifica que `/tenants/{tenantId}` y `/centros/{centroId}` existen. `crearConductor` verifica lo mismo y además que el `tenantId` del payload coincide con el `tenantId` del jefe que llama (anti cross-tenant). Si falla la verificación, devolver `invalid-argument` indicando qué referencia no existe.
  - **Ampliación 3.2.d:** para `crearConductor` se verifica también que `claims.centroId === payload.centroId` cuando invoca un jefe (anti cross-centro), por simetría con la identidad operativa del jefe `(tenantId, centroId)`. Mensaje: "Un jefe de tráfico no puede crear conductores en otro centro." (DUDA-11 de 3.2.d). Sin esta ampliación, un jefe del centro A podría crear conductores en centro B del mismo tenant, lo cual es abuso de permisos. Implementación: commit `e810832`.
- **D3.7 — Auditoría mínima en cada documento creado.** Los documentos nuevos en `/usuarios` y `/conductores` incluyen `creadoPor` (uid del invocador, `request.auth.uid`) y `creadoEn` (`FieldValue.serverTimestamp()`).
  - **Ampliación Bloque 4:** bootstrap CLI usa `creadoPor: 'bootstrap-cli'` como valor convencional al no existir `request.auth.uid` (script ejecutado fuera del flujo callable, sin invocador autenticado). Implementación: `scripts/bootstrap-super-admin.mjs`. Grep-eable para localizar todos los super_admins creados por bootstrap CLI vs por callables (que tendrían un uid real en `creadoPor`).

### Bloque 6 — routing protegido + sidebar por rol

- **D6.1 — ProtectedRoute solo gatea autenticación, no rol específico de ruta.** Defensa en profundidad por capas: UI (sidebar oculta items que el rol no debe ver, mejora UX), routing (`<ProtectedRoute/>` valida status+user.rol, evita accidentes con URL bar), backend (reglas Firestore rechazarán lecturas cuando lleguen en sesiones 4+, ÚNICA capa de defensa real). Si surge necesidad de `RoleGate` explícito por ruta, sub-bloque aparte sin tocar este contrato.
- **D6.5 — `homeForRol(rol)` agnóstico al destino.** Mapping `ROL_HOME: Record<Rol, string>` desacopla la convención. Añadir un cuarto rol futuro (ej. `inspector`) requiere solo añadir entrada al record, sin tocar `LoginPage`, `ProtectedRoute` ni `NotFoundPage`.

### Bloque 7 — forzado de cambio de password en primer login

- **D7.3 — `refreshAuthUser()` en AuthContext re-lee el doc `/usuarios` y actualiza `user` state.** Establece patrón canónico: cualquier mutación server-side que el frontend deba reflejar en estado inmediato se canaliza por este método. Aplicable a hidrataciones futuras (tenant/centro en Topbar — ver `TODO[topbar-tenant-centro-hidratado]`) y a estados que dependan del doc `/usuarios`.
- **D7.9 — Hidratación de AuthContext lee `/usuarios/{uid}` directamente con Firebase Web SDK, apoyándose en la regla self-only existente (`ownerOfDoc(uid)`) en `firestore.rules`.** Establece patrón canónico: las hidrataciones del frontend (tenant/centro pendientes, mi-horario, CRUDs en Sesiones 4+) leerán Firestore directamente apoyándose en reglas declarativas, NO en callables hidratadores. Tocar la regla `read` self-only sobre `/usuarios` rompe el flujo de login del Bloque 7 (hay un comentario explicativo encima de la regla en `firestore.rules`).

### Bloque 4 — Tenants y Centros

- **D4.1 — `actualizadoPor` + `actualizadoEn` como auditoría obligatoria de UPDATE en todo callable de modificación.** Refinamiento de D3.7 (que solo cubría CREATE con `creadoPor`/`creadoEn`). Todo callable cuyo verbo es "actualizar" escribe ambos campos en cada write, sin excepción. Implicación: los tipos del modelo añaden `actualizadoPor?: string` y `actualizadoEn?: Timestamp` (opcionales para retrocompatibilidad con docs legados). Aplica retrospectivamente a futuras refactorizaciones de `marcarPasswordCambiada` si se considera UPDATE auditable.
- **D4.2 — Defaults de campos opcionales aplicados en backend, no en frontend.** Los callables de creación rellenan defaults (`configuracion.zonaHoraria='Europe/Madrid'`, `configuracion.idioma='es'`, `estado='activo'`, `plan='basico'`, etc.) cuando el payload los omite. El form puede no enviarlos. Centraliza la regla en un único lugar y evita drift cliente/servidor. Aplica retroactivamente a `crearJefeTrafico`, `crearConductor` y `bootstrap-super-admin.mjs`, que ya defaultean campos del modelo (`passwordChangeRequired=true`, `estado='activo'`, etc.) en backend; D4.2 formaliza el patrón implícito.
- **D4.3 — Soft-delete se implementa como UPDATE del callable `actualizar*` con cambio del campo `estado`, NUNCA callable separado `eliminar*`.** Hard-delete reservado a super_admin manual desde consola Firebase para limpieza de datos contaminados (sin UI). Las reglas Firestore permiten `delete` solo a super_admin pero la web NO expone esa acción. Implicación: simplifica el menú de callables (4 por entidad principal: crear, actualizar, …) y unifica auditoría (`actualizadoPor` registra quién canceló/inactivó).
- **D4.4 — Validaciones de dominio (CIF, futuras: DNI, IBAN, matrículas) viven en `packages/shared/src/validators/` como funciones puras sin dependencias externas, compartidas entre frontend y backend.** Mientras `TODO[refactor-shared-build]` esté abierto, el validador se duplica SOLO en `apps/functions/src/cif-validator.ts` (copia necesaria porque `tsc → CJS` no resuelve los .ts de shared en runtime). `apps/web` consume la copia canónica de `packages/shared` directamente vía `@albius/shared` (Vite resuelve los .ts crudos del paquete). La copia 3 anticipada en B8 (`apps/web/src/lib/validators/cif.ts`) resultó redundante — ver `TODO[cif-copia3-redundante]`. La validación se ejecuta en frontend (UX instant feedback en blur) Y en backend (gate).

  Para CIF específicamente, el gate del backend admite escape hatch (`forzarCIF: true`) para casos legítimos no cubiertos por el algoritmo español estándar (empresas extranjeras, autónomos con DNI, sociedades civiles): el operador confirma conscientemente y se persiste flag `cifValidacionForzada: true` para auditoría. El patrón "escape hatch con flag de auditoría" será reusable para futuros validators de dominio cuando el caso real lo justifique.

- **D4.5 — Objetos compuestos en payload de callables (`configuracion`, futuros similares) tienen tratamiento asimétrico CREATE vs UPDATE.** CREATE: merge del objeto recibido con defaults backend. Permite payload parcial, los huecos se rellenan automáticamente. UPDATE: replace completo. El validator exige que el objeto incluya todos los sub-campos del modelo. Si el frontend quiere editar un sub-campo, debe hidratar primero el objeto entero, modificarlo, y re-enviarlo completo.

  Razón: evita ambigüedad de "envío `{idioma: 'en'}`, ¿qué pasa con `zonaHoraria`?". El replace en UPDATE hace explícita la responsabilidad del frontend. La asimetría es intencional: el alta es "rellena lo que falta", la edición es "este es el estado que quiero".

- **D4.6 — Soft-delete (cambio de estado a `'cancelado'`/`'inactivo'`) verifica que no hay entidades hijas activas dependientes antes de aceptar el cambio.** Si las hay, el callable rechaza con `failed-precondition` indicando qué bloquea (con conteo o lista). Cascada manual: el operador debe inactivar primero las dependencias. NO se implementa cascada automática (riesgo de borrado masivo accidental). Implementaciones del Bloque 9: Tenant → verifica Centros activos via query `where(tenantId, ==, X).where(estado, ==, 'activo')`. Aplicable simétricamente a Centro → Conductores activos (futuro Sesión 7).

- **D4.7 — Modal Dialog para CRUDs de entidades de Gobierno (Tenants, Centros, Usuarios).** Cuando el agente del CRUD es super_admin (uso interno, no comparte URLs), las altas y ediciones viven en Dialog modal con el listado de fondo, no en páginas separadas. Razón: preserva contexto + super_admins no comparten URLs. Reevaluar si una entidad pasa a tener 15+ campos. Origen: Bloque 10.

- **D4.8 — Listado client-side con re-fetch tras mutación, sin `onSnapshot`.** Para entidades administrativas de baja frecuencia (cardinality < 1000 y mutaciones < 10/día: Tenants/Centros/Usuarios en MVP), el frontend carga todo con `getDocs` sin paginación y filtra/busca client-side. Tras cada mutación exitosa, re-fetch completo del listado. NO usar `onSnapshot` (innecesario para datos administrativos de baja frecuencia, complica testing manual, consume reads). Paginación servidor cuando la entidad se acerque a 1000. Origen: Bloque 10.

- **D4.9 — Servicios `apps/web/src/lib/services/<entidad>.ts` como única superficie I/O Firebase desde la UI.** Componentes y páginas NO importan directamente `httpsCallable`/`getDocs`/`collection`. El servicio expone funciones tipadas que envuelven callables + queries. Centraliza tipos del wire, mapper de errores, transformaciones. Origen: Bloque 10 con `services/tenants.ts`.

- **D4.10 — Mapper de errores `HttpsCallable` centralizado (`lib/callable-errors.ts`).** Análogo a `auth-errors.ts`. Traduce códigos del wire (`invalid-argument`, `failed-precondition`, `unauthenticated`, `permission-denied`, `internal`, `unavailable`, `resource-exhausted`, `deadline-exceeded`) a mensajes en español. Preserva el mensaje específico del backend cuando es seguro mostrarlo (validation errors, mensajes user-friendly como el de D4.6); fallback genérico cuando no. Detección por duck typing, sin acoplar a la clase `FirebaseError`. Origen: Bloque 10.

- **D4.11 — Soft-delete UI vive en botón secundario destructivo separado del form (NO en el select de estado).** Form de edición excluye `'cancelado'` del select. Cancelación con Dialog de confirmación visualmente destructivo. Reactivación con botón "Reactivar" inline en fila de tenants cancelados. Refina D4.3 con su correspondiente decisión UX. Origen: Bloque 10.

- **D4.12 — Validators de dominio se ejecutan en `onBlur` con feedback inmediato + checkbox dinámico para escape hatch.** NO validar al cargar el form (no irritar al usuario antes de que escriba). Checkbox solo aparece cuando hay error real validable. Si el operador marca el checkbox, el form pasa `forzar*: true` al callable. Si edita el campo tras blur, se resetean tanto el flag de validado como el de forzar (el consentimiento se ata al valor concretamente evaluado). Aplicable a futuros validators (DNI, IBAN, matrículas) con la misma forma escape hatch. Origen: Bloque 10.

- **D4.13 — Páginas administrativas con gate suave de rol.** Las páginas reservadas a un rol específico (super_admin, jefe_trafico, etc.) renderizan `<NoAutorizadoView />` si `user.rol !== rolRequerido`. Defensa en profundidad sobre las reglas Firestore (que bloquean a nivel datos) + sobre la sidebar (que oculta el link). Evita render confuso si un user navega vía URL bar. **Patrón de implementación:** el gate vive en el componente exportado (ej. `TenantsPage`) que solo invoca `useAuth`; los hooks que dependen de permisos (`useState`/`useEffect`/`useMemo`) viven en un sub-componente (ej. `TenantsPageAuthorized`) renderizado solo si el gate pasa. Esto evita violación de Rules of Hooks si el rol cambia entre renders. Patrón reusable para `CentrosPage`, `UsuariosPage` y futuras pages administrativas. Origen: Bloque 10.

### Bloque 5 — Centros backend + frontend

- **D5.1 — Entidad hija valida que su entidad padre existe Y está en estado operativo antes de crearse.** `crearCentro` valida que el tenant existe y está activo; `crearLinea` validará centro activo; etc. Implementación: helper `assert<Padre>Activo` en `refs.ts`. Previene entidades huérfanas y entidades creadas bajo padres cancelados/inactivos. Origen: Bloque 11.

- **D5.2 — Semántica de códigos de error en gates de estado-padre: `invalid-argument` para "no existe", `failed-precondition` para "existe pero estado bloqueante".** Refina el patrón D4.6. Aplicado en `assertTenantActivo` (B11): tenant no existe → `invalid-argument` (ID inválido); existe pero no activo → `failed-precondition` (estado del padre bloquea). Aplicable a futuras `assertCentroActivo`, `assertLineaActiva`. La distinción permite al frontend separar "error de programación" de "estado bloquea, mensaje útil al operador". Origen: Bloque 11.

- **D5.3 — Entidades con 2 estados (activo/inactivo) omiten el Select de estado en el form de edición; las transiciones se gestionan solo con botones secundarios + Dialog de confirmación.** Refina D4.11 (que aplicaba a entidades con ≥3 estados como Tenant). Para entidades binarias como Centro: el form NO incluye campo estado; "Inactivar" y "Reactivar" son acciones explícitas inline con Dialog. Aplicable a futuras entidades de vida binaria. Origen: Bloque 12.

### Bloque 6 — Usuarios backend + frontend

- **D5.4 — Edición de campos dual-homed (Firestore + Firebase Auth) canalizada obligatoriamente por callable con escritura dual y rollback inverso.** Campos que viven a la vez en el doc Firestore y en el Auth user record (`email`→Auth email, `nombreCompleto`→Auth displayName) NUNCA se editan por escritura directa de cliente (las reglas `/usuarios` los vetan también para el owner). El callable escribe ambos sistemas en orden Auth primero, Firestore después: si la escritura Firestore falla, se revierte Auth al valor previo (rollback inverso, simétrico a `crear*`). El orden Auth-primero permite que colisiones (`email-already-exists`) aborten antes de tocar Firestore, sin rollback. Reusable para futuros campos dual-homed. Origen: Bloque 13.

  **Ampliación B21 — campos dual-homed ENTRE dos colecciones Firestore (no solo Firestore+Auth).** `crearConductor` copia `email`/`telefono`/`nombre`/`apellidos` tanto en `/usuarios` como en `/conductores`. Para evitar drift, el callable de la entidad SECUNDARIA veta esos campos y redirige al callable canónico de la entidad propietaria: `actualizarConductor` (B21) rechaza `email`/`telefono`/`nombre`/`apellidos` con `invalid-argument` y mensaje que apunta a `actualizarUsuario` (que sí mantiene la coherencia `/usuarios`↔Auth vía D5.4). El patrón "el campo se edita en UN solo callable, el resto lo vetan" se aplica tanto a Firestore+Auth (D5.4 original) como a colección↔colección.

- **D5.5 — Mutaciones de campos-identidad y claims se aíslan en callables dedicados, nunca en el `actualizar*` genérico.** `rol`, `tenantId`, `centroId` (custom claims) y `conductorId` (identidad del conductor, D3.1) quedan vetados en `actualizarUsuario` con `invalid-argument`. Sus cambios — cuando se necesiten — irán en callables dedicados con flags explícitos (`cambiarRolUsuario`, `moverUsuario`), porque arrastran re-set de claims + refresh de token en cliente + coherencia jerárquica. `conductorId` es inmutable de por vida. Mejor varios callables acotados que uno monstruoso. Origen: Bloque 13.

- **D5.6 — Callables que devuelven un secreto de un solo uso: el Dialog NO cierra al éxito; muestra pantalla intermedia con el secreto en input readonly + Copiar + aviso "no se mostrará de nuevo" + Cerrar explícito.** El secreto se renderiza antes del refetch del listado; el refetch va en background con `.catch` que solo loggea, para que un fallo de refetch nunca oculte el secreto. El cierre por overlay/Esc/X se bloquea mientras la pantalla de éxito está visible. Aplicable a `crearJefeTrafico`/`crearConductor` (devuelven `linkPasswordReset`, hoy distribuido manualmente — `TODO[email-transport]`) y a futuros callables con tokens/links de un solo uso. Origen: Bloque 14.

- **D5.7 — Selectores jerárquicos padre→hijo: dos `Select` encadenados donde el hijo se filtra por el padre seleccionado y se resetea al cambiar el padre.** El reset del hijo se encapsula en el componente del selector (no en cada form consumidor). Ambos niveles muestran solo entidades en estado operativo (activo). Si el padre no tiene hijos, el `Select` hijo se deshabilita con placeholder informativo y el submit queda bloqueado. Componente reutilizable (`TenantCentroSelect`). Aplicable a futuras jerarquías (centro→línea, línea→parada). Origen: Bloque 14.

### Bloque 7 — Líneas

- **D6.2 — Línea usa `estado` enum-3 (`'activa' | 'inactiva' | 'suspendida'`), excepción explícita y consciente a D5.3** (que mandaba binario activo/inactivo para entidades simples). Razón: casuística operativa más rica que Centro — la suspensión temporal estacional (línea de playa fuera de temporada) NO es lo mismo que la inactivación definitiva. Implica que el frontend de Líneas usa Select de estado (patrón Tenant D4.11), no botones binarios + Dialog como Centro (D5.3). Las entidades binarias siguen bajo D5.3; Línea es la excepción documentada. Origen: Bloque 16.

- **D6.3 — Unicidad del `codigo` de Línea es POR CENTRO, no por tenant.** Las redes urbanas de centros distintos son independientes: dos centros pueden tener cada uno su línea "1"; dos "42A" en el mismo centro se rechazan. Implementación: `assertCodigoLineaUnico(db, centroId, codigo, excludeLineaId?)` en `refs.ts` (análogo a `assertCIFUnico` de B9) + índice compuesto `(lineas: centroId+codigo)`. En UPDATE se revalida solo si el `codigo` cambia, con `excludeLineaId` para que la línea no choque consigo misma. Origen: Bloque 16.

- **D6.4 — Las entidades operativas nuevas nacen con auditoría canónica (`creadoPor`/`creadoEn` D3.7 + `actualizadoPor`/`actualizadoEn` D4.1) y NO arrastran `fechaCreacion`.** Línea es la primera: al no tener documentos legados, corta limpio al esquema canónico de auditoría. Refina el patrón para futuras entidades operativas (Paradas, Frecuencias, Tipos de turno), que deben nacer igual. La deuda de las 9 entidades viejas que aún usan `fechaCreacion` se rastrea en `TODO[unificar-fechacreacion-creadoen]`. Origen: Bloque 16.

- **D6.5 — Toda query de LISTADO desde cliente (`getDocs` con `where`) para un rol NO-super_admin debe incluir `where('tenantId','==',claims.tenantId)` para satisfacer la regla `read` `sameTenant(resource.data.tenantId)`.** Razón: en operaciones `list`, Firestore evalúa la regla SIN los documentos (`resource.data` es undefined), así que exige que la query CONSTRIÑA `tenantId` para poder probar la pertenencia — y de paso bloquea lecturas cross-tenant (un jefe filtrando un `tenantId` ajeno → `permission-denied`). Las colecciones super_admin-only (Tenants/Centros/Usuarios) nunca toparon con esto porque `isSuperAdmin()` cortocircuita el `||` antes de tocar `resource.data`; Líneas (B17) es la primera listada por un jefe. NO separar `get`/`list` con un `list` token-based (`allow list: isJefeTrafico() && token.tenantId != null` NO constriñe la query → leak cross-tenant: el jefe podría filtrar un centro de otro tenant y leer sus líneas). El patrón seguro es **constreñir la query, no relajar la regla** (verificado empíricamente con el SDK de cliente en B17). Aplica a futuros listados de jefe (frontend de Conductores, Paradas, Frecuencias). Origen: Bloque 17.

- **D6.6 — La duración de un turno (`duracionMinutos`, `duracionEfectivaMinutos`) se DECLARA explícitamente en el payload; NUNCA se calcula en backend desde `horaInicio`/`horaFin`.** Razones: (a) en turnos partidos la duración efectiva ≠ `fin − inicio` (hay corte intermedio); (b) en turnos que cruzan medianoche (`horaFin < horaInicio`) la aritmética se complica; (c) la duración a efectos de convenio puede diferir de la aritmética de horas (tiempo de bocadillo computable o no, redondeos del convenio). El backend valida **coherencia** (duraciones > 0, `efectiva <= total`, tramos del partido dentro de `[horaInicio, horaFin]` cuando NO cruza medianoche) pero NO impone la aritmética. Los consumidores futuros (cuadrante, optimizador) **consumen la duración declarada, no la recalculan**. Nota relacionada (sin canónica propia): TipoTurno resolvió el "eje mezclado" igual que Línea (D6.2) — se eliminó `tipoFranja` (que solapaba franja-horaria con `esPartido`) en favor de `esNocturno` boolean ortogonal + `esPartido` boolean; y su `estado` usa el patrón binario **D5.3** (`activo`/`obsoleto`, sin estado intermedio tipo suspensión estacional, a diferencia del enum-3 de Línea). Origen: Bloque 18.

- **D6.7 — La verificación de UI se hace con tests E2E de Playwright (`apps/web/e2e/`), no con checklist manual.** Reemplaza el verify manual en navegador de B17/B19. Patrón canónico del andamiaje (B20):
  - **Login una vez** vía proyecto `setup` que guarda `storageState` con **`indexedDB:true`** (Firebase Auth Web SDK persiste la sesión en IndexedDB, no en cookies/localStorage); los specs declaran `dependencies:['setup']` + `storageState` y arrancan autenticados, sin re-loguear.
  - **`webServer` propio en puerto FIJO** (`--strictPort`), independiente del puerto variable del dev server del usuario; el usuario no se preocupa del puerto.
  - **Emuladores = prerequisito asumido corriendo**; `globalSetup` los verifica (falla con mensaje claro si no), **siembra de forma idempotente** y hace **warm-up de los callables** (POST a sus endpoints) para evitar el cold-start del Functions emulator que cuelga la primera mutación de forma flaky.
  - Tests que **mutan estado resetean en `beforeEach`** recreando SOLO los datos de la entidad, **sin tocar los UIDs de Auth** (recrearlos invalidaría el `storageState`). `workers:1` (BD del emulador compartida → serie evita carreras).
  - **Cada entidad nueva con UI añade su propio spec** (`<entidad>.spec.ts`) + helper de reset.
  - Override ESLint para `e2e/**` + `playwright.config.ts` (node globals); E2E fuera de `tsc -b` (Playwright transpila aparte).

  A partir de B20, **la verificación de UI es automatizada** (`npm run test:e2e` con emuladores corriendo), no manual. Origen: Bloque 20.

- **D6.8 — Validación de NO-SOLAPAMIENTO temporal de tramos horarios.** Cuando una entidad tiene tramos `[horaInicio, horaFin)` que no deben solaparse dentro de un mismo agrupador (ej. Frecuencia: misma `lineaId` + `tipoDia` + `sentido`), se valida con la regla de intervalo **MEDIO-ABIERTO**: dos tramos solapan sii `aInicio < bFin && bInicio < aFin` (así tramos **adyacentes** 06:00–10:00 y 10:00–14:00 conviven sin falso conflicto; solo solapan si se pisan estrictamente). Se valida en el **callable** (NO en reglas Firestore — requiere leer otras filas del agrupador), y rechaza con `failed-precondition`. `excludeId` para que el `actualizar*` no choque consigo mismo; solo cuentan las filas **activas** (`activa===true`). En Frecuencia, además, `sentido: 'ambos'` colisiona con cualquier sentido (cubre ida+vuelta); en UPDATE se re-valida con los valores **efectivos** (payload ?? doc). Origen: Bloque 23 (primera validación de solapamiento temporal del proyecto). Reusable para futuros tramos no-solapables (paradas con ventanas, asignaciones, etc.).

- **D6.9 — Entidad SINGLETON por padre.** Cuando una entidad tiene exactamente UNA instancia por su entidad padre (ej. `Convenio`: uno por centro), se modela con **`id` = id-del-padre** (`id=centroId`), un **único callable UPSERT** (`guardar*`, NO `crear*`/`actualizar*` separados) que hace **set-si-no-existe / update-si-existe preservando `creado*`** (D3.7 en create, D4.1 en update), **sin campo `estado`** (no hay entre qué elegir; no aplica D5.3 ni soft-delete), **sin validación de unicidad** (el id determinista la garantiza por construcción, no hace falta `assert*Unico`), y **lectura por get-by-id directo** desde cliente — lo que **escapa del bug D6.5** del `list` (en un `get`, `resource.data` SÍ está disponible, así que `sameTenant(resource.data.tenantId)` evalúa bien sin necesidad de constreñir query ni índice). El callable veta los campos server-managed en el validador y, en UPDATE, los inmutables que identifican al padre (ej. `tenantId`). Reglas patrón `/lineas` (jefe-scoped + veto inmutables). Reusable para futuros singletons por padre. Origen: Bloque 25.

- **D6.10 — Materialización selectiva de asignaciones (el descanso ordinario NO se materializa).** En el cuadrante, el día libre normal de un conductor NO se persiste como documento: **"ausencia de fila `Asignacion` = día libre"**. Solo se crean documentos `Asignacion` para lo que **OCUPA** al conductor: `turno`, `reserva_presencial`, `reserva_localizable`, `vacaciones`, `baja`. El valor `'libre'` del union `TipoAsignacion` queda en el modelo por compat pero los callables `crear*`/`actualizar*` lo **rechazan** con `invalid-argument` (mensaje específico). Razón: materializar descansos serían decenas de miles de documentos vacíos al mes (≈178 conductores × ~60 días de horizonte) sin información útil. Coherente con la "materialización selectiva de servicios" del §8 (solo se crea `servicios` cuando hay algo que registrar). **Implicación para todo consumidor** (frontend del cuadrante, "mi horario", optimizador): al leer un cuadrante, la ausencia de asignación de un conductor en una fecha se interpreta como día libre — no se busca ni se espera un documento. Origen: Bloque 26.

- **D6.11 — Testing unitario (vitest) para lógica pura.** La lógica pura no trivial (algoritmos sin I/O: cálculo de calendario, transformaciones de datos, validadores de dominio…) se testea con **vitest en `packages/shared`** (ficheros `src/**/*.test.ts`, `npm test -w @albius/shared`), NO solo con scripts `verify-*.mjs` (que prueban I/O end-to-end contra el emulador) ni con E2E. Primer caso: `resolverTipoDia` (B27, 10 tests de casos límite). Criterio: una función pura **crítica** merece tests unitarios de sus casos límite (aquí, porque alimenta la demanda del optimizador). Las funciones que **SÍ** tocan Firestore (callables) se siguen validando con `verify-*.mjs` contra emulador (su valor es probar el I/O real + reglas), y la UI con E2E Playwright (D6.7). vitest es la tercera pata: pura, rápida, determinista, sin emulador. Origen: Bloque 27.

- **D6.12 — Lógica de escritura reutilizable (`logic/` separado del handler).** Cuando una operación de escritura debe ser invocable tanto por un **callable de usuario** (con `request.auth`) como por **otro contexto sin usuario** (un orquestador de servicio, otro callable), la lógica de escritura se extrae a una **función pura** en `apps/functions/src/logic/` con firma `(db, params, actorId) → resultado`, y el callable queda como **envoltorio fino** que hace SOLO auth + anti-cross (`assertJefePuedeTocarCuadrante` se queda en el callable) y delega con `actorId = invocadorUid`. Los **guards de identidad de usuario NO bajan** a la función extraída; esta solo valida lo **estructural** (cuadrante editable, fecha-en-mes) y ejecuta la mecánica de Firestore (batches). El `actorId` se persiste como `creadoPor`/`actualizadoPor`; convención de **actor de servicio `actorId='optimizador'`** (paralelo a `bootstrap-cli`/`system-seed` de D3.7/§14.9). Esto **habilita la Opción C** del optimizador (B29 llama estas funciones sin ser un usuario autenticado). Primeras funciones: `escribirAsignacionesLote`/`limpiarAsignacionesCuadrante`/`regenerarAsignaciones` (`logic/asignaciones.ts`) + `actualizarCuadranteDoc` (`logic/cuadrante.ts`). NOTA: operaciones que superan los límites de Firestore (500 ops/batch, 500 docs/transacción) **no son atómicas** — se trocean en batches y se documenta el no-atomicidad + la estrategia de reintento (idempotente). Origen: Bloque 28.

### Bloque 8 — Optimizador end-to-end (B29 Fase C)

- **D6.13 — Arquitectura del optimizador: Opción C + C1.** El motor (Python/Pyomo/HiGHS en Cloud Run) es una **función pura a gran escala**: recibe un `OptimizarRequest` (demanda + convenio + conductores + calendario) y devuelve un PLAN (`asignaciones[]` + `estadisticas`); **NO toca Firestore** (Opción C: la lógica de escritura vive en un solo sitio, TS, vía la maquinaria de B28). **C1**: una Cloud Function Node **lee** Firestore, compone el JSON, **llama** al Python autenticado y **escribe** el plan con `regenerarAsignaciones` + `actualizarCuadranteDoc` (`actorId='optimizador'`, D6.12). El cliente HTTP mintea un **ID token de Google** (`google-auth-library`, `getIdTokenClient(audience=URL_base)`) para invocar el Cloud Run **privado** (`--no-allow-unauthenticated`); la SA de runtime necesita `roles/run.invoker`. La URL se inyecta con `defineString("OPTIMIZER_URL")` (`.env`, no secreto). El contrato TS (`optimizer/contract.ts`) es **espejo 1:1 de `schemas.py`** (único campo no-ASCII: `año`, alias del `anio` de pydantic). Origen: Bloque 29.

- **D6.14 — Trabajo largo asíncrono vía Cloud Tasks (`onTaskDispatched`), NO trigger de Firestore.** Cuando una operación tarda más que un `onCall` razonable (el motor ~5 min), se parte en: un **callable corto** que valida + marca estado + **encola** una Cloud Task y devuelve en <1s (el usuario recupera el control), y un **worker `onTaskDispatched`** que hace el trabajo largo. Se elige Cloud Tasks (**techo 30 min**) sobre un trigger `onDocumentWritten` (**techo 540 s**, justo para 5 min) y sobre fire-and-forget sin await (frágil: Functions mata el proceso al devolver). La región va en el **resource path** del encolado (`locations/{region}/functions/{name}`) — el 2º arg de `taskQueue()` es `extensionId`, NO la región. `maxAttempts:1` (errores de app deterministas; ver `TODO[generacion-stuck-reset]`). Primer caso: `generarCuadrante` + `generarCuadranteWorker` (B29). Origen: Bloque 29.

- **D6.15 — Invocar callables de otra región desde el frontend: 2ª instancia `getFunctions` dedicada, sin tocar la existente.** Los 33 callables viven en `us-central1` (`functions = getFunctions(app,'us-central1')`); los callables del optimizador en `europe-west1`. Para invocarlos se añade **`functionsEu = getFunctions(app,'europe-west1')`** y se usa SOLO para esos callables, dejando `functions` intacta (cero regresión). NO se mueve toda la app a una región (re-desplegaría los 33, ver `TODO[firebase-region]`). Origen: Bloque 29 C.4.2.

- **D6.16 — `onSnapshot` para estado que cambia async en backend (excepción justificada a D4.8).** D4.8 manda *re-fetch tras mutación* (sin `onSnapshot`) para listas administrativas de baja frecuencia. **Excepción**: cuando un estado cambia en el backend **sin acción del cliente** (aquí, `estadoGeneracion` durante los ~5 min del worker), la suscripción reactiva `onSnapshot(doc(...))` es lo idiomático (el cliente no puede saber cuándo refrescar). `estadoGeneracion` (`idle`/`generando`/`completado`/`error`) es un **eje ORTOGONAL** al ciclo de vida del cuadrante (`borrador`/`publicado`/`cerrado`); lo escribe el backend (Admin SDK) y el front solo lo lee/observa. El callable de generación es **fire-and-forget**: devuelve `{ok,cuadranteId}` y NO trae el plan; el plan llega por el `onSnapshot`. Origen: Bloque 29 C.4.2.

- **D6.17 — Seed de datos a PRODUCCIÓN: Admin SDK directo (no callables) con el patrón de guardas de bootstrap.** Para sembrar volumen de prueba/inicial (38 tipos + 60 conductores + convenio + cuadrante), se escribe **directo con Admin SDK** (no callables: evita 60 Auth users innecesarios — el optimizador solo lee `tipos_turno`/`conductores`/`convenio`), **respetando a mano** todos los invariantes del modelo (campos requeridos, Timestamps reales, ids compuestos, **doc-id de tipo de turno = código** para coherencia trivial). Mismas **guardas que `bootstrap-super-admin.mjs`**: `--target production` obligatorio, `project_id===albius-cbdb1`, detección de credenciales (ADC **multiplataforma**: `CLOUDSDK_CONFIG` → `%APPDATA%\gcloud` en Windows → `~/.config/gcloud` POSIX), veto de env-vars de emulador, confirmación `CONFIRMAR`, idempotente (limpia su ámbito antes). **ASSERT de coherencia de IDs en el propio script** (cada `tiposTurnoPermitidos` debe resolver a un tipo sembrado, o aborta). Validable contra emulador antes de producción. Origen: Bloque 29 C.4.3/C.4.4.

### Bloque 9 — Calendario + Ausencias (B30/B32)

- **D6.18 — `lineaId` en `TipoTurno` es OPCIONAL y es una etiqueta de AGRUPACIÓN, no una dependencia operativa.** Se valida con `assertLineaDelCentro` **laxa** (la línea existe y es del mismo centro; NO exige `estado='activa'`), a diferencia de `Frecuencia`, que usa `assertLineaActiva` porque una frecuencia sobre una línea muerta no tiene sentido. Razones: un turno asociado a una línea estacional suspendida sigue siendo significativo para colorear/agrupar el cuadrante, y la simetría crear/editar evita "puedo crearlo pero no re-guardarlo porque la línea se suspendió". Un turno sin línea sigue siendo válido (turnos especiales). Origen: B30.1.

- **D6.19 — Las ausencias NO se materializan como asignaciones: colección `ausencias` como FUENTE ÚNICA.** Razones: (a) `regenerarAsignaciones` borra TODAS las asignaciones del cuadrante antes de volcar el plan y las destruiría en cada regeneración; (b) una baja larga cruza varios cuadrantes (grano mensual); (c) una ausencia existe aunque no haya cuadrante generado. No contradice D6.10 (que habla del descanso ordinario): `'vacaciones'`/`'baja'` siguen en `TipoAsignacion` por compat, pero **los consumidores (optimizador, Calendario, Mi horario) leen `ausencias` y cruzan**; el optimizador las recibe como `ausencias[]` (pares conductor-día) y no crea la variable. `Ausencia` es entidad propia, NO especialización de `Incidencia` (evento imprevisto sobre cuadrante publicado). Origen: B32.0/B32.1.

- **D6.20 — Tipo de ausencia = CATEGORÍA cerrada + CÓDIGO libre; rango CERRADO.** `categoria: 'vacaciones'|'baja'|'permiso'` es lo que entienden el motor y las estadísticas; `codigo?` es la sigla de cada empresa ("V", "E", "B", "AP", "PS", "PF"…), libre para escalar a varios clientes sin tocar el enum. Un solo modelo `fechaInicio`/`fechaFin` inclusive con **`inicio <= fin` (iguales = día suelto)** cubre tanto las semanas (V/E/B) como los permisos de un día (AP/PS) de los cuadrantes reales — distinto de `assertVigenciaCoherente` (Línea), que exige estricto `<`. **No-solape CERRADO por conductor** (`aIni <= bFin && bIni <= aFin`; tocar en el mismo día YA es solape, a diferencia del medio-abierto de D6.8 para tramos horarios). En update parcial de una sola fecha se cruza contra el valor **persistido**. RET (retén) no es ausencia sino turno de guardia. Origen: B32.1.

- **D6.21 — Los conductores ausentes el MES COMPLETO se excluyen del pool en `buildRequest`; los parciales entran con sus días recortados.** Si un conductor sin ningún día disponible entrara en el pool, todas sus variables se anularían y su `wload=0` contaminaría la media del término de equidad del motor (`span` vs `mean_load`), empujando a bajar la carga de los demás — con el ~17% de ausentes totales de TUCARSA no es un caso raro. La exclusión es un parche en el orquestador (`TODO[equidad-normalizar-dias-disponibles]`); cuando el motor normalice por días disponibles, sobra. Si la exclusión deja el pool vacío o algún tipo de turno sin ningún conductor disponible habilitado, `buildRequest` lanza un **error legible** (el worker lo refleja en `errorGeneracion`), nunca un plan con déficit inexplicable. Origen: B32.3.

- **D6.22 — Toda fecha del calendario y de las ausencias se maneja en UTC (`Date.UTC` / `getUTC*` / `toISOString().slice(0,10)`), nunca con getters locales.** El seed, el optimizador y los callables persisten las fechas a medianoche UTC; los inputs `date` envían "YYYY-MM-DD" que el backend parsea como medianoche UTC. Usar `getDate()`/`new Date(y,m,d)` locales en Europe/Madrid (UTC+1/+2) desplazaría el día (una ausencia del 20 se mostraría el 19, un turno caería en la columna equivocada). Aplica a `lib/calendario.ts`, `expandirAusenciaEnMes` (shared + espejo), `buildRequest` (`dias[]`), `services/ausencias.ts` (`tsToISODateUTC`) y a cualquier vista futura (Mi horario). `LineaForm.tsToDateInput` (B17) usa getters locales para la vigencia — deuda menor a alinear cuando se toque. Origen: B30.3 / B32.2.

### Bloque 10 — Publicación + edición manual del cuadrante (B33)

- **D6.23 — Validación de asignaciones manuales HÍBRIDA: el backend RECHAZA lo estructural, la UI AVISA y PERMITE lo de convenio.** Estructural = lo que rompe el modelo y la rejilla ocultaría: **R1** (un conductor no puede tener dos asignaciones el mismo día en el mismo cuadrante; `assertConductorLibreEnFecha` con `excludeId` para el update) y **referencias** (`conductorId` y `tipoTurnoId` existentes y del centro del cuadrante). Es crítico porque la rejilla del Calendario asume R1 ("primero gana"): un doble turno sería **invisible en pantalla pero contaría en KPIs y en "mi horario"**. Lo de convenio (descanso entre jornadas <12 h, conductor no habilitado para el tipo, ausente ese día) se calcula **en cliente** (`calcularAvisosAsignacion`, `lib/calendario.ts`) y se muestra como aviso no bloqueante con botón "… de todos modos": **un jefe necesita poder saltarse una regla por una urgencia real** (cubrir un hueco con quien sea); si se le bloquea, lo resuelve fuera de la herramienta y la app pierde la verdad. En `actualizarAsignacion` se valida contra los valores **efectivos** (payload ?? persistido) y R1 solo si cambian conductor o fecha. Origen: B33.2.

- **D6.24 — `crearAsignacionesLote` NO valida referencias ni R1 a propósito.** Lo usa el optimizador (vía `escribirAsignacionesLote`, D6.12) con ids reales del centro que `buildRequest` ya ha resuelto y con R1 garantizada por el propio MILP; validar de nuevo ~900 filas sería coste sin valor. La validación estructural de D6.23 aplica solo a las escrituras **manuales** (`crearAsignacion`/`actualizarAsignacion`). Origen: B33.2.

- **D6.25 — Publicar CONGELA el cuadrante; reabrir es la salida de emergencia; cerrar es definitivo.** `publicado` y `cerrado` son inmutables por callable (`assertCuadranteEditable` no se relaja): la edición post-publicación es el bloque de **Intercambios**. `reabrirCuadrante` (publicado→borrador) existe solo para deshacer un error de publicación: **solo desde `publicado`**, nunca desde `cerrado`; borra `fechaPublicacion`/`publicadoPor` (describen la publicación vigente, no un histórico); al re-publicar se vuelven a sellar. Publicar exige además `estadoGeneracion!=='generando'` (no se congela un cuadrante que el worker va a sobrescribir). La UI refleja el estado: botón Publicar solo con `borrador && completado`, celdas del Calendario clicables solo en borrador, motivo visible si no. Revisar reabrir cuando existan intercambios (`TODO[reabrir-cuadrante-vs-intercambios]`). Origen: B33.1.

- **D6.26 — Los asserts de referencia para escrituras puntuales son LAXOS (no exigen estado activo).** `assertConductorDelCentro` (B32) y `assertTipoTurnoDelCentro` (B33.2) comprueban **existencia + pertenencia al centro**, NO `estado==='activo'`: se puede asignar puntualmente un tipo de turno obsoleto o registrar una ausencia/asignación de un conductor inactivo, y la simetría crear/editar evita "puedo crearlo pero no re-guardarlo". Mismo criterio que `assertLineaDelCentro` (D6.18). Los asserts **estrictos** (`assertCentroActivo`, `assertTenantActivo`, `assertLineaActiva`, D5.1) quedan para crear entidades HIJAS bajo un padre operativo. Origen: B32.1 / B33.2.

### Bloque 11 — Mi horario del conductor (B34)

- **D6.27 — Las queries del conductor REPLICAN la regla Firestore que las autoriza.** Un `list` se evalúa **sin `resource.data`** (D6.5), así que si la regla exige `resource.data.conductorId == request.auth.token.conductorId`, la query DEBE constreñir `where('conductorId','==',<claim>)`; si exige `estado != 'borrador'`, la query DEBE constreñir `where('estado','in',['publicado','cerrado'])`. Si no, Firestore rechaza el `list` con `permission-denied` **aunque todos los docs sean suyos** — y el error se confunde con un problema de auth. Aplica a `/asignaciones`, `/ausencias` (por `conductorId`) y `/cuadrantes` (por `estado`); `services/miHorario.ts` lo documenta en cabecera. Corolario de diseño: cuando se endurece una regla por campo, hay que revisar todas las queries de cliente de esa colección para ese rol (y añadir índice si la nueva constricción lo pide; aquí `(asignaciones: tenantId+conductorId+fecha)` ya existía de B26). El `conductorId` viaja en el **custom claim** (las reglas no pueden hacer `get()` a `/usuarios` por doc en un `list`) y la UI lo lee del **doc** `/usuarios` (funciona con token anterior al claim). Origen: B34.1 / B34.2.

- **D6.28 — El conductor ve cuadrantes PUBLICADOS y CERRADOS, nunca BORRADORES.** Un borrador es trabajo en curso del jefe (se regenera, se edita a mano, se puede vaciar): mostrarlo crearía expectativas falsas y llamadas al jefe. Los cerrados son el **histórico** del conductor (meses pasados, inmutables, D6.25). Se impone en dos capas: la regla de `/cuadrantes` (`estado != 'borrador'` para `isConductor()`) y la query/filtro de la vista; las asignaciones se filtran además en memoria a los cuadrantes visibles del mes. Un mes sin cuadrante publicado muestra un **mensaje honesto** ("aún no está publicado"), NUNCA "sin turnos" (que es información falsa: los turnos pueden existir en borrador). Origen: B34.0 / B34.2.

- **D6.29 — Las ausencias se cruzan SIEMPRE que se muestre disponibilidad de un conductor.** `Ausencia` es fuente única y NO se materializa como asignación (D6.19), así que cualquier vista que pinte días (Mi horario, Calendario, futuros PDF/"quién trabaja hoy") debe cruzar `ausencias` con `expandirAusenciaEnMes`; sin cruzarlas, un conductor de vacaciones aparece como **"libre"**, que es información falsa por omisión (y en Mi horario, además, le diría al conductor que está disponible cuando no lo está). El día con ausencia se etiqueta con su categoría (+ `codigo` de la empresa si lo tiene) y NO cuenta como día libre en los resúmenes. Origen: B34.2 (ya aplicado por el optimizador en B32.3 y por el aviso del Calendario en B33.2).

### Bloque 12 — Convenio y festivos (B35)

- **D6.30 — Las reglas BLANDAS del optimizador REPORTAN.** Toda restricción que el motor modela como penalización (no como restricción dura) debe exponer un **contador de incumplimientos** en `EstadisticasOutput`/`Cuadrante.estadisticas` y en los KPIs de `CuadrantePage`: cuántas veces y a cuántos conductores. Una blanda silenciosa es peor que no tenerla: el motor la incumple "un poco" y nadie lo ve hasta que lo ve el conductor. El jefe tiene que verlo antes que el conductor. `validar_solucion` recalcula los contadores DESDE el plan mensual completo (independiente del modelo), no desde las variables del solver. Primer caso: findes consecutivos (`findesConsecutivosExcedidos`, `conductoresConFindesExcedidos`). Origen: B35.2.

- **D6.31 — Jerarquía de pesos del objetivo: cubrir una plaza domina SIEMPRE sobre cualquier regla blanda.** `W_COV=10000` por plaza sin cubrir ≫ `W_FINDE=500` por finde excedido > `W_NOCT=8` por par de noches seguidas > `W_EQ=2` por minuto de span de carga. Un cuadrante con una plaza sin cubrir es peor que uno que incumple una regla blanda (el servicio sale a la calle; la regla se reporta y el jefe la retoca). Al añadir una blanda nueva, su peso se sitúa por debajo de `W_COV` con margen (20 incumplimientos < 1 plaza) y se justifica en el código en unidades comparables (aquí: un finde de más ≈ 4 h de desequilibrio de carga). Las restricciones que hacen ILEGAL el cuadrante (R1, R2, R3, racha, ausencias, habilitación) siguen siendo DURAS. Medir antes de desplegar: una blanda cambia la calidad del mejor factible dentro del tope de tiempo, no el reloj (`TODO[week-time-limit-por-tamano-pool]`). Origen: B35.2.

- **D6.32 — La UI distingue explícitamente qué reglas del convenio aplica el optimizador y cuáles solo se registran.** Honestidad de producto sobre completitud aparente: un formulario de 10 campos "de convenio" sugiere que los 10 se aplican; si el jefe descubre que el descanso semanal no se comprueba, la confianza en TODO el cuadrante se rompe. `ConvenioPage` agrupa los campos en "Aplicado por el optimizador" (con matiz dura / blanda / aviso en la ayuda de cada campo) y "Registrado, aún no aplicado". Cada vez que el motor incorpore un campo, se mueve de grupo en la misma PR (B35.2 movió findes y antelación). Aplica a cualquier futura pantalla de configuración cuyo efecto real sea parcial. Origen: B35.1 / B35.2.

- **D6.33 — Las reglas de convenio que el jefe puede necesitar saltarse conscientemente se implementan como AVISO confirmable, no como bloqueo.** Mismo criterio que la edición manual de asignaciones (D6.23): si se le bloquea, lo resuelve fuera de la herramienta y la app pierde la verdad. Primer caso: `antelacionMinimaPublicacionDias` — el dialog de publicar muestra "El convenio exige N días de antelación; publicas con M" y el botón pasa a "Publicar de todos modos"; el callable `publicarCuadrante` NO lo comprueba. Se calcula en la UI (tiene convenio y cuadrante) para no tocar backend. Criterio para decidir aviso vs bloqueo: bloqueo solo para lo que rompe el MODELO o la LEGALIDAD del cuadrante (R1, referencias, estado del cuadrante, generación en curso); aviso para lo que es política de la empresa. Origen: B35.2.

## 14. Procedimiento de trabajo (emergente, validado tras Bloques 3-5)

Patrón iterativo que ha demostrado valor en los últimos sub-bloques:

  Diseño → OK del usuario → Aplicación → OK del usuario → Verificación → OK del usuario → Commit → OK del usuario

Reglas operativas observadas:

1. **Pre-commit checks obligatorios**: `npm run build` + `npm run lint` antes de cerrar cualquier commit. Build incluye `tsc -b` (validación de tipos). Origen: lección incorporada tras `e879854` (push con lint en rojo que requirió hotfix `6d76b68`).
2. **Verificación empírica antes de aplicar cambios sugeridos**. Patrón: cuando se ve algo aparentemente raro (línea duplicada, archivo extraño, proceso colgado), primero verificar con `grep` / `cat` / lectura del log; modificar solo tras confirmar la condición real. Lección consolidada: el "bug aparente" fue artefacto visual en 3 ocasiones distintas (commit C2 del 3.2.e, `target` en bootstrap, Vite "colgado" del Bloque 5).
3. **Mensajes de commit detallados** con trazabilidad de decisiones `D-N` y `DUDA-N`, hashes referenciados, justificación de cambios técnicos. El commit debe permitir a un lector futuro entender el contexto sin abrir archivos adicionales.
4. **Deuda explícita**: cuando se pospone una decisión, se anota inmediatamente como `TODO[nombre-descriptivo]` en §12 con contexto, qué resolvería y cuándo encaja.
5. **Separación de scopes para scripts**: `apps/functions/scripts/` para testing infrastructure (`verify-*` que validan callables, `seed-*` que crean datos de prueba); `scripts/` raíz para operaciones CLI standalone (`bootstrap-*` para bootstrap del sistema). Cada tipo tiene perfil de fail-safe distinto: producción (6 capas en bootstrap), emulator-only (hardcoded en seed).
6. **Working tree por commit**: stage explícito por archivo, nunca `git add -A` o `git add .`. Cada commit tiene propósito autocontenido.
7. **Limpieza de procesos al cerrar sesión**: TaskStop por ID + verificación manual de huérfanos. El proceso `java` del Firestore emulator NO recibe el SIGTERM de Node y queda activo en cierres anteriores (3.2.c, 3.2.d, 3.2.e, 4, 5). Matar con `Stop-Process -Force` confirmando con CommandLine que es el Firestore correcto.
8. **`--target` o equivalente sin default** para cualquier script CLI que pueda tocar producción. Forzar elección consciente del operador (capa 1 de fail-safe).
9. **Sistema de auditoría con `creadoPor` en 3 niveles**: callable (uid real de `request.auth.uid`), bootstrap CLI (`'bootstrap-cli'`), testing seed (`'seed-test-user'`). Si en producción aparece `'seed-test-user'`, es alarma temprana de datos contaminados de testing.
10. **Diagnóstico antes que kill**: cuando algo parece colgado, leer log antes de matar proceso. El primer paso de diagnóstico es siempre `tail` del log + `netstat`. Lección: Vite estuvo listo en 449ms; el poll script fallido buscaba string con escapes ANSI que nunca matcheaban.
11. **Verificación de UI vía E2E Playwright, no manual** (D6.7). La UI se verifica con `npm run test:e2e` (Claude Code lo ejecuta), no con checklist manual en navegador. **Prerequisito: emuladores corriendo** (`npm run emulate`) antes de lanzar los E2E; `globalSetup` falla con mensaje claro si no los encuentra. Origen: Bloque 20.
12. **Cierre de bloque = commit Y push juntos** (desde Sesión 9, B32.3). El prompt de cierre incluye ambos; Claude Code hace stage explícito, commit con el mensaje dado y `git push origin main`, y reporta hash + rango real del push + `origin/main`. El **deploy** (functions / firestore rules+indexes / Cloud Run) sigue siendo un paso explícito del usuario, nunca implícito en el cierre; Vercel despliega la web sola desde `main`. Regla de oro aprendida en B31: **un cambio en `apps/functions/src` no está en producción hasta que hay deploy** — anotar en el cierre si el bloque lo requiere (y si toca rules/indexes).
