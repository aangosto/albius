# @albius/functions

Firebase Cloud Functions (Node.js + TypeScript).

Aún no inicializado. Estructura placeholder.

Pendiente:

- Instalar `firebase-functions` y `firebase-admin`.
- Configurar `firebase.json` y `.firebaserc` en la raíz.
- Implementar endpoints según `docs/01-especificacion-funcional.pdf`.
